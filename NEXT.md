# NEXT

**Next action: ③ DESIGN THE MODEL-SELECTION PROCESS (Role C).** ⛔ **Planning first, its own session** (defer-major-phases) — do not start building it on a session's tail. The methodological reason is the user's own: choosing a model means designing a *fair test*, and a fair test cannot be designed while the system underneath it is still moving. It has now stopped moving.
→ **ORDER (user, 2026-09-14):** ① instrumentation gaps ✅ → ② notify-and-route ✅ → ③ **model selection** → ④ dev DB + on-device Android, ⛔ **permission granted at that point, not before.** ⚠️ Model selection comes BEFORE device testing.

## ✅ Done 2026-09-14 — ① and ② both closed. Full record in `tasks.md`.
✅ **`GET /feedback` works**, with the first real-DB tests that query has ever had. ⚠️ **The obvious fix was worse than the bug:** `ORDER BY ts` parses and sorts the *string*, so `…00Z` outranks `…00.5Z` (`Z` 0x5A > `.` 0x2E) and one fractional second transposes two reports. Re-selecting the bare column does not help — the engine resolves the idiom to the **cast** projection. The ordering column needs its own name: `timestamp AS sort_ts`.
✅ **`Err(String)` from an axum handler is `200 OK` with a text/plain body.** Fixed here and in `notes.rs`, which had it identically; `routes/mod.rs::no_route_handler_fails_with_a_bare_string` guards the class and has its own test proving it fires. ⚠️ Tauri commands keep `Result<T, String>` deliberately — different boundary, documented in `commands/mod.rs`.
✅ **`store::read` caps text at `READ_BODY_CHARS = 12_000`** and ⛔ **states the cut twice** — inline and as `FullRecord::truncated`. The silent cut was the bug; a bare cap would have been a second one.
✅ **Phase 5's notify-and-route, widened by the user to EVERY approval queue on EVERY feature.** `core/src/approvals.rs::summary` is the single count; nav badges and `ApprovalsElsewhere` on the assistant screen both render it. ⛔ Routing is read from `inbox::DOMAIN_REVIEWED`, never restated. ⚠️ It takes the **boot feature snapshot**, not live config, or a badge outlives its tab.
🔴 **"Needs review cannot be expressed as a query" was WRONG** and is corrected in `tasks.md`: true of the catalogue's `FilterField`, false of SQL. ⛔ **No column, no version bump, no migration.** ⚠️ The *model-facing* filter is still unexpressible and still open.
🔴 **A defect in the above, found by the user asking how refresh works, and fixed.** The badge refetches on a sync-epoch bump and there was **one** bump site. ⚠️ `PullEvent::Applied` fires **only when `pulled > 0`** — an empty poll sends `Idle`, which nothing listens to, so the epoch ticks when *another device's* change lands, never on a timer. A queue cleared **locally** therefore refreshed nothing: clear all three documents and the Archive badge keeps saying 3, indefinitely on one idle device. ✅ `sync_refresh::bump_sync_epoch` is now the one named way, called from **three** sites — import-batch commit (which had open-coded it), `ProposalCard` decide, archive field correction.
✅ Gates green: fmt ×2, clippy ×4, backend 1146, frontend 163. ✅ **Browser pass done** (mock, Playwright, 1280 + 390): badges render in both navs, the reminder row routes to Archive, **0 console errors/warnings**, and the bump is confirmed live — accepting one of two mock proposals made the Assistant badge appear as "1" with no reload. ⛔ **Still unverified: nothing against a live assistant, nothing on device, `/feedback` not proven against the box's data, and a *cleared* document queue cannot be shown in mock** (its count is a constant) — that rides the real backend, along with confirming no **local** path creates an unverified document without a bump.

## ⛔ Three things stopped ON PURPOSE — reasons, not oversights
- 🔴 **`budget.set` was written, tested, then REMOVED.** Actions are discovered only via `describe_type` on the `{type}.` prefix, and there is no catalogued `budget` type. Registering one needs a `category` **column** on `budgets` plus a `BudgetProjection` version bump ⇒ **a projection rebuild** — a migration, not cleanup, and not to be run against his live database unattended. ⚠️ Note the contrast with the archive count above: that one avoided a bump *because* the predicate fit in SQL; this one cannot.
- ⚠️ **Six of nine projections never `.check()` their `init_schema`**, so a failed `DEFINE` returns `Ok`. ✅ Adding it to all six was tried and the suite passed — nothing fails on a *fresh* DB. ⛔ Stopped anyway: a statement failing against his *existing* data would become a refusal to start, which must not arrive via an overnight change.
- ⚠️ **The `belief` entry's promised `retired` filter does not exist** — retirement is `superseded_at`. The description states the truth now. ⛔ Hiding retired beliefs is **wrong**; they stay readable on purpose (the audit property).

## Handover — ⛔ `HANDOVER.md` is DELETED (user, 2026-09-14)
⛔ **Do not recreate it.** A third handoff file is the drift `NEXT.md` and `tasks.md` already suffer from. **Decisions here; inventory in `tasks.md` § Handover prerequisites.**
✅ **Blast radius ANSWERED — conditional, never blanket.** Inside the isolated environment (throwaway phone, separate dev server) wipe, reseed, reinstall and break things freely. ⛔ **The grant does not transfer** — the live box and his real phone are always outside it. ⚠️ **So isolation is itself a design task owed before the handover:** pointing at production must be hard to do *by accident*, not merely something to avoid. Fold this into ③'s planning, since the model-selection runs are the first thing that will exercise it.
⚠️ **Failure policy is still his call** — stop and wait, or take the reading that keeps the run going? 🔴 **`adb devices` is still EMPTY**, physical and only he can clear it. ⚠️ `adb` lives at `~/android-sdk/platform-tools/adb`, **not on PATH** — that is not a missing toolchain.

## Standing decisions — inherit, do not re-derive
- ⛔ **ON-DEVICE AND REAL-DATA PASSES ARE BATCHED AND DEFERRED** to ④. ⛔ Never flag one as a blocker. ⛔ **IMAP STAYS OFF**; never flip `OMNI_ENABLE_IMAP` without him.
- ⛔ **Live model runs exist ONLY to show the pipeline works** — ⛔ never to rank models, and ⛔ never as grounds to call anything optimized. That is precisely what ③ is for.
- ⛔ **THE ARCHIVE IS THE SPINE.** ⛔ **ONE EMAIL = 1 + N DOCUMENTS.** 🔴 **BLOB BYTES DO NOT SYNC** — one durable copy, the box; ⚠️ verify a backup by SIZE, never exit code. ⛔ The server backup is sequenced before the **real backfill**, not before dev-server work.
- ⛔ **THE JOURNAL IS NEVER PROPOSABLE.** ⛔ Beliefs are ask-only. ⛔ **Irreversible actions can never be granted autonomy** — `transaction.clear` is the first live instance. ⛔ **`verified` means AN ORACLE CHECKED IT**; model-written fields are **always** `verified: false`.
- 🔴 **Pre-existing privacy item, NOT mine and ⛔ NOT to be scrubbed unattended:** the institution name sits in `.archive/post-v1/tasks-completed.md` and `.archive/v1.0.0/tasks-completed.md`, invisible to the ingress-filter hook because it predates it. **Raise it; do not rewrite his history.**
- ⛔ **Do not re-survey, ALL CLOSED:** retrieval is catalogue-driven · blob store + LRU · the archive page and all three viewer bugs · `imap_real.rs` · the corpus · Phases D–G · the auto-import review · finance propose actions · **the whole archive plan, Phases 1–5**.
- 🔴 **Builds: `JOBS=1`, `CARGO_PROFILE_DEV_DEBUG=0`**, ⛔ + `source scripts/fetch-onnxruntime.sh` ⚠️ **after a leading `cd <repo>`** — without it the `--features embeddings` link dies on an undefined C++ symbol out of `ort_sys`. 🔴 **A FULL DISK PRESENTS AS A LINKER BUS ERROR** — ⚠️ **disk hit 96% during this session; clearing `target/debug/incremental` (3.5 G) + the wasm one (586 M) recovered it to 92%.** ⚠️ A filter matching zero tests still prints `ok`. ⚠️ Frontend clippy without `--all-targets` does not build tests — a `Debug` bound missing on `Tab` passed clippy and failed `cargo test`. ⛔ Keep logs OUTSIDE the repo — SessionEnd pushes to a **PUBLIC** remote.
