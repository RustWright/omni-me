# NEXT

**Next action: `tasks.md` § ▶ RUNBOOK Stage 2 — the C2 + D shared scorecard.**
⛔ **Stages 0, 1 and C1 are BUILT** — don't redesign them. Design lives in
`docs/src/isolation.md` and `MODEL_BENCH.md` Parts 4 and 6.

## 🔴 Owed before anything decides a seat
- **Nothing has run against a real endpoint. Zero tokens spent.** C1 is dry-run-verified only.
- **RE-RUN Role A's bench — now UNBLOCKED.** 10/14 (71%) was at the OLD turn budget; the three
  boxes that gated it were stale and are cleared. Needs a seeded local hub and spends tokens.
- **`omni_require_dev_volume`'s docker read is unproven** — no daemon here, he declined one.
  ⛔ Prove it before Stage 4 leans on it; recipe in `deploy/smoke-guard.sh`'s header.

## ⛔ Decisions in force — inherit, do not re-open
- ✅ **An instrument must not make abstention structurally impossible** — it cost a whole bench
  run in Stage 1. ⛔ The C2/D form is **a schema with no way to say "this field is not in this
  document"**: check the envelope BEFORE spending a run. ⛔ Abstention outranks field accuracy.
- ✅ **NEW: multi-part documents FIXED** (his call, 2026-09-15) — `extract`/`read_document` take
  `&[DocumentPart]`, one shared image budget. ⚠️ **No producer yet**; wiring one is an API call.
- ✅ **Two product findings stay RECORDED, not fixed** (`MODEL_BENCH.md` Part 6): one date
  applied to all N statement postings · `ExtractionResult.total`'s doc contradicts its own
  prompt. ⛔ Both are design calls, his to make — don't repair them mid-stage.
- ✅ **NEW: the privacy pre-commit hook fired and was right** — a test literal put a real
  institution name in the PUBLIC repo. ⚠️ Read denylist hits by **file and line number**, never
  by printing the line: this transcript renders to `.log/` and syncs.
- ✅ **"No real data" was always a WRITE rule.** ⛔ Intact. The box and his phone are outside the
  grant, always. ⛔ No deploy, no SSH, no `reset-db.sh` against anything live.
- ✅ **STOP AND WAIT on a significant failure**, and write down what for and what came next.
- ⛔ **NO HAND-LABELLED GOLD SET.** C1 arithmetic-checkable · C2 **no oracle** · C3 **no
  producer**; C2 and D share one scorecard.
- ⛔ **No seat decided on a saturated instrument**, nor ever on preference. Thresholds first.
  ⚠️ OpenRouter credits are a GOAL — screen generously, deciding run goes direct. ⛔ Re-resolve
  every model id; `-Ultra` is 403 on this account. ⚠️ Vision parity is still unproven.
- ⛔ **Do not re-survey, ALL CLOSED:** catalogue retrieval · blob store + LRU · archive page ·
  `imap_real.rs` · Phases D–G · finance propose · notify-and-route · role wiring · isolation
  guard · bench de-saturation · **the 413 encoded-payload hazard, already handled in
  `core/src/extraction/media.rs`**. ⛔ **No `HANDOVER.md`.**
- 🔴 **`cargo test --workspace` FILLED THE DISK to 8.0K** — `omni-me-app`'s test binary links the
  whole Dioxus frontend. ⛔ Scope test runs to changed packages; `cargo clean -p` on the four
  members freed 10.6 GB. [[project-dev-machines-and-build-limits]].
