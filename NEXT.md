# NEXT

**Next action: FINANCE PROPOSE ACTIONS — but read the trap first.** The plan calls it *contained*: `ActionKind` (`actions.rs:51`) + one `build_events` arm + registering the `transaction` catalogue fixture behind `Feature::Finances`. ⛔ **Approval routes to the FINANCES UI, never the assistant inbox.** 🔴 **THE TRAP: `catalog.rs` says in TWO places that "Finances is deferred indefinitely… nothing registers one"** — on the `budget` entry in `NOT_QUERYABLE` and on `transaction_shape_is_expressible`. ⚠️ **Those comments PREDATE the user reopening finance (2026-09-11) and are stale, not decisions.** Registering the entry is correct now; ⛔ update both comments in the same change or the next reader will treat the registration as a mistake and revert it.
→ **WHY FINANCES AND NOT MORE ARCHIVE:** ⑤ said *archive first, then finances*, and **the archive's feature work is now done** (page, viewers, email rendering, assistant catalogue). What is left there is **blocked** (purge → Role C; notify-and-route → two undecided designs) or **operational** (server backup, a sequenced dependency of the REAL backfill, not of feature work). So finances is what ⑤ points at next — derived from state, not a new priority.
→ **ORDER (user, 2026-09-12):** ① draft→email ✅ · ② compact ✅ · ③ Phase 5 ✅ · ④ auto-import review ✅ · ⑤ **archive ✅ → FINANCES ← here.**
✅ **EMAILS RENDER IN THE ARCHIVE (2026-09-12)** — ⑤'s archive half. `parent_document_id` on `DocumentRow`, `document_children()`, a **`mime_type`** filter, command + bridge; frontend `EmailView` (headers/body from stored text, attachments listed as the child documents they are and clickable through), `ParentLink` for the reverse direction, and a "Mail only" toggle. ⛔ **An email renders from its STORED TEXT, never its bytes** — bytes are raw MIME, so the byte viewer would show base64 payloads, and re-parsing MIME in wasm would duplicate ingest and give a second implementation to disagree with the first. ⚠️ The mock email fixture deliberately has **no bytes**, so any attempt to fetch them fails loudly. ⚠️ `is_email()` and the filter both key on **`mime_type`, never `kind`** — mail has no kind and never will until extraction runs, so a kind-based check reports every email as an ordinary file. ⚠️ `split_email_text` depends on `archive::derive_text`'s format with **nothing linking them**; it degrades rather than breaks (no blank line ⇒ all body) and the comment cites RFC 5322 so the rule survives a change upstream.
⛔ **FEATURE-COMPLETE IS THE PRECONDITION FOR HANDING CLAUDE AUTONOMOUS OPERATION** (user, 2026-09-12, his words): *"so I can set you up to be able to run the dev server and on device testing independent of me and work on catching and cleaning up all the bugs."* He is the bottleneck **only for product decisions** — testing and model selection proceed without him, so doing them first inverts the dependency. ⛔ That is why real-data and on-device are deferred: **sequencing, never a quality compromise.** → **Sort work by "does this need him?"** Surface **product** forks; routine *implementation* calls stay mine. ⚠️ **The enablers of that autonomy are in scope too** — a device, the dev sync server (`project-dev-sync-server-requirement`: second port + seeded DB), a credential. **Surface any that need his hands NOW**, not at the handover, or the window this strategy protects is wasted. Memory: `project-feature-complete-before-testing-strategy`.

## ④ Auto-import review — DECIDED 2026-09-12, inherit these
⛔ **The v1 objection restated, so it is not misremembered:** *"The objection is open-ended gate config, not automation and not email as a source. Any replacement has to make 'which mail is relevant' self-maintaining."* The archive splits that into **what is worth keeping** vs **what becomes a transaction**; only the second still needed answering.
- ✅ **FETCH SCOPE = ALL MAIL.** ⚠️ **A credentials change, not a code change** — `watched_label` goes straight to `select()` and `imap_real.rs` already names `"[Gmail]/All Mail"` as valid. Retires the hand-maintained Gmail label, which was the *upstream* gate: mail outside it could never be archived, searched or reviewed.
- ✅ **FULL HISTORY, PACED.** ⚠️ **Also no code change.** `None => "*"` fetches only the latest (a guard against *accidental* backfill) but `Some(uid) => "{uid+1}:*"` — so **seed the cursor to `Some(0)`** and the existing `truncate(MAX_UIDS_PER_TICK)` drains everything at 200/tick ÷ 30 min ≈ 9,600/day. ⛔ **Seed the cursor; do NOT delete the guard** — it is right, just pointed the wrong way for a deliberate run.
- ✅ **DRAFTING IS TWO-TIER, AND THE FAST PATH MAINTAINS ITSELF.** Tier 1: sender patterns → deterministic parser, cheap and immediate. Tier 2: everything else → the **scheduled model pass**, so ⛔ nothing is silently missed the way a patterns-only gate misses every receipt from an unlisted sender. ✅ **After a sender recurs, the model PROPOSES a fast-path addition and the user only approves/rejects** — he never edits the list. That is the v1 requirement met: derived-and-approved, not hand-maintained.
  - 🔴 **So `RECEIPT_SENDER_PATTERNS` CANNOT STAY A HARDCODED `const`** — a list that grows by approval is **data**. ✅ This *improves* the open-core split: real sender domains leave source code for the user's own DB.
  - ⚠️ **Recurrence counts APPROVED DRAFTS, not model classifications** — building the fast path from unconfirmed model output is the dishonesty `verified` exists to prevent. ⚠️ **N defaults to 3** (2 is coincidence for a shared domain); revisitable once it is data. ⛔ The promotion proposal routes to the **auto-import review surface**, never the assistant inbox.
- 🔴 **NEW REQUIREMENT — PURGE-WITH-REVIEW, nothing for it exists.** All-mail brings spam, so the model must flag junk for deletion ⛔ **gated behind the user confirming each one**. Deferred to Role C, recorded now because: ⛔ **deletion is irreversible so it can NEVER be granted autonomy**; ⛔ the queue lives on the **archive page**; ⚠️ **soft-delete (routines' `removed`) DOES NOT meet the goal** — the purpose is reclaiming storage, so bytes must go; 🔴 **but blobs are shared** (the test `identical bytes are one blob`), so purging needs proof no other document references that `sha256`; ⚠️ a purged blob whose `DocumentArchived` event survives means a rebuild recreates the row — it must read as **purged**, never broken. ⚠️ **Sequencing:** backfilling before purge exists puts spam in with no way out.
- ⛔ **IMAP STAYS OFF — follows from the deferral, not re-asked.** Enabling today would archive all mail while drafting still ran the **old hardcoded patterns** with **no purge path**. ⛔ **Never flip `OMNI_ENABLE_IMAP` without the user.** Sequence: two-tier drafting + purge → enable → seed cursor. **Blocked on Role C:** scheduled model pass, fast-path promotion, purge.

## Phase 5 — done, and what it cost
✅ **THE ARCHIVE IS VISIBLE TO THE ASSISTANT.** `"documents"` out of `NOT_QUERYABLE`; `DOCUMENT` registered behind `Feature::Documents`; 3 FULLTEXT indexes added. Full rationale: `docs/src/archive.md` § What the assistant can see. ✅ **All gates green by exit code: 1191 tests, clippy ×4 configs, fmt, frontend ×154.** ⚠️ **NOT verified:** no document retrieved by a **live assistant**, the sweep has met only **synthetic rows**, nothing on device.
⚠️ **`string::concat(NONE, …)` returns the literal string `"NONE"`** — no error, clean sweep report, the word indexed as document content. ⛔ **Documents is the first catalogued type whose text columns are `option<>`** (every earlier one is `TYPE string`), so anything reading `text_fields` generically must coalesce. Sibling, opposite symptom: `string::lowercase(NONE)` is a hard **ERROR**. ⚠️ The test asserts the **uncoalesced** expression so it cannot rot into a tautology.
🔴 **THE ARCHIVE IS THE FIRST CATALOGUED CORPUS THE USER DID NOT WRITE.** `read` on an archived `.eml` hands headers and body straight to the model holding the verbs — which is what `docs/src/assistant.md` describes the **planned dual-model split** as preventing, calling the separation a coincidence of how things were built. ⛔ **That coincidence has ended; the split is now load-bearing.** ✅ Two controls already carry it: nothing changes without a user-accepted proposal, and the read verbs reach no network.
🔴 **PHASE 5'S OTHER HALF IS NOT BUILT — "notify and route".** Blocked on decisions, not typing: **"needs review" is not expressible** (`verified` lives inside the `fields` array, not a column — hoisting a count has precedent), and **no deep-link mechanism exists anywhere**.
⚠️ **OPEN: `store::read` truncates NOTHING.** `search`/`list` cut to `SNIPPET_CHARS`; `read` returns the whole row — fine for human-written notes, but a 40-page scan goes over whole. ⛔ Not a bare cap: a silently truncated document is one the model answers from believing it saw everything.
⚠️ **Three catalogue declarations follow from "every archive column is optional":** `handle: "filename"` not `title`; `filename` is also a **text field**; `list_order: archived_at` not `document_date`. ⛔ `sha256` + `device_id` hidden. ⚠️ `documents` is the only **self-referential** entry — attachments are a child collection over the same table.

## Standing decisions — inherit, do not re-derive
- ⛔ **ON-DEVICE PASSES ARE BATCHED, DEFERRED ON PURPOSE.** ⛔ Never flag one as a blocker. Milestone: **finances tab + auto-import + email inbox + scanned receipts**.
- ⛔ **THE ARCHIVE IS THE SPINE.** ✅ It holds **every file worth looking at later** — ⛔ never argue "clutter" against archiving. ⛔ **ONE EMAIL = 1 + N DOCUMENTS**; inline `cid:` parts are not catalogued, which is lossless **because the raw `.eml` is archived**. ⚠️ 3 of 5 real fixtures have **no attachment** — the body *is* the receipt.
- 🔴 **The review step is the SOLE control between a crafted email and a fabricated ledger entry** (`receipts.rs`), with **no SPF/DKIM**. ⛔ **THE PARSER/MODEL SPLIT IS THE NETWORK, NOT DETERMINISM:** parsers at ingest, model **scheduled and batched**; ⚠️ `archive.rs`'s rule is *"no model in here"*, ⛔ not "no fields in here".
- ⛔ **`verified` means AN ORACLE CHECKED IT.** ⛔ Model fields are **always** `verified: false`. ⛔ **No confidence score on the document path.** ⛔ **A field is editable only BESIDE its document.**
- ⛔ **Text folds by ORIGIN before arrival order:** extracted (2) > transcribed/unknown (1) > none (0). ⚠️ The guard is on `on_archived` too — the pull filter runs on the author's clock.
- ⛔ **Viewer renders CLIENT-SIDE from the LRU cache** so documents stay viewable **offline**; ⛔ never a download link. ⚠️ **The 8-page PDF refusal is MINE, not the user's** — revisitable, and must never reach the viewer.
- 🔴 **BLOB BYTES DO NOT SYNC** — one durable copy, the box. ⛔ Server backup precedes the real backfill. ⚠️ **Verify a backup by SIZE, never exit code.**
- ⛔ **Role C STAYS DEFERRED.** ⚠️ Nothing emits `DocumentTextTranscribed`; nothing calls `derive_fields`.
- ⛔ **APPROVAL LIVES WHERE THE DATA LIVES.** ⛔ **THE JOURNAL IS NEVER PROPOSABLE.** ⛔ Beliefs **ask-only**. ⛔ **Irreversible actions can never be granted autonomy.**
- ⛔ **Never hardcode a catalogue-derived COUNT** — derive from `catalog::visible`. ⚠️ The type-**name** list in `verbs.rs` is spelled out on purpose.
- ⚠️ **The privacy guard is an INGRESS FILTER, not an audit**, and ⚠️ **one real institution name can never be a denylist pattern** (common English substring) — memory `feedback-public-repo-keep-private-identity-out` names it and carries the grep. ⛔ Run it by hand on any public commit touching auto-import or institution-shaped data; a `NEXT.md` line naming it was caught that way this session.
- ⛔ **Do not re-survey, ALL CLOSED:** retrieval is catalogue-driven · blob store + LRU · `extraction/media.rs` · `imap_real.rs` · KNN/analyzers · Phases D–G · **the corpus** (765 PDF / 276 CSV / 233 MB) · the archive page + all three viewer bugs.
- ⛔ **Box deploy DEFERRED.** 🔴 **Builds: `JOBS=1`, `DEV_DEBUG=0`** (debug info was 24G of a 31G target), ⛔ + `source scripts/fetch-onnxruntime.sh` ⚠️ **after a leading `cd <repo>`**. 🔴 **A FULL DISK PRESENTS AS A LINKER BUS ERROR** — `rust-lld` writes via `mmap`, so ENOSPC surfaces as `ld terminated with signal 7` with **no ENOSPC string anywhere**; ⛔ on any linker crash check `df -h` FIRST. Cost two runs 2026-09-12. ⚠️ **Never compare test counts across `-p` sets.** ⚠️ A filter matching zero tests still prints `ok`. ⚠️ **clippy never links.** ⚠️ Keep logs OUTSIDE the repo — SessionEnd pushes to a **PUBLIC** remote.
