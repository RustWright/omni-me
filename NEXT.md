# NEXT

**Next action: PHASE 4 — THE ARCHIVE PAGE, AND A VIEWER FOR EVERY TYPE IN THE CORPUS.** Extract `AttachmentViewer` out of `finances.rs:4513` into a shared component and keep `finances.rs` using it (one viewer, two callers), then widen it: today it renders images, iframes PDFs, and drops **everything else** to a download link — which catches all **276 CSVs**, cannot show a value being corrected, and is the other thing Android WebView refuses. Plan: `~/.claude/plans/lets-continue-sharded-minsky.md` § Phase 4. ⛔ **PLANNING IS DONE.**

✅ **PHASES 1–3 BUILT.** Archive spine + `DocumentTextTranscribed` carrier (2026-09-12) + fields: `core/src/document_fields.rs` (statement parser → checked fields, format **discovery**, `derive_fields` dispatcher) and `core/src/extraction/document.rs` (`DocumentReader`, `document_prompt`, `document_schema`). **1166 tests green across core + server + agent, clippy clean, fmt clean.**
⚠️ **NOTHING CALLS `derive_fields` OR EMITS `DocumentTextTranscribed` YET** — sequencing, not oversight. The **model** half of both waits on **Role C selection** (deferred, below); the **parser** half is now unblocked (see below). ⚠️ **No real corpus has been ingested yet, and the CSV fixtures are synthetic minimal shapes** — the signature rule has never met a real export.
⛔ **`"documents"` sits TEMPORARILY in `NOT_QUERYABLE`** (`catalog.rs`) — ⛔ **Phase 5's first step deletes it**; left in, the archive is invisible to the assistant and nothing else reports it. 🔴 **`omni-me-private` holds UNCOMMITTED work the session hooks do NOT reach** (`Cargo.lock`, `Dockerfile`, `examples/headless_import.rs`, `examples/probe_realdb.rs`).
✅ **DECIDED (user, 2026-09-12): the split is NETWORK, not determinism.** ⛔ **Parsing runs AT INGEST** — device background, server, wherever ingest happens; it is deterministic and needs nothing remote. ⛔ **Model extraction is SCHEDULED and BATCHED**, because it is the half that pays network latency. ⚠️ This narrows `archive.rs`'s *"archiving is not extracting"* to mean **no model at ingest** — parser fields there are the same category as `derive_text`, not an exception to it.
→ **Unblocked, small, not yet built:** derive parser fields inside `archive::ingest_one` so **one** place serves both ingest paths (`/documents/archive` and device capture) and neither can forget it; it returns two events instead of one, so the two call sites append both. ⚠️ If a large file ever makes this block, move the call off the ingest await — ⛔ never duplicate it per caller.

## Decisions in force — inherit, do not re-derive
- ⛔ **THE ARCHIVE IS THE SPINE** (user, 2026-09-11). Email ingestion and finance statements become **producers into it**; finance propose actions are a separate, smaller, LATER item.
- ⛔ **`verified` means AN ORACLE CHECKED IT, never "a parser made it"** — ⚠️ this **overrides the plan's blanket "parser ⇒ verified true"**, on `archive.md`'s and `Verifiability`'s own argument: `import_blockers` is empty both when every check passed and when the format offered none, so `verifiability()` separates them. A walked balance chain covers the row-derived fields (period bounds, row count, closing balance); ⛔ `kind` and `verifiability` are never verified — one is a classification, the other describes the check. ⛔ Model fields are **always** `verified: false`, and there is no parameter to change that.
- ⛔ **No confidence score anywhere on the document path.** `ExtractionResult` has one because `verify` has arithmetic; there is no sum to reconcile in *"this is a 2023 notice of assessment"*. ⚠️ The HAND WASH trap is why the flag is the only honest guard. ⛔ **Correcting a field requires the document VISIBLE BESIDE IT.**
- ⛔ **No `ExtractionHint::Document` — deviated from the plan deliberately.** That hint feeds `prompt_for` → `extract()` → the finance-shaped `ExtractionResult`, so it would be reachable from a method that cannot express its answer. Instead a separate **`DocumentReader`** trait; `OpenAiCompatExtractor` implements both and shares one `ask()`. ⛔ `NullExtractor` implements only the finance one — a null *summary* would file a document with an empty kind, which is worse than an uncatalogued one.
- ⛔ **Format DISCOVERY signatures live in `document_fields`, never in the parsers.** Import tells a parser its format, so parsers treat columns they can work without as optional — the transfer map then claims any date+amount CSV. ⛔ Tightening the parser would break import. ⚠️ Header signatures match first; the **headerless** chequing map is the fallback only, or column coincidence outvotes a declared header. Two matches ⇒ **no parser fields**, never a guess.
- ⛔ **Text folds by ORIGIN before arrival order**, like fields: `extracted` (2) > `transcribed`/unknown (1) > `none` (0), ties to the newer value. ⚠️ **The guard lives on `on_archived` too, and that is the point** — the pull filter runs on the author's clock, so a transcription can fold BEFORE the archive event whose absent text would erase it. ⛔ Ingest stays deterministic: **do not wire an extractor into `archive.rs`**.
- ⛔ **The viewer renders CLIENT-SIDE, every type, from the LRU cache** — `attachments.rs` exists so a document stays viewable **offline**. **PDF → pdf.js** via the esbuild step CodeMirror already uses (⚠️ BOTH copy targets) · **CSV → a table of RAW ROWS**, ⛔ never the parser's interpretation · ⛔ **never a download link**. ⛔ `rasterize_pdf` stays EXTRACTION-only. 🔴 **Three live viewer bugs in `tasks.md` § Document viewing** (Android has no PDF renderer · HEIC classified viewable but undecodable · CSV has no viewer at all).
- 🔴 **BLOB BYTES DO NOT SYNC** (memory `project-blobs-do-not-sync`) — documents live in **ONE durable place, the box**. ⛔ Server backup is a **dependency sequenced before the REAL backfill**. ⚠️ **Verify a backup by SIZE, never exit code.**
- ⚠️ **The 8-page PDF refusal is MINE, not the user's** (corrected 2026-09-11). **Revisitable.** It constrains **extraction's** base64 budget; ⛔ it must NEVER reach the viewer.
- ⛔ **Role C selection STAYS DEFERRED to the end of this block.** Archive work + backfill testing run on the **dev sync server** with throwaway data; the **REAL** backfill waits for model selection. ⛔ Never quote a run as a ranking.
- ⛔ **APPROVAL LIVES WHERE THE DATA LIVES** — archive review on the **archive page**, ⛔ never the assistant inbox. ⛔ **THE JOURNAL IS NEVER PROPOSABLE**. ⛔ Beliefs **ask-only**. ⛔ **Irreversible actions can never be granted autonomy**; only the user grants.
- ⛔ **The log carries the outcome, never the anchor** — projections fold in **`received_at`** order. ⛔ **Notes: the model never sends a body** (`note.revise` = `find`/`replace`; exactly one match or refuse). ⛔ **Fan-out stops at `build_events`.**
- ⛔ **Extraction: text first, rasterize only on empty** · ⛔ **`json_schema`, never `json_object`** · ⚠️ budget on **base64 length** · ⛔ in the **extractor, not the route**. ⛔ **Workspace is rustls-only** — naming `ring` or `ClientConfig::builder()` panics.
- ⛔ **Never hardcode a catalogue-derived count** — derive from `catalog::visible`. ⚠️ **v3 refuses `ORDER BY` over a field the selection omits.** ⛔ **Do not re-survey, ALL CLOSED:** retrieval is catalogue-driven · blob store + LRU mirror · `extraction/media.rs` · `auto_import/imap_real.rs` · KNN/analyzers/fastembed · Phases D–G · **the corpus** (765 PDF / 276 CSV / 233 MB; 124 PDFs no parser can touch) · **box capacity** (29G free — ⛔ NOT a constraint).
- ⛔ **Box deploy DEFERRED.** 🔴 **Builds: NOT 5G, NOT `--scope`** — detached **service**, `JOBS=1`, `DEV_DEBUG=0`, ⛔ + `source scripts/fetch-onnxruntime.sh` ⚠️ **AFTER a leading `cd <repo>`** (a `--user` unit does not start in your cwd — cost a run 2026-09-12; echo `ORT_LIB_LOCATION` as line 1 to catch it in 20s). ⚠️ **clippy never links**, so green clippy is not evidence tests build. ⚠️ Keep logs OUTSIDE the repo — SessionEnd pushes to a **PUBLIC** remote.
