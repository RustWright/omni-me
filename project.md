# Project: omni-me

**Status:** **Cycle 4 Session 5 (Implementation) — Phase 3 STEP 2c (app-OTP re-auth CLIENT / task 3.5a DONE) COMPLETE 2026-06-17.** The Dioxus client now drives the proven re-auth backend, finishing 3.5a end-to-end. **Inline-in-row UX:** the existing Auto-Import Sources settings row (`AutoImportRow`) grows an amber "Reconnect needed" callout + `Reconnect` button when a source is `needs_reauth` + `reauth_capable`; the button expands an inline 6-digit OTP field whose `Submit` calls a new `reauth_source` Tauri command → `POST /auto_import/reauth` (OTP in the JSON body, never the URL). The `ReauthOutcome` drives the row: `active`→success+collapse+parent re-pulls `/status`→healthy; `invalid_otp`→"code rejected" (field stays); `not_supported`/`error`→inline message. **The seam this closed:** the server already emitted `auth_state`/`reauth_capable`, but each proxy `AutoImportSourceView` (Tauri command layer + frontend types) deserialized lossily and silently dropped them — declared both (`#[serde(default)]`) at every hop so the signal survives to the screen. The Reconnect callout is **orthogonal to the `health` badge** (passive "is data flowing" vs imperative "user must act"): a degraded-but-active source shows no callout, only `needs_reauth` does. Clippy clean (Tauri + frontend, both feature configs). **Playwright mock walkthrough of all 5 states, 0 console errors** (WS shows the callout, others don't; Reconnect reveals the field; `000000`→rejected/field-stays; valid 6-digit→success→row healthy on refresh; PNGs in `logbook/_assets/source-reauth-reconnect/`). **Real-OTP happy path proven E2E vs the real WS account** (`cargo tauri dev` desktop → private `omni-me-private` server on real `credentials.toml`, localhost:3000): a live TOTP code flipped `auth_state → active`, then a manual `Fetch now` returned `last_outcome: success` / `health: healthy` — proving the **session refreshed**, not just the flag cleared (the two-clocks model: `registry.reauth` only flips `auth_state`, so the successful pull is independent evidence of the session clock). **This unblocks task 2.5** (WS auto-import may deploy to the VPS once the Phase 2 deploy pipeline lands — the SSH-for-OTP failure mode is gone). Public-only change set (frontend, src-tauri, docs, logbook PNGs); `omni-me-private` untouched this session. The `(logbook)` capture for 3.5a is deferred to a later drafting pass. ━━ **Prior: Phase 3 STEP 2b (app-OTP re-auth backend slice / 3.5a server half) COMPLETE 2026-06-15.** Turned the previously-buried `needs_reauth` signal into a tracked, exposed `AuthState`. Public engine: new `ImportError::NeedsReauth` + `AuthState`(`Active`/`NeedsReauth{reason}`) + `ReauthOutcome` public serde types; two **defaulted** `AutoImportSource` trait methods (`reauth_capable`→false, `reauth`→`NotSupported`) so non-reauth sources opt in for free; `SourceRegistry` now tracks `auth_state`+`reauth_capable` per source (`record_tick` flips to `NeedsReauth` on the new variant, a clean tick or successful reauth clears it, a transient blip leaves it untouched) + a registry `reauth(name, otp)` method; new `POST /auto_import/reauth` (OTP in body, never the URL) returning the `ReauthOutcome`; `GET /auto_import/status` now carries `auth_state`+`reauth_capable`. `SubprocessSource` threads the new variant up + speaks the frozen `reauth` verb to its helper. Private overlay: `ws-helper` `reauth` handler → `wealthsimple::reauth` (factored `spawn_driver(otp)` shared by pull+reauth; driver fresh-login maps exit 0→`reauth_ok`, 4→`invalid_otp`, else→`error`). Public +14 tests (62 auto-import) + clippy clean both feature configs + server clippy clean; private +4 tests (26) + clippy clean + both bins. **Live round-trip vs real config (GREEN):** `/status` exposes the fields (`reauth_capable` true only for the `wealthsimple-python` SubprocessSource, false for wise + 3 IMAP); a dummy-OTP reauth drove the **real WS python driver → real WS login → rejected → exit 4 → `invalid_otp`** (so the driver already implements the otp fresh-login path); unknown source→404; non-capable `wise`→`not_supported`; the scheduler WS tick surfaced `needs re-auth` and backed off while the other 4 sources ticked clean. Verification caught a stale binary first (`cargo test` rebuilt the lib but didn't relink the bin → explicit `cargo build`, re-verified — same class as the Phase-1 Android stale-frontend trap). Routes landed under `/auto_import/*` (not `/sources/*` as the design sketched — matches the live prefix). Remaining at that point = the client side (built next, in Step 2c above): Dioxus "Reconnect {source}" + OTP-field UI + Playwright + the real-OTP happy-path test. Contract docs + `SOURCE_REAUTH_DESIGN.md` updated. ━━ **Prior: STEP 2a (subprocess contract + WS pull-helper) COMPLETE 2026-06-15.** Froze the engine↔helper JSON contract in prose (`SUBPROCESS_SOURCE_CONTRACT.md`) + code (`HelperRequest`/`HelperResponse`/`HelperStatus` public serde types in `core::auto_import::subprocess`). Built the generic public `SubprocessSource` (the WS adapter's generic tail — wrap drafts→propose→project — generalized; 6 fake-helper tests). Converted WS to a standalone `ws-helper` overlay binary (`fetch_drafts` in a slimmed `wealthsimple.rs` + `src/bin/ws-helper.rs` + new `src/lib.rs` sharing adapters) that **reads its own credentials**, so the engine never sees a bank secret — the open-core boundary is now structural, not a convention. Helper discovery = sibling-of-current-exe + `OMNI_WS_HELPER` override (the convention all future subprocess plugins reuse). `driver_script` path hardened (engine validates the helper command; helper resolves the driver to absolute + existence-checks). Public 6 new tests + clippy clean (still bank-free); private builds both binaries + 22 tests + clippy clean. **Smoke-verified live (RUN_EXIT=124):** WS now ticks via `SubprocessSource → ws-helper → python driver`; real session-expiry → driver exit 5 → helper `needs_reauth` → engine graceful backoff (1→64s), Wise + 3 IMAP unaffected, clean shutdown. Reauth verbs (`HelperRequest::Reauth{otp}` + `needs_reauth`/`reauth_ok`/`invalid_otp`) frozen in the contract. **STEP 2b = next session** = app-OTP re-auth full stack (3.5a: engine `AuthState` + `/reauth` route + helper `reauth` handler + Dioxus "Reconnect" UI). Deferred: Wise helper; SC email-handler wrinkle (out-of-scope per the contract — stays in-process; future shape = helper decrypts PDF, engine extracts via LLM); real account-map (3.9). ━━ **Prior: Phase 3 (open-core split) STEP 1 COMPLETE 2026-06-14.** Bank code relocated out of the public engine into a NEW private overlay repo (`RustWright/omni-me-private`, `main`@`8b07e83`, registered as a `productive_learning` submodule). Public `omni-me` now builds + runs **bank-free / zero-source**: source inversion (`auto_import/setup.rs` `setup_from_credentials`→generic `spawn_sources`; new `run(RunConfig{source_builder})` seam in `server/src/lib.rs` owning all generic startup + `SourceCtx`/`SourceFuture`/`SourceBuilder`; public `main.rs`=zero-sources builder ~25 lines), bank submodules removed from `auto_import/mod.rs`, `credentials.rs` slimmed to `{imap,gemini}` (serde ignores the private bank sections in the shared `credentials.toml`), account roster→client settings-file (dropped `balances::LISTABLE_ACCOUNTS`; `account_summaries`/`dashboard_summary` take `roster:&[String]`; Tauri client loads a `roster` file from app_data, default-empty), and the 3 adapters + WS driver deleted. **446 tests + clippy clean, re-verified post-deletion.** Private overlay = composition-root `build_sources` (fills the seam — user's Learn-by-Doing WS branch: two-part creds+driver_path precondition, warn-on-half-config) + relocated Wise/WealthSimple/SC adapters (imports rewritten `crate::`→`omni_me_core::`) + `BankCredentials` + `data/roster` (canonical, non-secret) + driver + README; path-deps on public. **SurrealDB pinned 3.0.4** (fresh lockfile floated to 3.1.x → un-compilable `diskann 0.53.0`/rust#100013; pinned the whole surreal family to match public — omni-me uses zero vector-search so no feature loss; **both repos must bump in lockstep** — README note + `tasks.md` backlog). **23/23 adapter tests + clippy clean + GREEN smoke test vs real config:** Wise + 3 IMAP/SC tick (events=0); WS drove the real WS API, detected the expired Cycle-3 session, hit OTP-required (exit 5), backed off exponentially, stayed isolated from the 4 healthy sources (graceful degradation demonstrated live). Smoke test also caught a real migration side-effect — the user's `credentials.toml` `driver_script` still pointed at the moved public path (user fixed → private path). **Design banked:** app-entered-OTP source re-auth (`SOURCE_REAUTH_DESIGN.md`; generic `AuthState`+`/sources/status`+`reauth/{start,submit}` public, WS login protocol private, "Reconnect {source}" client UI; account uses TOTP → resolved to single-step `submit`, no trigger; tracked `tasks.md` 3.5a + the 2.5 deploy-blocker; [[project-source-reauth-app-otp]]). **Step 2 (subprocess-helper conversion + freeze app↔helper JSON contract + app-OTP re-auth + driver-path hardening) = the VERY NEXT session.** ━━ **Prior: Phase 1 COMPLETE 2026-06-08** (tasks 1.1–1.13; on-device 1.9 IME inset + 1.10 caret-above-keyboard verified on the real device). **Major build-pipeline root cause found + fixed:** the release APK was embedding a STALE DEBUG frontend — `tauri-build` bakes the `frontendDist` dir (=debug) into the native `.so` and the WebView serves from there, while `npm run build` only ever refreshes the *release* dir, so **no JS/frontend change reached the device all session** (only native Kotlin did). Not a cache — uninstall/clearBrowserCache/`?v=` were all no-ops. Fixed via new `scripts/android-build.sh [debug|release]` (`--config` override of `frontendDist`→release for the build only; `cargo tauri dev` untouched). Docs+memory corrected (`tauri-app/BUILD.md`; had it backwards). **Caveat:** prior-session on-device claims for 1.8b/nav-drawer ran against the debug-embedded frontend — likely valid but worth a re-confirm. **Next: Phase 2 (server go-live) ∥ Phase 3 (open-core split).** ━━ Earlier this phase, tasks **1.1–1.8a done**: root continuity store + journal/notes/captures/txn-list state migrations + auto-save resilience (retry/backoff + save-state indicator) + store-to-disk persistence. 1.6 resolved via lean path B (no save daemon; see [[project-autosave-robustness-b]]). Journal + capture continuity verified live via Playwright; frontend clippy + backend check clean. **Deferred to Session 6 (fresh context):** 1.8b position restoration (scroll/cursor/active-tab + real-disk verify), then 1.9–1.13 (Android IME inset + nav drawer + on-device verify). Detail in Session Log + `tasks.md` Phase 1. **Prior planning: Cycle 4 Session 4 (Planning) COMPLETE 2026-06-06.** Cycle 4 = polish → stable v1; completion bar is daily-use comfort. 7 design decisions settled (see `tasks.md` § Design decisions) and `tasks.md` reset into 6 phases (P1 daily-use foundation → {P2 server go-live ∥ P3 open-core split + extensibility, the one-way door} → P4 real-data go-live import 6.5 → P5 editor feel + properties → P6 release polish). Operating model: dogfooding is the test harness + a running friction log for expected scope creep. Plan-mode fact-check corrected two assumptions (auto-import source registry = `server/src/main.rs`, not `setup.rs`; CI already exists → Phase 2 adds deploy). **`tasks.md` is canonical for Cycle 4 tasks**; plan at `~/.claude/plans/vivid-cooking-kurzweil.md`. **Next:** Session 5, Phase 1.

**Prior (Cycle 3 Phase 7, 2026-06-04):** Cycle 3 **Phase 7 complete** (real-data go-live 6.5 deferred to Cycle 4). R2 ad-hoc transaction query: GUI builder + filter DSL. New pure, DB-free `core::query` (`ast`/`parser`/`eval`) filters the structured `transactions` projection; **24 unit tests** on synthetic fixtures. **Account matcher (user-settled):** case-insensitive `:`-segment-prefix subtree by default, `$`-anchored own-only to distinguish value booked directly on a parent from its sub-accounts — segment-anchored so `Food` never leaks into `Foodie`/`FoodStamps`; the matcher takes account strings at query time (no enumerated vocabulary), so the journal cleanup's renames are invisible to it. Engine kept pure → doubles as the planned WASM demo island. Command `run_transaction_query` (`commands/budget.rs`) + `query_candidate_transactions` (`db/queries.rs`, a subset of the proven `list_transactions` SurrealQL); frontend `QueryBuilderView` in `finances.rs` (new `FinancesView::Query` + Home tile) with predicate rows, Match ALL/ANY toggle, "Include sub-accounts" checkbox, live Generated-query mirror, results via the reused `TransactionListRow`; bridge `invoke_run_transaction_query` (+ mock substring approximation). **472 workspace tests + 24 new query tests pass; clippy clean across workspace --all-targets + frontend (mock + non-mock).** Playwright-verified end-to-end against mock (tile → builder → live DSL `account:Expenses:Food`→`…$`→`account:Expenses` → run → 4 results); screenshot in `logbook/_assets/ad-hoc-transaction-queries/`. **Not yet verified:** the command's real-DB path (SurrealQL execution + real-projection posting deserialization) — deferred to next app run (2026-06-04 decision; low risk, mirrors proven `list_transactions` + reuses `events::Posting`). **7.3 + 7.4 (2026-06-04):** base-currency picker shipped — persisted setting (mirrors timezone file pattern) read by dashboard/accounts/budget aggregation when the caller passes `None`; frontend stopped hardcoding `Some("CAD")`. 7.4 (cache-management surface) was **already shipped** with the 3.7/3.8 attachment-cache work and just needed checking off (Playwright-confirmed). Both real-DB-persistence paths share the 7.2 defer (mock-verified; mirror proven patterns). **Next:** Cycle 3 feature work is complete — remaining is **Cycle 4**: open-core public/private split (one-way door) → real-data go-live import (6.5) → polish/logo/stable-v1. No active Cycle-3 tasks left.
**Cycle 4 backlog:** cross-submodule state management (tab-switch during in-flight capture loses progress) + inset-bridge structural followups (chain `WindowInsetsListener` instead of replacing; `cargo:rerun-if-env-changed=TAURI_DEV_HOST` upstream contribution) + transaction list pagination state lost on nav-to-detail-and-back (each route re-mount resets `transactions` / `offset` signals; would need shared store keyed by list-instance) + Settings-managed declared-accounts list (lift `core::balances::LISTABLE_ACCOUNTS` const into the `accounts` SurrealDB table) + liquidity-aware `can_i_afford` (per-account `is_liquid` flag drives the verdict; ships alongside the declared-accounts UI since they share the same table) + **Phase-5 deferrals:** (a) 5.4 inline-edit per detected recurring pattern (vendor/amount/cadence tweak before confirm — workaround today: dismiss + rescan, acceptable friction since detector re-emits on next tick); (b) 5.7 balancing-posting affordance for hidden-fee resolution on merge (wire-transfer fees, FX spreads — workaround today: record a separate manual transaction for the fee against the same account, accepted because hidden fees are an edge case in the user's WS-heavy flow); (c) 5.5 credit-card CSV variant (CIBC credit-card export adds a card-number column — workaround today: chequing parser format works for both Asset/Liability accounts since the sign convention is uniform, only the source-account label differs); (d) reconciliation candidate engine FX-spanning matches (same-commodity-only today, no cross-currency pairing — workaround: manually merge cross-currency transactions via TransactionForm); (e) CIBC CSV format verification against real-world export sample (current parser tested against synthetic CSVs only — first real-use carries some risk of column-layout mismatch).
**Decision 2026-06-01 (Cycle 3 Session 5):** Real-data go-live import (task 6.5) **deferred to Cycle 4, after an open-core public/private split** — Cycle 3 closes on mock data only. Real-data import is a one-way door (ends the cheap-breaking-changes window per [[feedback-breaking-changes-ok]]), so the split's large refactor lands while only mock data is at risk; this also aligns with the existing end-of-Cycle-4 daily-use baseline. Security audit (this date): **no secrets / credentials / real data ever committed** to the public repo (gitignores held) — bank-name metadata is the only exposure, and it is *already* public via the mylearnbase logbook posts (prose names all four banks; the Accounts screenshot shows the real roster incl. "CIBC Aventura"). Consequence: scrubbing omni-me history is moot, and **re-publishing from a clean snapshot is off the table** — it would 404 ~20 commit-pinned source permalinks in 6 published posts. Fix forward, keep history. Split direction = **Option A (public core + private overlay)**; full scope + checklist in `tasks.md` Cycle 4 backlog.
**Last Updated:** 2026-06-17

## Session Log

| Session | Date | Status | Summary |
|---------|------|--------|---------|
| Session 5: Implementation — Phase 3 Step 2c (app-OTP re-auth client / 3.5a DONE) (Cycle 4) | 2026-06-17 | **3.5a complete — Dioxus Reconnect UI built + real-OTP-verified; unblocks 2.5** | **Built the client half of app-entered-OTP source re-auth, finishing task 3.5a end-to-end** (the server half shipped 2026-06-15 as Step 2b). **Inline-in-row UX (user-chosen over modal/banner):** the existing Auto-Import Sources settings row (`AutoImportRow`, `frontend/src/pages/settings.rs`) grows an amber "Reconnect needed" callout + `Reconnect` button when a source reports `auth_state.kind == "needs_reauth"` + `reauth_capable`; the button expands an inline digit-filtered 6-digit OTP field whose `Submit` calls a new `reauth_source` Tauri command (`commands/auto_import.rs`) → `POST /auto_import/reauth` (OTP in the JSON body, never the URL — keeps it out of access logs). `ReauthOutcome` drives the row: `active`→success+collapse+parent re-pulls `/status`→healthy; `invalid_otp`→"code rejected" (field stays, input clears); `not_supported`/`error`→inline message. **Core seam fixed:** the server already emitted `auth_state`/`reauth_capable`, but each proxy `AutoImportSourceView` (Tauri command layer + `frontend/src/types.rs`) deserialized lossily and **silently dropped** the unknown fields — declared both (`#[serde(default)]`, mirroring the loose `last_outcome` idiom) at every hop so the signal survives to the screen. The Reconnect callout is **orthogonal to the `health` badge** (passive "is data flowing — wait out a blip" vs imperative "the user must act"): a degraded-but-active source shows no callout, only `needs_reauth` does. **Verified:** clippy clean (Tauri + frontend, both feature configs); **Playwright mock walkthrough of all 5 states with 0 console errors** (WS shows callout, Wise/IMAP don't; Reconnect reveals field; `000000`→rejected/field-stays; valid 6-digit→success→row healthy on refresh; PNGs in `logbook/_assets/source-reauth-reconnect/`); **real-OTP happy path proven E2E vs the real WS account** (`cargo tauri dev` desktop → private `omni-me-private` server booted on real `credentials.toml`, localhost:3000) — a live TOTP code flipped `auth_state → active`, then a manual `Fetch now` came back `last_outcome: success` / `health: healthy`, proving the **session actually refreshed**, not just the flag cleared (the two-clocks model: `registry.reauth` only flips `auth_state`, so the successful pull is independent evidence of the ~weeks session clock vs the ~30 s TOTP clock). **frontendDist note (user-flagged):** the `tauri-build` `frontendDist` embedding + `scripts/android-build.sh` override is **Android-only** and unaffected by the public/private split (relative debug path, `tauri-app/` + `frontend/` both stayed public; only the SERVER boot must be the private binary) — the desktop `cargo tauri dev` flow used here rebuilds the debug dir fresh, so no stale-frontend trap. **This unblocks task 2.5** — WS auto-import may deploy to the VPS once the Phase 2 deploy pipeline lands; the SSH-for-OTP failure mode is gone. Public-only change set (frontend, src-tauri, docs, logbook PNGs); `omni-me-private` untouched. `(logbook)` capture for 3.5a deferred to a later drafting pass. Memory: [[project-source-reauth-app-otp]] (3.5a complete), [[project-extensibility-mechanism]] (closing line), [[project-android-build-pipeline]] (split clarification). |
| Session 5: Implementation — Phase 3 open-core split, Step 1 (Cycle 4) | 2026-06-14 | **Step 1 complete — bank code relocated; private repo created + pushed + smoke-tested** | **Open-core split Step 1: all bank-specific code moved out of the public engine into a new private overlay repo, public engine now runs bank-free.** Most of the cut was pre-wired (object-safe `AutoImportSource` trait + generic `SourceRegistry` + caller-injected IMAP); the one un-inverted point was `setup_from_credentials`, replaced by generic `spawn_sources` + a single `run(RunConfig{source_builder})` front-door seam in `server/src/lib.rs` (`SourceCtx`/`SourceFuture`/`SourceBuilder` types; `run` owns db/gemini/blob/extractor/registry/AppState/interval/serve; public `main.rs` passes a zero-sources builder). `credentials.rs`→`{imap,gemini}` only (serde ignores the private `[wise]`/`[wealthsimple_python]`/`[[sc_accounts]]` sections in the shared `credentials.toml`). **Roster→client settings-file:** dropped `balances::LISTABLE_ACCOUNTS`; `account_summaries`/`dashboard_summary` take `roster:&[String]`; Tauri client loads a `roster` file from app_data (default-empty→empty Accounts screen). 3 adapters + WS driver `git rm`'d from public after the private copies verified. **Public: 446 tests + clippy clean, re-checked post-deletion.** **Private overlay (`RustWright/omni-me-private`, `main`@`8b07e83`, submodule of `productive_learning`):** composition-root `main.rs::build_sources` (user's Learn-by-Doing WS branch — two-part creds+driver_path precondition, `warn` on half-config) + relocated `wise`/`wealthsimple`/`sc_ngn` (imports `crate::`→`omni_me_core::`) + `BankCredentials` + `data/roster` + driver + README; path-deps on public crates. **SurrealDB pinned 3.0.4** — the fresh private lockfile floated to 3.1.x which drags in `diskann 0.53.0` (fails to compile on the toolchain, rust#100013); pinned the whole surreal family to match public (omni-me uses only B-tree indexes, zero vector-search, so no loss); separate repo = separate lockfile, so **both repos must bump in lockstep or the overlay re-floats** (README + backlog note). `tempfile`→runtime dep; `wiremock` dev-dep; 2 dead-code warnings resolved (kept `WsImportEnvelope.accounts` with `allow`+doc, removed orphaned `unmatched_account_name`). **Private: clippy clean + 23/23 adapter tests + GREEN smoke test against real config** — Wise + 3 IMAP/SC ticked (events=0); WS drove the real WS API, found the ~4-week-old session expired (`Not Authorized` from WS's own GraphQL), fell back to login, hit OTP-required (driver exit 5), backed off exponentially, and stayed **isolated** from the four healthy sources (graceful degradation demonstrated live, not asserted). The first smoke run also caught a genuine migration side-effect unit tests can't: `credentials.toml` `driver_script` still pointed at the moved public path (user repointed → private). Scratch DB ran from the private dir so the dogfooding DB was untouched. **Design banked this session:** `SOURCE_REAUTH_DESIGN.md` — interactive source re-auth so the WS OTP is entered **in the app, never via SSH-to-VPS**; the capability is itself open-core (generic `AuthState`+`/sources/status`+`reauth/{start,submit}`+"Reconnect {source}" UI public; WS login protocol private). User confirmed WS uses **TOTP** → the trigger step vanishes, flow collapses to a single `reauth/submit`. Tracked as `tasks.md` **3.5a** + the **2.5** hard deploy-blocker (no WS auto-import on the VPS until it ships). **Memory:** [[project-source-reauth-app-otp]]. **Curiosity:** WS OTP/session "two-clock" model + why it felt fragile (`.curiosities/cycle-4.md`). **Process note (user, this session):** project.md is getting unwieldy — plan to archive+reset session-log/status history to `.archive/` once stable-v1 ships (filed Phase 6). **Step 2 (subprocess-helper conversion + freeze the app↔helper JSON contract + build the app-OTP re-auth + harden the driver path) = the very next session.** Public Step-1 changes committed at session end; private pushed; parent submodule pointer committed local, pushed at parent-sync. |
| Session 5: Implementation (Cycle 4 Phase 1) | 2026-06-06 | Paused — Phase 1 (1.1–1.8a done) | **State-continuity foundation.** New root-held `continuity` store (`use_context_provider`, joins tz/pending-share contexts): one `ContinuityKey` enum (user's learning-mode contribution, 1.1) keys three parallel maps — `sessions` (`EditSession`: journal+notes editors 1.2/1.3, incl. note title + draft→saved `NewNote→Note(id)` promotion), `captures` (`CaptureDraft`: in-flight finances form 1.4 — **full-form-draft** per [[project-capture-continuity-full-draft]] + Home "resume capture" affordance), `lists` (`ListState`: txn-list pagination 1.5 — restores rows/offset/filter across list→detail→back). Each page hydrates-on-mount (store preferred over backend; fresh-extraction/dashboard-filter take precedence over a stale draft) + a write-through mirror. **1.6 resolved via lean path B** (no root save daemon — store re-fire on remount covers nav-away-and-back; kill-gap folded into 1.8a; avoids id-writeback coordination) — see [[project-autosave-robustness-b]]. **1.7 auto-save resilience:** shared `autosave.rs` (`SaveState` + generic `save_with_retry` + `SaveIndicator`); backoff policy (user's contribution) = exp 500/1000/2000/4000ms ×4 then fail; wired into both editors' auto + manual save. **1.8a store persistence:** serde on store types + `PersistedWorkspace` (maps as `Vec<(k,v)>` — serde_json rejects enum keys); backend `get_workspace`/`save_workspace` (JSON in `app_data_dir`, mirrors `commands/settings.rs`) + bridge/mock; writer-gated boot-load + debounced write-back. **Verified live (dx serve mock + Playwright):** journal type→tab-switch→return = text intact; finances capture fill→leave→resume-banner→form fully restored→Cancel clears. Frontend clippy clean + backend `omni-me-app` check clean throughout. **Deferred:** 1.8b position restoration (scroll/cursor/active-tab/open-note + auto-show on initially-open page + real-disk verify of 1.8a). Operating mode: checkpoint on store-shape (1.1) + save-state (1.7) decisions, autonomous elsewhere. **Next (Session 6, fresh context):** 1.8b, then 1.9–1.13 (Android IME inset + nav drawer + on-device verify). |
| Session 4: Planning (Cycle 4) | 2026-06-06 | Complete | Cycle-4 kickoff run as a Planning session with an embedded design pass on the genuinely-fuzzy unknowns; carried backlog re-examined as raw material. **7 decisions settled** (detail in `tasks.md` § Design decisions): extensibility = one mechanism, two shapes (subprocess plugins for data sources, generalizing the WS user-provided-driver pattern + config-selection for provider-swaps incl. bring-your-own-LLM via one OpenAI-compatible `LlmClient`); committed stable VPS; keep the custom CodeMirror editor; **full state-continuity rework** (root-held store survives navigation + workspace-position persistence survives Android app-kill; content safety via hardened auto-save, not hot-exit); inline Obsidian-style properties panel on top; keyboard-occlusion fix by extending the Android IME inset bridge (`InsetBridge.kt`, chain not replace); navigation = drawer opened by **both** edge-swipe (`setSystemGestureExclusionRects`, verified viable) and a button. **Operating model: dogfooding is the test harness** — Phase 1 makes the app livable, then daily use feeds a running friction log (expected scope creep gets a home). Ran in **plan mode** to fact-check before writing: corrected two assumptions (source registry = `server/src/main.rs`, not `core/.../setup.rs`; CI already exists → Phase 2 adds deploy, not CI-from-scratch). **`tasks.md` reset** into 6 phases (P1 → {P2 ∥ P3} → P4 → P5 → P6) + reconciled carried backlog + friction-log section. Two feedback memories saved: [[feedback-explain-jargon-and-acronyms]], [[feedback-verify-claims-before-asserting]]. Plan: `~/.claude/plans/vivid-cooking-kurzweil.md`. No code this session. **Next:** Session 5, Phase 1. |
| Session 5: Implementation — Phase 6.1/6.2/6.3/6.4/6.6 (Cycle 3) | 2026-05-26 | **Phase 6 import infrastructure complete (uncommitted, 6.5 pending external cleanup)** | **All Phase-6 tasks that don't depend on the user's external cleanup pass shipped in one session while user worked on the cleanup in parallel.** Auto-mode handover after Phase 5 logbook drafts were published. **6.1 (`core/src/journal_import.rs`):** parser + walker (resolves `include` globs per POC 0.1b shape) + in-house elision filler (single-elided posting expands into one balancing posting per commodity from the explicit sums; multi-elided is a deferred error → `balance_failures`) + `@`/`@@` price preservation (Total-price reduced to per-unit by dividing by abs(quantity)) + tag extraction from posting comments (filters `txn_id`/`attachment` so journal-file-projection-written metadata doesn't round-trip as user tags) + `apply_plan` (rename + drop) + dependency-free FNV-1a 64-bit content hash → 16-char hex. **6.6 (same module):** `apply_a2_rewriter` walks postings, calls existing `accounts::strip_business_prefix`, appends `Tag::KeyValue { key: "type", value: "business" }`, idempotent (re-run is a no-op), recomputes content_hash + txn_id after mutation so dedup at commit time reflects the rewritten state. **6.4 (`core/tests/journal_import_paisa.rs`):** two `#[ignore]`-gated tests against `.reference/paisa/main.ledger` — `parses_paisa_journal_at_scale` (files_parsed≥50, transactions≥5k, all txn_ids unique, all postings non-empty) + `paisa_a2_rewriter_runs_clean` (no `Expenses:Business:` leftover post-rewrite, ≥1 type:business tag landed). Live run: **117 files / 5,826 transactions / 92 accounts / 11 commodities / 0 balance failures / 1 parse error** (Paisa's `Monthly in 2025/10/01` non-hledger DSL in `budget.ledger`). A2 touched 17 postings across 3 Business accounts. **Bug surfaced during first paisa run:** content_hash collided for legitimately-identical transactions (two $5 coffees on the same day at the same place) — fixed by extending txn_id to `import-{hash}-{occurrence}` where occurrence is the 1-based index of that hash within the same parse; stable across re-runs as long as journal ordering is preserved. **6.2 + 6.3 backend (`tauri-app/src-tauri/src/commands/journal_import.rs`):** `preview_journal_import` canonicalizes path → stashes on new `AppState::last_journal_import_path` → spawn_blocking parse → returns `JournalImportPreview { files_parsed, transactions_count, per_account[], commodities, sample_transactions[≤50], parse_errors, balance_failures, already_imported_count }`. `commit_journal_import` refuses any path that doesn't match the previewed canonical → re-parses → applies A2 (default-on, opt-out via plan) → applies user's drop/rename plan → batched lookup against `transactions` projection for already-existing txn_ids (chunks of 500 to stay within SurrealDB query bounds) → emits `TransactionRecorded` event per surviving draft. Returns `{ committed_count, skipped_existing_count, dropped_count, a2_rewrites, balance_failures, parse_errors }`. **6.2 frontend (in `pages/finances.rs`):** `JournalImportView` three-state UI (Idle = path input + Preview button; Previewed = stats grid + parse-error/balance-failure callouts + per-account row table with checkbox + rename input + sample-transactions list; Done = success summary). New `AccountRow` + `SampleTxnRow` + `StatCell` components. Mock-feature bridge invokes return synthetic preview/result so `dx serve --features mock` walks the screen end-to-end without a real journal on disk. **One clippy fixup:** the `const_is_empty` lint rejected a const-eval compile-time check on `BUSINESS_HIERARCHY_PREFIX.is_empty()` — replaced by removing the no-longer-needed marker functions (the prefix is verified by the existing `accounts::tests` regression). **Verification:** 448 core lib tests + 38 tauri-app lib tests + 18 new journal_import tests + 2 live paisa integration tests pass; workspace `cargo clippy --all-targets -D warnings` clean; frontend wasm clippy clean for both `mock` and non-mock features. **Unverified:** no `dx serve` / Playwright visual walkthrough yet (user offline; will run at next session); live end-to-end against real journal (parse → commit → projection round-trip) hasn't been exercised — only the parse half is live-validated. The end-to-end belongs to 6.5 anyway. **Cycle 4 candidates flagged inline (not yet hoisted to `tasks.md`):** glob-style account-pattern drops (today: exact-match drop only — user has to uncheck each `Assets:Business:*` row individually); large-journal preview latency (5826 txns via `take(SAMPLE_LIMIT=50)` doesn't show the full chart of accounts in the sample list — fine for spot-check, but a paginated full-list view would help for confidence on Phase 6.5); rerun-safe-after-edit dedup (today: editing the journal mid-import session shifts content_hashes, breaking the occurrence-index stability — workaround: re-preview before commit). **No new memories saved this session:** the work is execution of planned tasks. The content-hash-collision bug is captured in code via the `duplicate_transactions_get_distinct_occurrence_ids` test, not in memory (project-shaped, not feedback-shaped). **No commits pushed** per [[feedback-no-push-during-cycle]]; staging left for user review at session end. **Next:** user finishes cleanup → 6.5 (post-cleanup end-to-end against cleaned journal) → Phase 7. |
| Session 5: Implementation — Phase 5 logbook capture for 3 bundles (Cycle 3) | 2026-05-26 | Complete (3 posts published as drafts in mylearnbase, no commits there) | **All three Phase 5 logbook bundles drafted + published in a single fresh-context session per the deferred-logbook experiment.** Section-by-section drafting per [[feedback-logbook-section-by-section]] across all three; all published with `--skip-verify` per [[feedback-logbook-publish-postfixes]] (parallel-test ordering nondeterminism on frontend exec blocks). **Bundle 1 (`budget-setup-progress`, tags `dioxus, budgeting, ux`):** §3 names per-category targets + colored progress bars + calendar-anchored vs rolling cycles; §4 builds descriptive-vs-prescriptive axis framing ("am I on track for what I planned?") leveraging the dashboard's reusable aggregation pipeline; §5 names two permanent non-goals (custom:N picker exposure + envelope/carry-forward budgeting); §6 has 19-test core exec (4 buckets: parsing 7 / window resolution 3 / aggregator 5 / posting flatten 4) + 5-test frontend exec (round-trip 2 + 3-band color 3) + cites of `period_to_days` / `compute_budget_progress` / `progress_color_class` + 2 screenshots. **`budgeting` tag introduced** as first-instance — plausibly recurring (bundle 2 used it too). §4 went through 4 revisions before landing — user noted dense sentence-structure on v1; one-beat-per-sentence reshape + tail-smoothing yielded v4. **Bundle 2 (`recurring-detection-confirmation`, tags `dioxus, event-sourcing, budgeting`):** §3 names Confirm/Dismiss workflow + Scan-now + nightly background pass + idempotency-via-pattern-id; §4 leads with planning-needs-recurring-obligations (dashboard widget + Can-I-Afford verdict) + frames the false-positive catch that motivates the Confirm step; §5 names variable-amount patterns + income-side recurrences as permanent non-goals; §6 has 11-test core exec (3 buckets: classification 5 / scanner 5 / determinism 1) + cites of `classify_cadence` / `detect_patterns` / `recurring_scanner::spawn` + 1 screenshot; §7 carries 3 bullets including a concepts-demo candidate for the cadence classifier policy. User pushed back on L-b-D reference in original §7 (no reader context for "Learn-by-Doing") + concision feedback applied across bullets. **Bundle 3 (`reconciling-multi-source-transactions`, tags `dioxus, ledger, event-sourcing`):** title shifted from "Unified reconciliation..." options to "Reconciling multi-source transactions" per user choice; §3 reshaped from inline-bold-list-in-paragraph to a bulleted three-screen tour (Import statement / Reconcile / Balance check) per user direction; CIBC specificity dropped from the description-text in §3, moved to §5 scope context. §4 builds 5-beat causal chain: multi-source data → dup risk (concrete Loblaws example) → cost of not reconciling → `Unmatched`-clearing-account mechanism → what reconciliation does (drains `Unmatched`). §5 names two permanent non-goals (auto-merge without approval + three-way merges); CSV-variants / FX-pairs / hidden-fee-affordance all deferred to §7 per [[feedback-logbook-scope-vs-deferred]]. §6 has 22-test core exec (parser 7 / matcher 12 / balance-check 3) + 2-test frontend confidence-pill exec + cites of `parse_cibc_chequing` / `find_match_candidates` / `balance_check` + 3 screenshots (statement-import form / Reconcile pairs+no-match / Balance-check discrepancy verdict). §7 carries 4 bullets: concepts-demo candidate + 3 deferrals (cross-commodity matching / hidden-fee balancing posting / CSV format variants). User explicitly asked the demo-deferral rationale — answered: the demo is a separate post-form in a different repo (concepts in mylearnbase) with its own interactive build-out, today's logbook flags candidacy rather than bundling two posts in one session. **`reconciliation` tag held off** — first-instance with limited plausible follow-up clusters; bundle 3 published with 3 existing-inventory tags. **Mid-session stale-screenshot fix:** the csv-import PNG captured during Phase 5 close-out still showed "Phase 5.7" project jargon despite the inline UI-text fix landing in code (post-PNG-capture, lines crossed). Re-took via `dx serve --features mock` + Playwright MCP; the fresh PNG shows the corrected "Use the Reconcile screen" text — a data point in favor of running visual sweeps *after* close-out text fixes, not as part of them. **Memory updated:** [[feedback-logbook-section-by-section]] — §6 evidence-plan-first discipline elevated from "can be more autonomous" to "required pre-write checkpoint" after user prompted "what's your plan?" twice mid-session (bundles 2 and 3). No new memories saved otherwise; this session was execution of an established process. **Next conversation:** user reviews 3 drafts in mylearnbase + flips `draft=false` when ready; then Phase 6 (existing hledger journal import + A2 business-hierarchy → tag migration). |
| Session 5: Implementation — Phase 5 audit-driven close-out (Cycle 3) | 2026-05-24 | **Close-out complete** | **Post-implementation audit caught two spec-vs-shipped gaps + closed them, plus visual sweep with preserved screenshots + boot-smoke equivalent + Cycle 4 backlog enumeration.** **Gap 1 (5.3 nightly scheduler):** spec said "nightly background pass over event log" but only manual trigger had shipped; closed by adding `tauri-app/src-tauri/src/recurring_scanner.rs` (60s warm-up post-boot + 24h cadence, modeled on `auto_close_scheduler` shape; calls into `recurring::detect_patterns` + checks `recurring_patterns` table for idempotency, emits new `RecurringTransactionDetected` events for unseen ids only — preserves user confirmations across re-scans). Wired into `lib.rs::setup` after `auto_close_scheduler::spawn`. **Gap 2 (5.7 no-match path):** spec called for "statement-sourced events without a candidate accept a manual category fill-in (emits `TransactionUpdated` replacing `Unmatched`) + `TransactionCleared`" but only the pair-merge flow had shipped — statement rows with no pairing candidate had no UI resolution path, breaking the workflow. Closed by: (a) extending `core::events::budget_projection::on_transaction_updated` to accept a `postings` change set (was previously description + date only); (b) new `list_unmatched_without_candidates` Tauri command (filters `list_unmatched_transactions` by id-not-in-candidates); (c) new `resolve_unmatched` Tauri command that rewrites the Unmatched leg into a user-supplied category leg + auto-emits `TransactionCleared` when `statement_source` is present; (d) `NoMatchRowCard` component below the matched-pairs section with category text input + Resolve button. **Visual sweep:** `dx serve --features mock` walked through all 5 new screens (BudgetListView with 3-tier progress bars green/amber/red, RecurringReviewView with mock Netflix+Coffee patterns, StatementImportView with file picker, ReconciliationReviewView with matched pairs + no-match section, BalanceCheckFormView with discrepancy verdict) + dashboard Unmatched click-through verified to land on Reconciliation (Phase 4 placeholder dead). 8 PNGs saved to `logbook/_assets/{budget-setup-progress,recurring-review,csv-import,reconciliation-merge,balance-check}/` for the deferred-logbook session per [[feedback-preserve-visual-verification-screenshots]]. **One inline UI-text fix:** StatementImportView's explainer was leaking "Phase 5.7" project-jargon into user-facing text; rewrote to "the Reconcile screen". **Boot-smoke equivalent:** `cargo build -p omni-me-app` (full binary link) verifies IPC handler registration — the `tauri::generate_handler!` macro resolves all command names at compile time, so a clean link implies every command is reachable. Headless env can't run `cargo tauri dev` directly so this stands in for the runtime smoke. **Cycle 4 backlog enumerated** on project.md status header with explicit workarounds for each deferral: (a) 5.4 inline-edit per recurring pattern (workaround: dismiss + rescan); (b) 5.7 balancing-posting affordance for hidden-fee resolution on merge (workaround: separate manual fee transaction); (c) 5.5 credit-card CSV variant (workaround: chequing parser format works for both Asset/Liability accounts); (d) reconciliation FX-spanning matches (workaround: manual TransactionForm merge); (e) CIBC CSV format verification against real-world sample (current parser tested against synthetic CSVs only). **Audit pattern itself worth flagging:** the dx-serve walkthrough surfaced zero latent UI bugs this time (vs Phase 4's 2-of-2 bug-finder rate), which is a single data point in favor of the deferred-logbook experiment for compute-heavy/data-light Phase 5 screens — but Phase 4's bugs were both in data-rich rendering (FX datetime + flex-bar layout + form prevent_default), so the absence of bugs here is consistent with a "data complexity drives latent UI bug rate" hypothesis rather than vindication of the experiment. **Memory captured:** none new — the audit + close-out is execution of a known process (the gap-audit pattern itself isn't novel; what's new is that we ran it before close instead of after). **Next conversation:** logbook session with screenshots already captured. |
| Session 5: Implementation — Phase 5 (Cycle 3) | 2026-05-24 | **Phase 5 complete (8 commits, implementation-only mode)** | **All 8 Phase 5 sub-tasks shipped end-to-end in one push per [[project-phase5-deferred-logbook-experiment]] — no inline logbook captures, three bundles deferred to a dedicated fresh-context session after push.** **5.1 (`0aabfea`):** W4 budget setup screen — frontend `BudgetListView` with add/edit/remove flow over existing `BudgetSet/Updated/Removed` events; new `remove_budget` Tauri command; new `core::budget::period_to_days` pure helper (Learn-by-Doing on the parse policy + `interval > 0` guard); period picker exposes weekly/biweekly/monthly only (custom:N stays in schema, hand-crafted budgets round-trip via `period_label` fallback); 5 new frontend tests + 7 new core tests. **5.2 (`9d7d309`):** actual-vs-planned progress bars inline on each BudgetRowCard — new `core::budget::{BudgetProgress, current_period_window, compute_budget_progress, collect_expense_postings, budget_progress_summary}`; calendar-anchored windows for weekly/monthly (Sun-Sat / 1st-to-last per Learn-by-Doing on the policy split), rolling-N-days for biweekly + custom:N; backend orchestrator does journal parse → Prices construction → posting flatten → progress compute; frontend bars (`progress_color_class` green<75/amber<100/red>100) clamp at 100% visually while verdict text surfaces over-budget %; `last_day_of_month` lifted from `routines.rs` private to `pub(crate)`. **5.3 (`ed6776d`):** W3 recurring detection scanner — new `core::recurring::{DetectedPattern, classify_cadence, detect_patterns}`; **two real classifier bugs surfaced during Learn-by-Doing** (median-index-off-by-one on odd lengths + "mean OR median in band" matching too permissive — both fixed via simplified shape: median + per-bucket tolerance + all-gaps-must-agree consistency check; `weekly_with_one_outlier`, `stale_pattern_with_year_long_gap`, `median_correctness_on_odd_length` regression tests added); deterministic `pattern_id` derivation via natural-key hash + `scan_recurring` Tauri command that skips already-tracked ids to preserve user confirmations; new `dismiss_recurring` Tauri command emitting `RecurringTransactionDismissed`. **5.4 (`3bcf0b0`):** W3 confirm/dismiss UI — `RecurringReviewView` with "Scan now" button + per-row Confirm/Dismiss; `list_recurring` Tauri command repurposed to return parsed `RecurringPatternView` shape (replaces raw `RecurringPatternRow` over the wire); new `RecurringPattern` + `ScanRecurringResult` frontend mirrors. **5.5 (`322ac94`):** CIBC chequing CSV parser — new `core::statement_csv::{parse_cibc_chequing, ParsedStatementRow, MoneyDirection, CsvParseError}`; format-agnostic `ParsedStatementRow` output shape leaves Asset/Liability sign translation to the caller; permissive parser skips header row + blank-blank rows + zero-amount rows, accepts MM/DD/YYYY fallback; `import_cibc_chequing_csv` Tauri command builds source-posting + `make_unmatched_mirror` per row and emits `TransactionRecorded` with `statement_source` set; **`statement_source: Option<String>` added to `TransactionRecordedPayload`** (additive optional field per [[feedback-breaking-changes-ok]]) — all 4 existing payload construction sites patched (`commands::budget::record_transaction`, `commands::auto_import::commit_batch`, `core::ledger` test, 2× `core::journal_file` tests); `StatementImportView` with file picker + form for source-account/statement-label/commodity. **5.6 (`b417ed4`):** Unified matching engine — new `core::reconciliation::{find_match_candidates, UnmatchedTxn, MatchCandidate, MatchSignals}`; gates on amount-cancels-out (sum-to-zero check) + same-commodity + within-days-window; scores via day-decay × 0.5 floor at max_gap + 0.2 description-similarity bonus (token Jaccard over lowercased descriptions); stable lex-ordered `primary_id` so each pair appears once; `clears_statement` flag true when exactly one side has `statement_source`; new `list_unmatched_transactions` query helper + `list_match_candidates` Tauri command with inline `ReconciliationTxnPreview` per side so UI doesn't refetch per row. **5.7 (`2e41bd6`):** Unified reconciliation review UI — `ReconciliationReviewView` with two-column candidate cards (confidence pill: High≥0.85 / Medium≥0.6 / Low + matching emerald/amber/muted color); `merge_transactions` Tauri command emits `TransactionsMerged` (strip-Unmatched-legs + concat + pick description from primary + pick attachment from whichever side has one) + auto-emits `TransactionCleared` when one side has `statement_source` (cleared_date taken from the statement side); local-only Skip dismisses pairs until reload (no event emitted); dashboard Unmatched widget click-through repointed from filtered TransactionList to Reconciliation review. **5.8 (`16df901`):** Balance check — new `core::budget::{balance_check, sum_cleared_postings, BalanceCheckResult}` + `list_cleared_transactions` query helper + `check_account_balance` Tauri command + `BalanceCheckFormView` (account / commodity / statement-balance / as-of date form → verdict card with balanced-green / discrepancy-amber styling). **Process experiment notes:** **No mid-task verification PNGs**, **no logbook drafting**, no UI verification beyond clippy + tests — all visual verification deferred to the logbook session per the experiment design. **Two Learn-by-Doing recoveries this session** (`period_to_days` first attempt had the "custom:0" exact-arm-vs-parsed-value leak; `classify_cadence` first attempt had the median-index bug + over-permissive matching) — user took my feedback inline + we recovered both before merging. **Mid-session direction change captured to `[[project-phase5-deferred-logbook-experiment]]` memory** (no more L-b-Ds for the rest of this session; use AskUserQuestion for important decisions only — no important decisions actually came up after the switch). **No memory captured for the implementation work itself** — Phase 5 was straight execution of planned tasks, no surprise architectural shifts to record. **Next conversation:** dedicated fresh-context logbook session covering all 3 bundles in sequence (5.1+5.2 budget, 5.3+5.4 recurring, 5.5-5.8 reconciliation including the demo candidate). |
| Session 5: Implementation — Phase 4 logbook capture for 4.4 + 4.5+4.6 (Cycle 3) | 2026-05-24 | Complete (both posts published as drafts in mylearnbase; cite anchors pushed) | **Logbook entries "Account list with multi-currency balance aggregation" and "Financial-health dashboard" assembled + published.** Both drafted section-by-section per [[feedback-logbook-section-by-section]]; cumulative effect of the per-section discipline = **4 new editorial-direction memories saved this session** (vs. the prior session's intentional defer of 4 single-data-point observations — the corpus is now growing fast enough that lessons promote on the second occurrence rather than waiting for a third). **4.4 (`account-list-multi-currency`):** mock data extended with `Assets:StandardChartered:NGN` row (`value_in_base: None`, `total_in_base: None`) so the screen's `—` badge for unconvertible commodities is visible in the demo shot without modifying the real `LISTABLE_ACCOUNTS` allowlist (committed `cee3c27` as mock-only update; the allowlist edit becomes part of the Settings-managed `accounts` table queued for Cycle 4 per existing backlog item). Tags `dioxus, ledger, multi-currency, fx` — all reused from inventory, no new tags introduced (audit ran after user surfaced gap that I'd skipped the `tagging.md` discipline). Published via `logbook publish --skip-verify` (cargo test parallel-thread ordering + `--strip-cargo-noise` not propagating to verify rerun; documented as tooling feedback at `mylearnbase/editorial/_feedback/logbook.md` 2026-05-24 entry). Cite anchors at `core/src/balances.rs:93` (`account_summaries` entry) + `:180` (test module top); §6 evidence = 8-test exec block + 3-bucket prose (convertibility math / account surfacing / declared-account metadata splicing) + cites + one Playwright shot showing the multi-commodity Wise row aggregating CAD+EUR+USD into a single CAD total alongside the SC NGN `—` badge. §7 carries the deferral to the Settings-managed allowlist + a concepts-demo candidate for "how P directives compose into a Prices lookup" (curiosity also captured to `omni-me/.curiosities/cycle-3.md` per home CLAUDE.md curiosity protocol — user said "I actually don't understand how P directives work in Ledger"). **4.5+4.6 (`financial-health-dashboard`):** **Two latent product bugs surfaced + fixed during visual verification** (committed `a68b2ee`). Bug 1: trend chart bars rendered at 0px because outer container's `items-end` (chosen to align bars to bottom of chart area) overrode default `stretch`, leaving the inner `flex-1` bar container with no resolved height for `% heights` to resolve against; fix = `items-end` → `items-stretch` on outer + `min-h-0` on column + bar container so flex children can shrink without overflowing. Bug 2: clicking "Check" on Can-I-Afford navigated back to Journal because the form's `onsubmit` handler took `_` instead of an event and never called `prevent_default()`, so the browser's default form submission fired a POST to the SPA URL which Dioxus's router interpreted as a navigation home; fix = `move \|_\| ...` → `move \|e: FormEvent\| { e.prevent_default(); ... }`, matching the project's existing `prevent_default` precedent in `settings.rs` (paste/cut/drop handlers) and `routines.rs` (dragover). Both bugs were latent because each module's unit tests cover the math (`bar_height_pct` percentage math, `can_i_afford` policy verdicts) but no test exercises the rendered DOM — the "gap between unit tests and rendered DOM" is real and the bug-finder pattern is now **2-of-2 in Phase 4 logbooks** (the 4.4 + 4.1-4.3 captures on 2026-05-23 surfaced the `render_exchange_rate` time-component bug — same shape, different mechanism). Tags `dioxus, ledger, mobile-development, ui, ux` — picked existing `ui` over new `dashboard` (would have been first instance; if a second dashboard-shaped post comes later, split off then) + added `ux` for the Can-I-Afford policy-decision Learn-by-Doing angle. Cite anchors at `core/src/dashboard.rs:90` (`dashboard_summary` entry) + `:312` (`can_i_afford` policy with the strict-`>` rule). §6 evidence = 14-test exec + 2-bucket prose (aggregator structure 9 tests + Can-I-Afford policy 5 tests including the strict-`>` boundary that proves the user-voiced rule) + two Playwright shots (dashboard overview at default state showing all 5 widgets with trend bars rendering correctly, then Can-I-Afford with $2,500 entered yielding the red "No — you'd be at −$530.10" verdict). §7 carries the liquidity-aware Can-I-Afford deferral (Cycle 4 backlog item, revisit trigger "when distinguishing liquid vs illiquid actually matters") + a concepts-demo candidate for the Can-I-Afford policy-as-a-spectrum (naive cash → cash-after-recurring → liquidity-aware → forecast-aware; lets a reader play through each policy and see when verdicts diverge). **Section-by-section discipline produced 4 new feedback memories** (each surfaced by specific user pushback during a section review, all promoted because the lesson generalizes): [[feedback-logbook-why-over-now]] (§4 must lead with why feature exists, timing is the tail), [[feedback-logbook-no-phase-numbers]] (Phase/Cycle/Session are project-jargon per editorial doc — use event/date anchors), [[feedback-logbook-no-static-enumerations]] (don't snapshot churning sets as if structural — describe capability, name today's mix as today's), [[feedback-logbook-scope-vs-deferred]] (§5 is for design scope, planned-but-not-yet-built belongs in §7 as deferral). [[feedback-logbook-publish-postfixes]] **revised** to reflect that 3 of the original 4 post-fixes are now silently auto-handled by the publish tool (bash{image} stripping + page-bundle layout + image URL rewriting); only the alt-text tracking-ID rewrite remains as a reliable manual fixup, applied to both posts this session. **Orphan-PNG cleanup also shipped (`ff2ac5f`):** 7 settings screenshots from a 2026-05-18 auto-commit (`214a785`) had leaked into the repo root via Playwright's default-cwd write + session-end `git add -A`; deleted with `git rm`, added `/*.png` root-only gitignore rule to prevent recurrence. **Three commits pushed to origin** (`cee3c27` + `ff2ac5f` + `a68b2ee`) so the dashboard post's cite permalinks resolve (editorial-doc carve-out from [[feedback-no-push-during-cycle]]). **Memory captured:** [[feedback-logbook-why-over-now]], [[feedback-logbook-no-phase-numbers]], [[feedback-logbook-no-static-enumerations]], [[feedback-logbook-scope-vs-deferred]] (new); [[feedback-logbook-publish-postfixes]] (revised to reflect tooling improvement). **No commits in mylearnbase per established pattern** — both new post page-bundles + alt-text edits + tooling-feedback entry all sit uncommitted there pending user's manual review and draft-flip. **Next conversation:** user reviews draft posts in mylearnbase + flips draft=false when ready, then Phase 5 starts (W4 budget setup/actuals + W3 recurring detection + unified reconciliation engine). |
| Session 5: Implementation — Phase 4.4 + 4.5 + 4.6 (Cycle 3) | 2026-05-23 | **Phase 4 complete** | **4.4 account list + 4.5+4.6 R1 dashboard shipped in two commits.** **4.4 (`45f37bf`):** new `core::balances::account_summaries` pure fn over `ledger::balances` + `ledger-utils::Prices`; explicit `LISTABLE_ACCOUNTS` allowlist (Learn-by-Doing: user picked exact-match drop-by-default over prefix rule — Assets:Wealthsimple:Cash, Assets:Wise:CAD, Liabilities:CIBC:CreditCard, Unmatched). `AccountSummary` struct carries per-commodity quantities + base-currency `total_in_base` per balance; merges declared-account metadata (display_name, last_reconciled_through). Tauri `account_summaries` cmd reads `<app_data>/budget.journal`; frontend `AccountListView` + `AccountSummaryCard` with pure `format_money` formatter (thousands separators + 2-decimal pad + crypto-precision preservation). **Surfaced + fixed `render_exchange_rate` bug:** P directive was missing required time component (ledger-parser's `parse_commodity_price` requires datetime, not date) — every Frankfurter-written P directive in the user's journal would have broken `account_summaries` parsing on first run. New `render_exchange_rate_roundtrips_through_parser` regression test locks the contract. **Cycle 4 backlog:** lift `LISTABLE_ACCOUNTS` into Settings-managed `accounts` table (per `feedback_proportional_commits` + user observation "I may change my mind a lot"). **4.5+4.6 (`3d2f3f3`):** `core::dashboard::dashboard_summary` pure fn produces 4-widget payload (net worth + Unmatched + monthly buckets + recurring). Net worth = sum of listable accounts' `total_in_base`, **`Unmatched` explicitly excluded** because it's the reconciliation-pending clearing account, not real money. Monthly trend bucketing classifies posting by account prefix (`Income:` → flip sign for display, `Expenses:` → pass through); foreign-commodity legs convert via `Prices` or drop silently. `bucket_postings_by_month` splits into a `_parsed_postings` inner variant taking `serde_json::Value` so tests don't need DbValue construction (surrealdb-types v3 doesn't expose a `from_json_value` constructor). Same DbValue-detour pattern for `distill_recurring`. **Learn-by-Doing: `can_i_afford` policy.** User picked **conservative-after-recurring** (option #2) — `net_worth - next_month_recurring_total - amount > 0`, strict greater-than (exactly $0 left = can't afford, per user "I would never want my net worth to go to 0 after any purchase"). Returns `AffordVerdict { can_afford, remaining_in_base, policy_label }`. After review feedback, `policy_label` redesigned to name the rule that fired ("Net worth − next month's recurring") rather than restate the math — frontend composes the message from `remaining_in_base` via `format_money`. 5 verdict-shape tests (afford / can't afford / exactly-zero boundary / no data / refund). New `next_month_recurring_total` helper normalizes any cadence to per-month equivalent (`amount * 30 / cadence_days`); drops foreign-commodity recurring patterns. Tauri commands `dashboard_summary` + `check_affordability` with `dashboard_to_view` / `bucket_to_view` / `recurring_to_view` boundary mappers (Decimals → String); `month_cutoff_date` helper scopes the trend query to the requested window. Frontend `DashboardView` + 5 widget cards: `NetWorthCard`, `UnmatchedCard` (amber accent + click-through when pending, white when steady-state zero), `MonthlyTrendCard` (paired income/spending bars per month, scaled by `max_trend_magnitude` + `bar_height_pct` pure helpers), `RecurringCard` (vendor + amount + `cadence_label` per row), `AffordCard` (amount input + verdict badge). Unmatched click-through routes to `TransactionList` filtered to `account: "Unmatched"` via new `initial_filter` prop on `TransactionListView` + one-shot `pending_txn_filter` signal in `FinancesPage` (drained + cleared on `TransactionList` render so back-and-forth doesn't replay). 7 new frontend pure-fn tests for dashboard helpers (`max_trend_magnitude`, `bar_height_pct`, `cadence_label`). Dashboard CTA added to Finances Home with accent-tinted styling. **Cycle 4 backlog entry:** liquidity-aware `can_i_afford` — user noted: "I obviously would like the fancy check with liquidity, but we don't have the data for it"; once declared accounts grow an `is_liquid` flag (alongside the Settings UI from the 4.4 backlog item), `can_i_afford` becomes `liquid_assets - recurring - amount`. **Tested:** 378 workspace lib tests pass (added: 7 in `core::balances`, 14 in `core::dashboard`, 11 frontend `format_money` + dashboard helpers); clippy `--all-features -D warnings` clean. **Unverified:** no `dx serve` / Playwright walkthrough; live FX conversion against real journal not exercised (mock fixtures only). **Memory captured:** none — implementations of planned tasks, no surprise architectural shifts; the two design calls (allowlist + afford policy) live in code as project-shaped decisions, not memory. **Next conversation:** user starts logbook capture for 4.4 (account list) and 4.5+4.6 (R1 dashboard) bundles in pair-mode, then continues to Phase 5 (W4 budget + W3 recurring detection + unified reconciliation). |
| Session 5: Implementation — Phase 4 logbook capture for 4.1+4.2+4.3 bundle (Cycle 3) | 2026-05-23 | Complete (published as draft, mylearnbase uncommitted) | **Logbook entry "Browse, view, and edit recorded transactions" assembled + published.** Mock backend seeded with 4 fixture transactions + 35 KB synthetic Loblaws receipt PNG (`bridge.rs` + new `mocks/` dir; committed `1465ec2`) to make `dx serve --features mock` render meaningfully for Phase 4 read-path screenshots. Three load-bearing Playwright screenshots at 390×820 mobile portrait, one per externally-observable behavior (filtered list / detail-with-attachment / inline-edit mid-state — the last opportunistically caught when click-resolution landed on the `+ tag` affordance, proving `stop_propagation` works without needing to set it up deliberately). **Draft assembled section-by-section per user direction** (new memory [[feedback-logbook-section-by-section]]): §3 names list+filter, detail+attachment, inline edit in four sentences one per sub-feature (revised from a too-dense paragraph carrying two triple-lists); §4 builds the causal chain prior-bundle-gap → minimum-loop-closure → bundling rationale (revised once to drop disingenuous "transactions started landing" framing — the app is pre-daily-use so claiming usage-driven causation was dishonest; revised again to clarify ambiguous "the feature" referent that defaulted to the post title rather than the app-level snap-see-fix loop); §5 bulleted-with-clauses (clauses carried info not in §3) + separate trailing paragraph for the known pagination-state-resets limitation; §7 skipped (publish prunes — six plausible bullets would have been forced-full); §6 has 2 exec blocks (6 backend filter tests + 5 frontend pure-fn tests), 5 commit-pinned cites (first filter test + `TransactionListView` + frontend `mod tests` + `impl Drop for ObjectUrlGuard` + `EditableCategoryChip`), 3 screenshots with descriptive alt-text, 7 prose threads connecting elements + a closing "what's not in the evidence yet" paragraph (live WebView round-trip on Android still unverified). Published via `logbook publish --skip-verify` because the captured `cargo test` output reorders between runs (`tokio::test` doesn't guarantee execution ordering — distinct from the 2026-05-17 capture-bundle session's compile-timing variant of the same nondeterminism family). **Four manual fixups required post-publish** — three reproducible publish-tool quirks (showboat `bash {image}` blocks not stripped from published markdown; copied PNGs land with showboat tracking-ID filenames; flat-file vs page-bundle layout) plus one new failure mode (`zola serve --drafts` refuses to build the entire site rather than 404'ing a single broken `@/posts/.../slug.md` cross-post link — discovered when the link to `capture-transaction-with-receipt` failed because that prior post had been bundle-restructured in an earlier session; the page-bundle link form is `@/posts/.../<slug>/index.md`). Capture post (`capture-transaction-with-receipt`) updated in-place to past-tense Gemini-verified framing — honors the original "this section gets updated" promise made at first publication. 3 new tooling entries added to `editorial/_feedback/logbook.md` (screenshot output not processed by publish; init --title set silently; cross-post link breakage refuses Zola build). 4 editorial-direction observations (§4 disingenuous specificity, §4 ambiguous "the feature" referent, §3 two-triple-lists splittable-sentence tell, §5 bulleted-vs-prose decision criterion) **intentionally NOT recorded** — single-session data points; user direction is to wait for recurrence before promoting. **Memory captured:** [[feedback-logbook-publish-postfixes]] (4 reproducible publish-tool quirks requiring manual fixup), [[feedback-logbook-section-by-section]] (per-section drafting cadence — discuss each section before running CLI to commit), [[feedback-logbook-feedback-doc-scope]] (`editorial/_feedback/logbook.md` is for tooling-behavior observations only, not editorial direction). **No commits in mylearnbase per user direction** — new post page-bundle at `content/posts/logbook/omni-me/browse-view-edit-transactions/` + capture-post correction + 3 feedback-doc entries all sit uncommitted there pending tooling fixes. **Next conversation:** Phase 4.4 account list (multi-currency balances; Frankfurter + manual NGN `P` directive paths converge) and/or 4.5+4.6 R1 dashboard. |
| Session 5: Implementation — Phase 4 logbook bundle 4.1+4.2+4.3 (Cycle 3) | 2026-05-21 | Complete (logbook stop) | **Phase 4.1+4.2+4.3 shipped browse-first in three commits.** **4.1 (`bbd7d0e`):** browse-first slice expanded to include filters. Backend `TxnFilter` struct (date_from/date_to/account/tag/category) with `normalize()` that drops blank strings; dynamic WHERE-clause builder (each axis adds a clause iff Some) keeps the SurrealDB query terse when no filters are set. Account substring match uses `array::any(postings, |$p| string::lowercase($p.account) CONTAINS string::lowercase($account))` — SurrealDB v3 closure syntax verified against `surrealdb-core` source + 6 new tests against real DB fixture. Tauri command shape: `TransactionView` clean wire type with `into_json_value()` round-trip for FLEXIBLE `postings` + `attachment`. Frontend: `FinancesView::TransactionList` variant, `TransactionListView` shell with mount-load + offset/page-50 pagination + load-more button + loading/error/empty states; `TransactionListRow` ledger-style (all postings visible, flex-wrap header with category chip + tag chips + cleared check + attachment paperclip — three UX calls made by user via AskUserQuestion w/ preview). `FilterBar` component with `draft_filter` / `active_filter` separation (typing doesn't fire queries; Apply copies draft→active and `use_effect` re-runs on active_filter change). Home placeholder ("Transactions list lands in Phase 4") replaced with a real navigation button. **4.2 (`5012083`):** new `get_transaction` Tauri command + register. `invoke_get_transaction` + `invoke_fetch_attachment` bridge invokes. `FinancesView::TransactionDetail` variant + `selected_txn_id` sibling signal (variant stays Copy). `TransactionListRow` becomes a clickable button → routes to detail. `TransactionDetailView` fetches by id on mount; `TransactionDetailBody` renders full metadata (date + description header, cleared badge with cleared_date suffix, statement_source line) + postings panel. `AttachmentViewer` uses object-URL rendering: `ObjectUrlGuard::from_bytes(bytes, mime)` builds a `Blob` via `js_sys::Uint8Array` + `web_sys::BlobPropertyBag`, returns `URL.createObjectURL(blob)`; **Drop impl calls `URL.revokeObjectURL`** so the WebView doesn't leak object URLs as the user navigates in and out of detail views. `classify_attachment(mime)` routes to one of three render arms: `<img src=url>`, `<iframe src=url>` (PDF — WebView renders natively), or download link. Added `Blob` + `BlobPropertyBag` + `Url` features to frontend `Cargo.toml` web-sys deps. 5 new pure-fn tests for `classify_attachment` + `extract_attachment_meta`. **4.3 (`9db909d`):** `EditableCategoryChip` + `EditableTagList` reusable components. Click chip → input replaces it (autofocus); Save on Enter or button click, Cancel on Escape, empty-trim clears category, dedupe-then-push for tags. **All chip-internal clicks call `e.stop_propagation()`** so the parent list-row's click-to-detail doesn't fire mid-edit. Closure-share pitfall: `Signal::set` requires `&mut`, so a `commit` closure that calls multiple Signal::set calls becomes FnMut and isn't Copy — can only `move` into one event handler; resolved by inlining the commit logic at each call site instead of sharing a closure variable. Wired into both `TransactionListRow` and `TransactionDetailBody` via a local `Signal<TransactionView>` seeded from the prop, so save → optimistic local update renders immediately without parent refetch. `invoke_categorize_transaction` + `invoke_tag_transaction` bridge invokes (backend `categorize_transaction` + `tag_transaction` Tauri commands already existed from Phase 1.8); both projection handlers replace-not-append (tag handler sets `tags_top = $tags`, so frontend computes the new full vector each edit). **Tested:** 294 core + 38 tauri-app lib tests pass; 6 new backend filter tests verified the dynamic-WHERE query against a real `tempfile::tempdir` + `BudgetProjection` fixture (including blank-string normalization + tag membership via `IN tags_top` + category exact + date range + account case-insensitive substring); 5 new pure-fn tests in `pages::finances::tests` for `classify_attachment` (image/PDF variants, case-insensitive, fallback to Other) + `extract_attachment_meta` (complete ref + missing sha256 returns None). Workspace clippy + frontend clippy clean with `-D warnings`. **Unverified-and-logged:** no `dx serve` / Playwright visual verification (user opted "full speed ahead" — deferred to logbook-capture session); object-URL lifecycle on rapid back-nav not exercised in WebView; live edit flow against a real captured transaction not run end-to-end. **Cycle 4 backlog:** transaction-list pagination state resets to page 1 on any nav-to-detail-and-back (each FinancesView arm re-mounts the list component fresh); would need a shared store keyed by list-instance to preserve offset across nav. **Memory captured:** none — Phase 4 work is straight implementation of planned tasks; no surprise architectural shifts to record. **Next conversation:** user starts logbook capture for the 4.1+4.2+4.3 bundle ("Browse, view, and edit recorded transactions"; tags per `tasks.md` annotation: dioxus, tauri, surrealdb, ux) in fresh context, then drafts the logbook post before continuing with Phase 4.4 (account list with multi-currency balances) and 4.5+4.6 (R1 dashboard). |
| Session 5: Implementation — Phase 3.10 UI + Android device test + Phase 3 close-out (Cycle 3) | 2026-05-18 → 2026-05-20 | **Phase 3 complete** | **Phase 3.10.5+.6 (`bf7e00a`):** Tauri commands `list_pending_batches` / `commit_batch(batch_id, accepted_indices, fx_rate?, fx_commodity?)` / `dismiss_batch(batch_id, reason?)`; commit fans out `TransactionRecorded × N` + optional `ExchangeRateRecorded` (`pick_fx_event_date` = latest accepted-row date per user decision) + `AutoImportBatchCommitted`. Finances UI: pending-count banner on Home (`use_resource` keyed to Home view), `FinancesView::{BatchList, BatchReview}` variants with `Signal<Option<String>>` sibling for selected `batch_id` (keeps the enum `Copy`); per-row accept checkbox, source-metadata `<details>`, manual-FX rate prompt gated on `MANUAL_FX_CURRENCIES_UI` (NGN today). **Phase 3.10.7 (`491452d` + `d682c24`):** first-pass safe-area attempt (`pb-mobile-nav` calc with `env(safe-area-inset-bottom)`, viewport-fit hook) + server tick-interval env override; then a cargo feature gate (`auto-import`) lifted out of `core` so the Tauri client cross-compile no longer pulls in `imap`/`native-tls` (Android target was failing to link OpenSSL). **Phase 3.10.8 (`86dfb05`) — the deferred mobile-UX win:** structural Android safe-area inset bridge. The chain that needed to work: `enableEdgeToEdge()` ✓ + `viewport-fit=cover` ✓ + `env(safe-area-inset-*)` ✗ — diagnosis via an in-app diagnostic banner showed `sa-bot 0px` despite `sa-top 24px` working; Chromium Android WebView auto-forwards display cutouts + top status bar but **not** the bottom navigation/gesture bar (long-standing limitation, not Tauri-specific). The fix: `MainActivity.onCreate` installs an `OnApplyWindowInsetsListener` that reads `systemBars + navigationBars + tappableElement` (max — different Android versions report nav-bar height under different inset types), converts physical px → CSS px via `displayMetrics.density`, then `evaluateJavascript` injects `--safe-area-inset-{top,bottom,left,right}` CSS custom properties onto `document.documentElement.style`. Frontend CSS reads `var(--safe-area-inset-bottom, env(safe-area-inset-bottom, 0px))` so iOS / desktop still get `env()` natively and Android gets the bridged value. Listener fires only in the first ~150ms (before the 54MB WASM bundle has parsed), so a five-tier `postDelayed` schedule (0.5 / 1.5 / 3 / 6 / 10s) re-injects onto the loaded document. **Phase 3.10.9 (`152544a` + auto-end `a6e0c91`):** extracted bridge from `MainActivity.kt` into `InsetBridge.kt` so the larger logic lives in a non-templated file; then migrated both files into the project's existing `tauri-app/src-tauri/android-overrides/` pattern that `build.rs::apply_android_overrides` already uses for Phase 3.3's share-target — survives `tauri android init` regenerations cleanly. (Lesson captured: before adding new infrastructure, grep for an existing solution to the same problem — found the override mechanism only at final review, after force-adding into `gen/android/` first.) **Auto-import device end-to-end test 2026-05-18 evening:** APK install dance — debug build baked in `TAURI_DEV_HOST=10.2.0.2:1430` from a prior `cargo tauri android dev` invocation (cached `tauri-build` output didn't re-read env), required `cargo clean -p omni-me-app --target aarch64-linux-android` + `TAURI_DEV_HOST=` + release build (debug bakes dev URL via `cfg!(debug_assertions)`); LTO=true OOMs the laptop's surrealdb-core compile → `CARGO_BUILD_JOBS=2` + `CARGO_PROFILE_RELEASE_LTO=false` + `CODEGEN_UNITS=16`. Manual `zipalign` + debug-keystore signing landed the signed APK. End-to-end flow verified on device: banner picked up `gmail_personal` pending batch over Tailscale, review screen rendered, the dollar amount matched the actual Walmart receipt email, Commit fan-out worked. **Phase 3 followup (today, `beb7a6d`):** dropped `statement_extraction_to_events` + `receipt_extraction_to_events` + their `build_event` helper from `core/src/extraction/event_mapper.rs` (~200 lines of dead code, kept alive only by their own tests post-3.10.3 refactor); mapper tests rewritten against `*_to_drafts` to preserve coverage shape. Both `wise.rs::pull_projects_proposed_batch_with_unmatched_mirror` + `wealthsimple.rs::pull_projects_proposed_batch_with_unmatched_mirror` now register `AutoImportProjection` alongside `BudgetProjection` in `test_db_and_runner` and assert source name + dedup-key prefix + pending status + per-draft external_id format + Unmatched-mirror sign convention against the projected `pending_auto_import_batches` row (previously was `let _ = db; // until 3.10.4 lands` — a TODO-in-code that rotted after 3.10.4 actually shipped). Settings sections 3.8 (`CacheSection` at `tauri-app/frontend/src/pages/settings.rs:234`) + 3.9 (`AutoImportSection` at `:494`) confirmed implemented — checkbox drift only, not actual gap. **288/288 core lib tests + 46/46 auto-import tests pass; clippy clean with `-D warnings`.** **Three auto-import source-health bugs logged in Phase 2 Known Limitations** (`gmail_work` + `yahoo` intermittent cursor-save `Connection uninitialised`, `gmail_personal` 502-after-success from manual tick endpoint, `wealthsimple-python` OTP re-auth needed) — pre-existing server issues surfaced by the device test, not new regressions. **Memory captured:** [[project-android-safe-area-inset-bridge]], [[feedback-disk-reclaim-before-android-build]] (`df -h` + aggressive `cargo clean` before heavy cross-compile; 41GB reclaimed via one workspace clean), and the cumulative `feedback-no-100-percent-claim` reinforcement. **Cycle 4 carries:** chain `OnApplyWindowInsetsListener` instead of replacing (so soft-keyboard handling survives if any exists); upstream `cargo:rerun-if-env-changed=TAURI_DEV_HOST` to `tauri-build`. |
| Session 5: Implementation — Phase 3.7 attachment cache (Cycle 3) | 2026-05-17 | Complete (logbook stop) | **Phase 3.7 attachment cache + dropped-bytes fix shipped in one pass.** Server-side: `/documents/extract` extended with `?attach=true` query — when set, handler hashes body (SHA-256), atomic temp + rename into `state.blob_dir`, returns wrapped `ExtractResponse { extraction, attachment: AttachmentRef }`. Single round trip; idempotent if hash already present. Client-side: new `commands::attachments` module with `cache_write` / `cache_read` / `evict_to_cap` / `cache_clear` + 3 Tauri commands (`fetch_attachment` cache-first w/ `/blobs` fallback + repopulate, `attachment_cache_size`, `clear_attachment_cache`). 200MB cap, mtime LRU (atime unreliable on `noatime` mounts; touch-on-hit keeps frequently-fetched blobs resident). `extract_document` Tauri command now always sends `attach=true`, parses new wrapped response, mirrors bytes into local cache on success (non-fatal cache-write failure — extraction still wins). `AttachmentRef` mirrored in frontend `types.rs`, threaded through `ExtractedDraft` → `TransactionForm` (own `Signal<Option<AttachmentRef>>` initialized from `initial.attachment`) → included in `TransactionFormDraft` payload. `TransactionForm` shows "Attachment saved · filename · N bytes" row in lieu of thumbnail (thumbnail render is Phase 4 transaction-detail territory — `fetch_attachment` is already in place). 5 attachment unit tests pass (write/read roundtrip, miss returns None, eviction by mtime, invalid-hash rejection, clear keeps dir). `cargo clippy --all-targets` clean for server + tauri-app; ALSO fixed 4 pre-existing stale test fixtures in `server/tests/{common/mod.rs,sync_phase2_integration.rs}` that were missing the `extractor` field (predated this session; `NullExtractor` plugged in). Frontend WASM clippy clean for both mock + non-mock features. **Cycle 4 backlog:** "Cross-submodule state management" — tab-switch during in-flight async loses progress without warning (today's symptom: photo + extract mid-flight + tap another tab = silent loss); broader rework is to lift in-flight captures into a top-level shared store keyed by capture-id. **Phase 3 Known Gaps** updated: dropped-bytes item marked RESOLVED; tab-switch race annotated with the Cycle 4 link. **Next conversation:** user starts the logbook capture for the 3.1-3.6+3.7 bundle in fresh context (first one). |
| Session 5: Implementation — Phase 3 capture flows (Cycle 3) | 2026-05-17 | Complete (logbook stop) | **Phase 3 capture-flow bundle (3.1 + 3.2 + 3.4 + 3.5 + 3.6) shipped in 5 commits.** 3.3 partial. Stop point is the start of the logbook capture work (user picks up in fresh context). **3.1 (`ab18561` mock, `9f37de7` real):** new `Finances` tab in side+bottom nav (Tab::Finances added; "Money" rejected, "Ledger" rejected, "Finances" picked — covers capture+transactions+accounts+budget+dashboard+reconciliation). `FinancesPage` with capture-tile grid (Photo/PDF/Email/Manual). `PhotoCapture` sub-view with file picker (`accept="image/*"` + `capture="environment"` mobile-camera hint), state machine, retry button. Learn-by-Doing: user designed `CaptureState` enum (Idle/Working/Draft/Error with `retry_bytes: Option<(Vec<u8>, String)>` for one-tap retry without re-pick) — got the Signal-vs-local-variable distinction wrong on first attempt (assigned `state = X` instead of `state.set(X)`), corrected with insight on Dioxus's reactive runtime + double-clone idiom for FnMut event handlers. Server endpoint `POST /documents/extract?hint=<hint>` (raw bytes body, Content-Type for MIME, query for hint, 15MB body limit per-route; `extractor: Arc<dyn DocumentExtractor>` lifted into `AppState` always — NullExtractor fallback if no credentials). Tauri command `extract_document` proxies via `reqwest` from `tauri-app` to server. **3.2 (`d41a819`):** refactored `PhotoCapture` → generic `DocumentCapture(kind: DocumentKind)` covering Photo + PDF; PDF surfaces a hint picker (bank_statement / brokerage_statement / paystub / receipt) because `route_from_mime` returns `None` for `application/pdf`. **3.3 (deferred):** added Android `SEND` intent filter for `image/*` + `application/pdf` to `gen/android/.../AndroidManifest.xml`, but that path is gitignored. Durable home requires Tauri 2 override convention + Rust intent-handler — deferred to a dedicated Android build session. **3.4 (`9f8bea4`):** `EmailCapture` — textarea + Extract button, sends `body.into_bytes()` with MIME `text/plain` + hint `email_body`. Same state-machine pattern as DocumentCapture (FnMut `kick_off` closure shared between initial Extract and Retry buttons; Signal handles being Copy is what makes this work). **3.5 + 3.6 (`f539f37`):** unified `TransactionForm` for manual entry + confirm-draft. Bubble-up refactor: removed `Draft(ExtractedDraft)` variant from DocumentCapture+EmailCapture `CaptureState`, added `on_extracted: EventHandler<ExtractedDraft>` prop; parent `FinancesPage` owns `pending_draft: Signal<Option<ExtractedDraft>>` (lives outside `FinancesView` enum to keep the enum `Copy`). `FinancesView::TransactionForm` rendered from both Manual tile (initial=None) and post-extraction (initial=Some(draft)). Form validates date+description non-empty, ≥2 postings, numeric amounts (lax f64 parser — see Phase 3 Known Gaps for the rust_decimal-strict mismatch). Save calls `invoke_record_transaction` → existing `record_transaction` Tauri command from Phase 1. Visual verification via Playwright: Photo → 1.2s mock extract → TransactionForm pre-populated with canned Loblaws receipt (date `2026-05-17`, description `Loblaws — Groceries`, postings `Expenses:Groceries 42.18 CAD` / `Assets:Wealthsimple:Cash -42.18 CAD`) → Save → back to Home. Manual flow opens empty form (2 default posting rows, CAD default commodity, `+ Add posting` affordance, `Remove` disabled below 2 rows). **Honest framing:** Phase 3 ships at the UI surface against mock; real backend wiring is in place (server endpoint + Tauri command) but the **live Gemini round trip is unobserved**. `cargo tauri dev` runtime path stays on the Phase 2 known-list. **Phase 3 Known Gaps documented in dedicated `project.md` section** — most consequential: attachment bytes are discarded after extraction (no SHA-256 + `/blobs/{hash}` upload + `AttachmentRef` wiring), no posting-balance validation, amount parser laxness vs. `rust_decimal` strictness, tab-switch-during-capture unmount race semantics unverified. **Honest skipped 3.6 spec item:** "attachment thumbnail visible" — got dropped during the bubble-up refactor; called out as the highest-priority gap. **Next conversation:** user starts logbook capture in fresh context (first one, taking time to learn the workflow). |
| Session 5: Implementation — Phase 1 + Phase 2 (Cycle 3) | 2026-05-17 | Complete | **Massive 42-commit day: all of Phase 1 (Core Foundation) + all of Phase 2 (Server-side capture + Auto-import) shipped + verified live against real APIs.** **Phase 1 (~14 commits):** Cold-start began with Learn-by-Doing on `Tag` representation (user chose enum with custom `Display + FromStr`, picked up a deep insight about `serde_with::DisplayFromStr` adapters being type-generic). Landed 16 budget event types (`TransactionRecorded` + 15 others including `TransactionsMerged` and `TransactionCleared` for unified reconciliation). Built journal-file projection (append-on-event hledger writer at app_data dir), SurrealDB `BudgetProjection` (4 tables × 15 handlers; `transactions_merged` uses BEGIN/COMMIT for atomic merge). 12 Tauri budget commands shipped + registered in `invoke_handler`. `core/src/accounts.rs` got A2 helpers + `Unmatched` mirror builder + posting validation. `core/src/ledger.rs` wired `ledger-parser` v6 + `ledger-utils` v0.6 with round-trip test against `journal_file::render_transaction` — closes 1.13's "golden sample" property behaviorally. Projection idempotency tests added (rebuild = byte-identical state). **Phase 2A (~5 commits):** `DocumentExtractor` trait + per-hint `ExtractionHint` + hybrid routing (sender > MIME > PDF-needs-user-pick) + `NullExtractor` for tests. `GeminiExtractor` wraps `GeminiClient.complete_multimodal_json` (base64-encoded inline PDF/image data). Verification pass (`verify` + threshold gate; clamps confidence on failed checks). Frankfurter FX fetcher with `ExchangeRateRecorded` event + `P`-directive emission in journal-file projection. `#[ignore]`-gated integration tests scaffolded for receipt/brokerage/paystub/email fixtures. **Phase 2B (~10 commits):** `2.9-spike` SnapTrade REJECTED (live-verified during signup — service under maintenance + URL targeted at WS Trade only; per [[project-ws-api-strategy]] the 95% volume lives in Cash + Chequing + Crypto which Trade doesn't cover). WS path B subprocess wrapper shipped + driver script template (`scripts/wealthsimple_driver_example.py`). Wise REST client built + **live-verified against real account** — caught 2 bugs on first call (Wise returns `"type":"PERSONAL"` uppercase, my code matched lowercase; `amount.value` is JSON number not string, custom `deserialize_lenient_decimal` accepts both via `Number::to_string()` to preserve precision without f64 round-trip). IMAP poller infrastructure (`ImapFetcher` + `ImapHandler` traits + dispatch + cursor). Multi-account credential schema (`HashMap<String, ImapCredentials>` for `gmail_personal`/`gmail_work`/`yahoo`). SC NGN/USD password derivation (3rd-8th chars of NUBAN, confirmed against the SC notification email body) + `pdftotext -upw`-based decrypt + extract — **live-verified against the user's real password-protected USD statement** (2207 chars of clean text). `mail-parser` MIME helper verified against `.reference/imap poller/*.eml`. `ScNgnHandler` + `ReceiptHandler` (catch-all sender pattern list). `setup_from_credentials` glue + `auto_import_scheduler` (per-source loop + exponential backoff to 1h cap). Real `AsyncImapFetcher` using sync `imap` crate + `tokio::task::spawn_blocking` — **live-verified pulled an actual message from the user's real Gmail INBOX**. **User-pushed audit revealed major wiring gaps** (BudgetProjection + JournalFile not registered in Tauri runner; `setup_from_credentials` never called; handlers had stub `Ok(Vec::new())` returns; no `Gemini-Extractor` plumbed) — closed in 6 commits over 30 min. **`.env` → `credentials.toml` port script** written + run cleanly; loader roundtripped all 4 categories (wise/gemini/wealthsimple_python/2 sc_accounts/3 imap). **User-pushed second audit revealed architectural inconsistency** (auto-import wired to Tauri client despite [[feedback-llm-server-side]] saying Gemini calls go server-side) — relocated entire wiring from `tauri-app/src-tauri/src/lib.rs` to `server/src/main.rs` in one commit. **WS live-verification dance:** installed `ws-api` 0.33, attempted login with real creds, hit OTP exception, fixed 4 driver bugs across multiple OTP retries (`get_activities` not `get_account_activities`, `WealthsimpleAPI.login` is static-returns-session not method, `WSAPISession.from_json` expects raw string not pre-parsed dict, camelCase field names in activity envelope — `canonicalId`/`occurredAt`/`amountSign` not snake_case). After fixes: pulled 7 WS accounts (Crypto, 3 TFSAs, RRSP, FHSA, General Checking, Back to school) + 297 transactions. **User-pushed third audit (the "you sure?" pattern)** revealed wiring gaps in helper-construction layer (WS `driver_script` None by default, IMAP sources never constructed) — closed by extending `WealthSimplePythonCredentials.driver_script` + `SourceConfig.imap_sources: Vec<Arc<ImapSource>>` + per-account `build_imap_sources()` in caller. **Server-boot smoke test (after relocation)** caught a real bug: `WealthSimpleSource::run_driver` didn't pipe `session_path` through stdin even though the driver demanded it (fixed by adding `session_path: Option<PathBuf>` to `WealthSimplePythonCredentials` + populating in JSON envelope). **User-pushed fourth audit** ("100% sure there's nothing else?") surfaced concrete concerns landed inline this session: (a) Wise `fxRate` + `totalFees` were silently dropped → now extracted as `Tag::KeyValue` posting tags (`wise_fx_rate:...` / `wise_fee:1.01 CAD`) for user verification; (b) `.reference/`-loading tests panic on missing fixtures → all fixture loaders now skip gracefully with `eprintln!`; (c) server `tracing-subscriber` env filter defaulted to `omni_me_server=debug` only, hiding all `omni_me_core::auto_import_scheduler` tick logs — now includes `omni_me_core=info` so auto-import activity surfaces in default `./omni-me-server` runs. **Significant security incident** mid-session: while inspecting `.env` for a quoting issue (`WS_PASSWORD` contained shell metacharacters), I used `substr` on the line content as a "diagnostic" and leaked a chunk of the user's WS password into the conversation. User rotated the password + single-quoted the value in `.env`. New feedback memory [[feedback-no-substring-secret-inspection]] saved. Several other lessons captured: [[feedback-smoke-test-catches-integration-bugs]] (35 LOC of smoke setup found a real production bug unit tests missed), [[feedback-no-100-percent-claim]] (user had to push 4 separate audits to extract real gaps; should default to "tested but not verified" framing). [[project-auto-import-host]] memory records the server-side architectural choice. [[project-ws-api-strategy]] updated with the live-verification + 4 driver bugs found. **Documented Phase 2 known limitations** in dedicated `project.md` section: JournalFile projection only writes on 3 of 16 budget events (divergence on mutations); WS driver `description` field falls through fragile key chain; no frontend bindings for budget commands (Phase 3 work); `tracing-subscriber` Tauri + server filters not unified; `imap-proto` future-Rust warning; WS first-run OTP dance + no UI for "session expired"; SurrealDB tempfile test race documented + unfixed; Tauri `cargo tauri dev` runtime not smoke-tested today (only server was). |
| Session 1: Initiation | 2026-03-02 | Complete | Defined goal (all-in-one personal life app with LLM processing + data sovereignty), user (self only), success criteria (daily use by choice, adaptable), timeline (MVP end of March, prototype by September), and motivation (tax pain, lost ideas, no more excuses) |
| Session 2: Research | 2026-03-05 | Complete | All 13 features reviewed, core tech decisions finalized (Tauri v2, Dioxus, SurrealDB, Event Sourcing, Gemini Flash). Output: `research.md` |
| Session 3: Architecture | 2026-03-07 | Complete | Formalized `architecture.md` — security review, risk assessment (5 risks with mitigations) |
| Session 3: Planning (Cycle 1) | 2026-03-08 | Complete | 38 tasks across 7 phases. PWA deferred. All Claude Code. 3 parallel tracks for core infrastructure. Output: `tasks.md` |
| Session 4: Implementation | 2026-03-08 | Complete | Phase 0 POCs all passed (desktop + Android). SurrealDB, Tauri, Dioxus, CodeMirror validated. |
| Session 4: Phase 1 | 2026-03-25 | Complete | Workspace scaffold (1.1), SurrealDB connection layer (1.4), Axum server (1.5), CI/CD workflow (1.3). VPS deferred — DO rejected payment, going to Hetzner when stable. |
| Session 4: Phases 2-6 | 2026-03-29 | Complete | Event store + sync, LLM pipeline, UI shell + CodeMirror, Journal/Notes, Routine Manager — all features functional on desktop. Code review done (6 fixes applied). |
| Session 4: Phase 7 | 2026-03-30 | Complete | Tracing, editor fixes, sync wiring, settings page, Android APK, Tailscale sync. Fixed projection apply after sync pull. Found SurrealKV stability issue (commit queue overflow after ~24h). Cycle 1 complete — all features working, sync verified phone→server→desktop. |
| Session 4: Planning (Cycle 2) | 2026-04-19 | Complete | 48 tasks across 7 phases + 3 stretch. Tier 1 (editor revamp + auto-save/sync + journal/generic split + Obsidian import/export) + full Tier 2 (routine UX). Breaking event schema changes accepted. Time-of-day labels removed. Day-close: manual + auto (when 3 properties filled). Tier 3 (budget) deferred to Cycle 3. Output: `tasks.md`. |
| Session 5: Phase 0 (Cycle 2) | 2026-04-19 | Complete | Core foundation rewrite — event schema split (journal/generic), routine event expansion (reorder/remove/undo/modify), DataWiped event, frequency parser (Learn-by-Doing), notes+routines projections rewritten, auto-close background tick (chrono-tz, local midnight+30s grace), 15 new Tauri commands. 99 workspace tests. Commit `1031e23`. |
| Session 5: Phases 1-3 (Cycle 2) | 2026-04-19 | Complete | Three parallel worktree subagents merged: Track B editor (auto-wrap, checkbox widget, journal-mode timestamp, dirty/clean IPC), Track D sync (debounced buffer 1s, push debouncer 2s, exponential backoff 1→60s ±10% jitter, probe-based network monitor, retry accelerator, 4-state status reporter, kill-server integration test), Track C nav shell (responsive sidebar/bottom at 768px, Notes tab added, Journal Today/Calendar sub-tabs, Notes Recent/Search sub-tabs, sync status chip). Integration glue: frontend `SyncStatusSnapshot` mirrors backend, editor `journal_mode` prop wired through. Commits `48578ff..a0e407b`. 113+ tests passing across workspace. |
| Session 5: Phase 4 (Cycle 2) | 2026-04-20 | Complete | Calendar + Day view landed sequentially on main (Phase 3 merge had already done 4.3/4.4/4.5). `TodayView` → `DayView(date)` refactor with keyed remount; shared `selected_date` signal lets calendar clicks jump to arbitrary days with "← Back to today" link. `CalendarView` builds 6×7 Monday-first spillover grid, reactive to month nav, with per-day accent dots pulled from `list_journal_dates`. `build_month_cells` implemented via Learn-by-Doing (user wrote `std::iter::successors` variant after review feedback). Host-runnable logic test locked in — confirmed `cargo test` on default target works for non-browser code in the wasm-targeted frontend crate. Commit `957a4ce`. |
| Session 5: Phase 6 (Cycle 2) | 2026-04-20 | Complete | Tier 2 routines landed sequentially on main across two sittings. Sitting A: Data Wipe (Danger Zone in Settings, arming + typed-phrase confirmation, paste/cut/drop disabled, emits `DataWiped`) + remove buttons for items and groups (6.3, 6.4, 6.7). Sitting B: routine-item edit form (inline Edit/Save/Cancel per step, name + duration + unit) with `duration.rs` helper for min↔hour conversions — `split_minutes_for_display` implemented via Learn-by-Doing (user chose exact-divisor policy); duration unit picker added to both Add and Edit forms; frequency picker expanded with Biweekly / Monthly / Custom-N-days + clamped inline int input serialized as `custom:{N}` (6.1, 6.2, 6.5). Sitting C: drag-to-reorder routine groups on Daily Flow — `reorder.rs` pure logic module (`reorder_groups_after_drop` + `to_orderings_payload`) implemented via Learn-by-Doing (user chose asymmetric drag-up-before / drag-down-after policy after iterating on symmetric filter-trick), wired through HTML5 DnD with optimistic `pending_order` override signal and `invoke_reorder_routine_groups` (6.9). Visual testing via `dx serve --features mock` + Playwright MCP verified drag-reorder, custom-N picker, edit form, add form. |
| Session 5: Phase 7.3 + Phase 5.1 | 2026-04-22 | Complete | Notes search clear button (X inside input, Escape key also clears) — 7.3. Journal template engine: `journal_template::render(date)` primes both `initial_content` and the `content` signal on new journal open; template body user-written via Learn-by-Doing (fenced YAML, `daily_note` tag list, 3 frontmatter properties, H2 prompt heading) — 5.1. Feedback saved: reflection fields stay in YAML frontmatter (user writes single run-on sentences, uses Obsidian property panel). Also corrected stale memory that flagged 6.6 (undo complete/skip) as deferred — verified already implemented in Phase 6. Commit pending. |
| Session 5: Phase 5.2-5.8 + Phase 7.1 | 2026-04-23 | Complete | Obsidian import/export end-to-end in one autonomous run (user near weekly token cap, skipping Learn-by-Doing + Playwright verification, to revisit after reset). Added `core::import` module with parser + walker + mapper + classifier (`serde_yml` dep). `TODO(human)` slot reserved in `split_frontmatter_and_body` — placeholder impl lets downstream work. Tauri commands: `preview_import`, `commit_import` (re-reads files from disk, not UI data), `export_obsidian` (filename sanitizer strips forbidden chars). Frontend: `pages::import_export::{ImportFlow, ExportFlow}` with Idle→Scanning→Previewing→Committing→Done state machines; per-row accept/skip checkbox + editable override for title/date + has-legacy-properties indicator. Settings page wires `ImportExportSection` above Danger Zone. 18 core import tests pass including the 7.1 round-trip test. 7.2 (seconds duration) deferred to Cycle 3 — would require breaking event schema across 16 touch points. Commit pending. |
| Session 6: Test-gap audit triage + close-out | 2026-04-26 | Complete | Verified each of the 12 proposals in `reviews/2026-04-24-test-gaps.md` against current codebase. After Logic/Perf/Bloat fix cycles + Security M1 landed their inline regression tests, **9 of 10 live proposals were superseded or trivial** — fix-cycle commits absorbed the regression-test demand inline (Critical #1 added 3, W4 added 3, W5 added 3, security M1 added 4). Only **#3** (parser case-sensitivity / whitespace-strict) survived because it tests an *absence* — that no `.trim()` or `.to_lowercase()` happens — which no fix commit would naturally produce. Locked in as `parse_is_case_sensitive_and_whitespace_strict` covering uppercase variants, mixed case, surrounding whitespace, and leading-whitespace-on-custom (defeats `starts_with("custom:")` guard). 18/18 routines tests pass; clippy clean. Test-gap doc annotated with disposition table. **Cycle 2 Session 6 fully closed; Cycle 2 complete.** |
| Session 5: tasks.md rewrite + reconciliation pivot (Cycle 3) | 2026-05-16 | Complete | **Cycle 3 `tasks.md` consolidated from ~55-task pre-replan plan to ~62 active + 8 done across 8 phases.** Eliminated the standalone `plans/2026-05-15-cycle-3-replan.md` doc — replan content now lives canonically in `tasks.md` per `feedback_no_separate_replan_doc.md`. **Largest single decision: W1 statement reconciliation and W6 cross-source merging collapsed into one unified workflow** after user observed both drew the same two-column candidate-matching UI; "statement vs ledger" and "auto-import vs auto-import" are the same matching engine fed by different input sources. New Phase 5 shape: 5.5 statement CSV parser (produces `TransactionRecorded` events with `Unmatched` placeholders) → 5.6 unified matching engine (signal-only candidate scoring; source-agnostic) → 5.7 unified review UI (emits `TransactionsMerged` + optional `TransactionCleared`; handles no-match path via category fill-in) → 5.8 balance check via `cleared`-sum. Phase 1 grew by 2 events: `TransactionsMerged` (1.14) for cross-source merging with lineage preservation; `TransactionCleared` (1.15) orthogonal to merging (a transaction can be cleared without merge OR merged without clearing — all 4 quadrants of the merged×cleared matrix are real in normal use). **Unmatched-account pattern adopted:** single top-level `Unmatched` clearing account (no `Assets:`/`Expenses:` prefix); steady-state invariant `Unmatched.balance == 0`; non-zero balance signals reconciliation pending or hidden fee (e.g. wire-transfer fee, FX spread) → user adds balancing posting to close. Saved as `project_unmatched_account_pattern.md`. **WS auto-import path:** added 2.9-spike SnapTrade evaluation (15-min user-run; quantified library risk from `gboudreau/ws-api-python` shows ~bi-monthly auth-regex hotfixes, community catches same-day); 2.9 default = subprocess `gboudreau/ws-api-python` if spike rejects SnapTrade; native Rust port deferred to Cycle 4. Saved as `project_ws_api_strategy.md`. 2.12 IMAP NGN handler split into 2.12a (decrypt + extract) and 2.12b (batch draft + manual FX prompt). **Phase 7 backlog deferred to Cycle 4** per "Cycle 3 already substantial, Cycle 4 is dedicated polish" scope decision: 7.5 (Daily Flow visualizer), 7.6 (FlushFailed → StatusReporter), 7.8 (configurable FORCE_GENERIC_DIRS), 7.9 (Arc<dyn EventStore>), 7.10 (seconds duration unit) all moved out. **Logbook + demo annotation pass on tasks.md** per PROJECT_PROCESS.md § Process Flow Session 4 obligation (which had been missed at session start — user surfaced the gap; saved as `feedback_planning_logbook_demo_annotations.md`). After early misfire flagging Phase 0.1 (Path B pivot) as logbook-worthy, corrected lens via `feedback_logbook_annotation_lands_at_feature_ship.md`: annotations cluster on user-observable feature-ship tasks, not infrastructure pivots. **Final candidate set: 10 logbook bundles + 2 demos.** Distribution by axis: inflow ×2 (capture flow, auto-import), outflow ×3 (transaction surface, account list, R1 dashboard), action ×3 (W4 budget, W3 recurring, reconciliation), backfill ×1 (import + A2 rewriter), inquiry ×1 (queries). Demos on reconciliation merge engine + R2 filter DSL (both pure-transform WASM-island candidates). **6 feedback memories saved:** `feedback_no_separate_replan_doc`, `feedback_smaller_proposal_chunks`, `feedback_planning_logbook_demo_annotations`, `feedback_logbook_annotation_lands_at_feature_ship`, `feedback_demos_optional_per_cycle`, `feedback_shared_ui_shape_is_a_tell`. **2 project memories saved:** `project_ws_api_strategy`, `project_unmatched_account_pattern`. **Curiosity captured (`.curiosities/cycle-3.md`):** how does the projection logic transform a heterogeneous event stream (`TransactionRecorded` + `TransactionsMerged` + `TransactionCleared` + `TransactionUpdated` + ...) into a valid hledger document? Sketched 3 options in conversation (regenerate-on-demand vs incremental vs hybrid); likely Learn-by-Doing material when Phase 1.6 starts — note that the regenerate path doubles as the Path B replay test. **Next session opens cold:** read `project.md` + `tasks.md`, then start Phase 1.1 `TransactionRecorded` event design (Learn-by-Doing on `Posting.amount` representation — rust_decimal vs string vs other is the cardinal data-type decision rippling through every downstream phase). |
| Session 5: Post-POC replan (Cycle 3) | 2026-05-15 | Complete | **Cycle 3 plan revised end-to-end after Phase 0 POCs invalidated several Session-4 assumptions.** Full revised plan at `plans/2026-05-15-cycle-3-replan.md`. **7 locked decisions:** (1) journal file = per-device only, regenerable from events (matches Notes/SurrealDB pattern); (2) **A2 finally resolved** — tag-only with Phase 6 import rewriter (strip `Expenses:Business:` segment, emit `type:business` posting tag, ~50 LOC); past mis-tagged "business on personal card" transactions stay untagged until user retags through normal use; (3) **R2** = expand 7.1 GUI builder (amount threshold, description-contains, OR/AND combinator) + Rust filter DSL on `ledger-utils` (~150-200 LOC); LLM-NL queries → Cycle 4; (4) **Three auto-imports added to Cycle 3** — WS unofficial API (covers 95% of user's transaction volume), Wise official API (multi-currency, low effort), Email IMAP poller dual-purposed for Standard Chartered NGN monthly statements (password-protected PDF decrypt → Gemini multimodal) AND online-purchase receipt auto-fetch; (5) **Document routing = hybrid policy** (IMAP sender-based + photo defaults to receipt + PDF requires user pick); **Veryfi DocumentExtractor implementation deferred to Cycle 4** since scope grew with auto-imports — Gemini multimodal is sole Cycle 3 extractor; extractor trait + routing scaffold stays so Veryfi swap-in is single-impl drop-in later; (6) **NGN FX = manual entry per Standard Chartered statement** at import review (stored as hledger `P` directive); ExchangeRate-API integration → Cycle 4; (7) **Scope level A — no further cuts**; Cycle 3 grows from ~50 → ~75 tasks. **Material findings driving the replan:** (a) **POC 0.1b A2 finding** confirmed — existing journal uses `Expenses:Business:*` hierarchy, not tags; (b) user surfaced **3 accounts not yet in the journal** (CIBC chequing, Wise CAD/USD/EUR multi-currency, Standard Chartered Nigeria NGN); (c) **manual sync killed the prior tracking attempt** — those 3 accounts never made the journal because keeping existing ones in sync was already painful; auto-import promoted from "post-MVP nice-to-have" to "without it, Cycle 3 ships and stops getting touched"; (d) **95% of transaction volume is in WealthSimple** — drove WS API to top auto-import priority despite unofficial-API risk; (e) Standard Chartered emails monthly password-protected NGN PDF statements — the IMAP+decrypt+Gemini pipeline is the only realistic path for NGN auto-import. **Cycle 4 grown commitments:** Veryfi `DocumentExtractor` impl, ExchangeRate-API for NGN auto-rates, LLM-translated NL queries for R2 evaluation. **Cycle 5+ filed:** inbox management feature (user's "far future dream"), Open Banking Canada evaluation. **Curiosity captured (Cycle 3):** "What is an Email IMAP poller, end to end?" — user explicitly didn't grasp it. **Plan ref:** `plans/2026-05-15-cycle-3-replan.md` (in-project, tracked) + `~/.claude/plans/dreamy-inventing-fog.md` (session artifact). **Next session opens cold:** rewrite `tasks.md` to reflect revised phase structure (~75 tasks across Phases 1-7), then Phase 1.1 `TransactionRecorded` event types (likely Learn-by-Doing on `Posting`'s `amount` representation choice — rust_decimal vs string vs other). |
| Session 5: POC 0.3 close (Cycle 3) | 2026-05-09 | Complete | **POC 0.3 closed via curl validation against the three commercial providers, with one architectural pivot mid-session.** Gemini 2.5 Flash multimodal PASS on FHSA brokerage statement (5 pages, 685KB PDF) — clean structured extraction in 6.7s, all fields correct (account_holder, account_type, institution, period dates, ending_balance=9880.00 CAD, transaction_count=13), ~2400 tokens incl. 882 thoughts tokens. Veryfi PASS plumbing (3-credential auth: CLIENT-ID + USERNAME + API-KEY) but **document-type-blind on the general `/documents/` endpoint** — defaulted to receipt parser, returned wrong total (23578.18 vs 9880.00), mis-categorized vendor as "Insurance", confused account holder with line item description. Phase 2 must route to Veryfi's specialty endpoints (`/bank_statements/`, `/checks/`, `/w2s/`, etc.) per document type. **Nanonets DROPPED** after multi-step debug: v4 workflow API auth + workflow-create + doc-upload all worked, but extraction returned `status: failure` with empty fields because instant-learning workflows require pre-configured field schemas, and PATCH/PUT to add fields didn't persist (PATCH returned 405, PUT returned 200-but-no-effect). User lost confidence in Nanonets given the integration cost vs payoff; preferred to keep PaddleOCR as escape hatch instead. **PaddleOCR moved from "Phase 2 promoted" back to backlog (Phase 7.11)** after I over-interpreted user's "drop Nanonets, prefer PaddleOCR" as "implement PaddleOCR now" — kicked off pip install, user clarified they meant strategic preference not immediate work. Aborted install + cleanup (~13 GB build artifact growth from session work, kept registry intact). **Final Phase 2 strategy: 2 active providers (Veryfi specialty + Gemini multimodal generalist), 1 OSS backlog escape hatch (PaddleOCR Phase 7.11).** Phase 2 task list now 9 items (was 10) — Mindee tasks dropped, Nanonets dropped, PaddleOCR demoted, routing simplified. Memory + tasks.md + MEMORY.md all updated to reflect final strategy. Veryfi+Gemini API keys are in this session transcript — user should treat as ephemeral testing keys and rotate if leaked. |
| Session 5: POC 0.2 + Phase 0 close (Cycle 3) | 2026-05-09 | Complete | **POC 0.2 PASS — full Android↔server blob round-trip works.** Server side: typed `BlobError` enum (thiserror) with `IntoResponse` impl, `routes/blobs.rs` exposes `PUT /blobs/{hash}` + `GET /blobs/{hash}` with 5 MB body limit override, hash-format validation (64 lowercase hex), atomic ULID-named-temp + rename, idempotency via `try_exists` (201 first / 200 cached), MIME inference via `infer` crate. Tauri side: `commands/blobs.rs::test_blob_roundtrip` generates timestamped payload, computes SHA-256 client-side, PUTs+GETs via existing `state.http: reqwest::Client` + `state.server_url: RwLock<String>`, returns `BlobRoundTripResult` struct. Frontend: `bridge::invoke_test_blob_roundtrip` + `BlobRoundTripTestSection` component on Settings page above Danger Zone. Test on Galaxy S9 (Android, arm64-v8a, switched from daily-driver) over Tailscale (`100.88.180.30`): PUT 201 + GET 200, latency 0ms for small payload. **Hash-verification Learn-by-Doing**: user contributed the inner write logic; first attempt shadowed `body` with the digest result causing the file to be 32 bytes of digest instead of original content + missing the comparison step entirely. Diagnosed both bugs constructively, user re-applied the fix correctly. Subsequent thiserror refactor swapped tuple `(StatusCode, String)` errors for typed `BlobError` enum across the route — `#[from] std::io::Error` derives the `From` impl that lets `?` propagate IO errors with no manual `.map_err`, `IntoResponse` impl maps variants to HTTP status codes in one place, ~12 lines of repeated `.map_err(|e| (...))?` collapsed. **Bonus fix: editor.rs:179 release-build compile error closed** (`feedback_known_bugs` item) — Cycle 1's `read_only` re-check fix only landed on debug branch; Tauri's `beforeBuildCommand: "npm run build"` triggers release frontend compile and surfaced it for the first real release attempt. Backlog 7.7 now done. **Build chain operational notes**: `npm run dev` produces debug-bundle assets at `frontend/target/dx/frontend/debug/web/public` (the path `frontendDist` references); `cargo tauri android build --debug --target aarch64` for ~10 min cold (~3-5 with cache). Stale APK from March 29 (557 MB) was on disk — always check timestamp before declaring success. Fresh APK is 1.14 GB debug. **Server boot needed `GEMINI_API_KEY=dummy` env var** (main.rs panics on missing) — must use `export GEMINI_API_KEY=dummy` or inline-prefix; bare `VAR=value` line sets shell var only, not env var. **OOM during cold compile** on Surface laptop (memory `Known Issues` warned about surrealdb-core memory pressure) — `cargo run -j 2` (or `-j 1`) limits parallelism to fit. **Adb authorization** got reset between phone reboots; user re-authorized via "Allow USB debugging" prompt. **Mindee → DocumentExtractor pivot finalized as 4-provider strategy** (Veryfi 100/mo true free + Gemini Flash multimodal + Nanonets $200 lifetime no-card + PaddleOCR sovereignty floor). **POC 0.3 reframed** from Tauri scaffolding to curl validation against all three commercial providers once user has API keys; trait + Tauri integration moved to Phase 2.4/2.5/2.6 where real receipts give it design substrate. **Phase 0 POCs now closed** — all four risk-validation goals met (Path B desktop + Android, blob round-trip, extractor strategy without runtime test). **Phase 1 (Core Foundation) is unblocked** for next session. Memory updates: `project_path_b_pta_engine.md`, `project_a2_business_hierarchy_finding.md`, `project_document_extractor_strategy.md` (replaced earlier `project_mindee_dropped_gemini_primary.md`), `feedback_no_bash_for_file_writes.md`, `project_known_bugs.md` (editor.rs:179 marked fixed). |
| Session 5: POC 0.1c + Mindee pivot (Cycle 3) | 2026-05-09 | Complete | **POC 0.1c PASS — device run validated.** Cross-compiled debug binary (`aarch64-linux-android` target, NDK r28 clang wrapper at API level 24) executed on Galaxy phone (`R3CR8082XLJ`, Android 15, `arm64-v8a`) via USB adb. **Byte-identical results desktop ↔ device**: 5,826 transactions / 11,020 postings / 92 accounts / 11 commodities / 1,365 `@`-price postings parsed; 80 balance accounts computed; `Assets:Checking:WealthSimple = 5076.10 CAD`; multi-commodity-per-account aggregation matches (`Assets:Crypto:Cash = 1027.25 CAD + -0.0101707000 BTC`). One observability finding: HashMap iteration order of multi-commodity components differs across runs (sums match but display order varies) — UI must sort explicitly by commodity name before rendering. `.cargo/config.toml` for the POC pins linker to `aarch64-linux-android24-clang`; chrono auto-pulled `android_system_properties` for tz lookup, no extra config needed. **Architecture pivot: Mindee dropped.** User attempting POC 0.3 Mindee signup discovered free tier retired May 2026 — only 14-day trial available. Decision: drop Mindee entirely, promote Gemini Flash multimodal to primary extractor for all input modalities (photos / PDFs / text / paystubs). Aligns with `feedback_prefer_integration_over_rewrite.md` + data-sovereignty principle (fewer external services). Saved to `project_mindee_dropped_gemini_primary.md`. **Cycle 3 plan revisions**: drop tasks 2.4 (Mindee Receipts) + 2.5 (Mindee Invoices); promote 2.6 (Gemini structured-output) to cover all modalities; simplify 2.9 (routing) to single-provider. POC 0.3 repurposed to validate Gemini-multimodal photo flow instead of Mindee. **Next:** POC 0.2 — `/blobs` server endpoints + Tauri command + device round-trip. |
| Session 5: POC 0.1b (Cycle 3) | 2026-05-09 | Complete | **Major architecture pivot — hledger CLI dropped in favor of pure-Rust embedded PTA engine.** Mini research session (POC 0.1.0) found that both `ledger` and `hledger` are available as Termux packages on Android (resolves the original "no Android binary" blocker), but more interestingly identified `ledger-parser` v6 + `ledger-utils` v0.6 as a viable pure-Rust path that eliminates the external-binary requirement entirely (mirrors SurrealDB-embedded pattern). Path B chosen — rationale: Tailscale already imposes per-device setup friction, budget feature shouldn't add another Termux dependency. POC 0.1b built standalone crate at `poc/ledger-parse/` and ran against `.reference/paisa/main.ledger` (user's actual production journal): **5,826 transactions / 11,020 postings / 11 commodities / 92 distinct accounts / 1,365 `@`-price postings parsed cleanly across 116 of 117 files**. Multi-commodity-per-account aggregation works (`Assets:Crypto:Cash` shows both `1027.25 CAD` AND `-0.0101707000 BTC`). Required content-prep wrapper: trim trailing whitespace per line + append `"\n\n"` (handles nom-incomplete edge case on real-world files). Includes are NOT auto-resolved by parser — owned recursive-glob resolver lives in POC main.rs. ledger-utils 0.6 still pinned to ledger-parser v6; v7 upgrade would need ~50 LOC of own balance walker (deferred). **One library limitation surfaced**: `~ Monthly` periodic transactions in `budget.ledger` not supported — irrelevant since W4 budget feature replaces them with custom UI/projection. **Architectural finding for Phase 1 to revisit**: existing journal encodes business via `Expenses:Business:*` account hierarchy, not tags. Cycle 3 Session 4 chose A2 tag-based — Phase 1 needs to decide hierarchy-only / tag-only-with-import-rewrite / hybrid (saved to `project_a2_business_hierarchy_finding.md`). **Disk cleanup:** removed 51G of build artifacts before POC build (omni-me/target was 40G of multi-target Android cumulative; frontend target 4.9G; archived poc-dioxus + rust-exercise targets; cargo registry/src; /tmp/hledger.tar.gz). 7.7G → 59G free. POC code committed pending. **Next:** POC 0.1c — cross-compile ledger-parser + ledger-utils to `aarch64-linux-android` and run inside Tauri Android shell against sample journal. |
| Session 4: Planning (Cycle 3) | 2026-05-02 | Complete | Budget feature scoped: 15 must-have / 2 deferred (R3 self-employment tracking, R4 tax form validation). **Major architecture pivot — Paisa dropped.** Cycle 1's "hledger CLI + Paisa via embedded WebView + Tailscale" decision overwritten because (a) Paisa's UI overwhelm was likely part of why prior tracking attempts failed, (b) original Paisa choice was for time-to-working-app, no longer the constraint, (c) mobile compatibility was unverified. New architecture: events → projections write to hledger journal file → hledger CLI for queries → custom Dioxus UI on both mobile and desktop. **Key decisions:** A1 multi-currency CAD-base + inline FX rates from receipts + Frankfurter daily fallback for gaps (USD ~couple/month, polish-tier); A2 tag-based business/personal separation (`type:business`/`personal` posting tags) chosen because no separate business bank account makes virtual-account splits misleading; A3 chart-of-accounts audit happens externally (separate Claude session reviewing user's existing journal pre-import); A4 investments naturally handled via hledger account types; LLM extraction routing — Mindee Receipts/Invoices primary with Gemini Flash structured-output fallback (with arithmetic verification: line-item-sum == total, gross-deductions == net); attachments via content-addressable server-side blob store (`/blobs/<sha256>` over HTTP) with on-device LRU cache + manual clear in Settings; mobile UI is a subset of desktop split along time-sensitive (capture, glance) vs session-work (reconciliation, budget setup, import) lines. **Cross-cycle commitments documented:** Cycle 4 = polish + logo + stable v1 + branch-gate for future features; daily-use baseline shifted from end-of-Cycle-2 to end-of-Cycle-4 (editor friction was the gap). **Process retrospective from Cycle 2:** review approach repeats document-then-triage with 4 docs + test-coverage upfront; subagent default model = `opus` (saved as `feedback_subagent_default_model.md`); no parallel worktrees; new feedback `feedback_prefer_integration_over_rewrite.md` saved (drove the hledger-as-live-engine decision); MEMORY.md compacted 26.0 KB → 8.9 KB; project-local `PROJECT_PROCESS.md` got a sync-banner pointing at the root copy. Phase plan: 0 risk-validation POCs (hledger-on-Android, blob-roundtrip, Mindee-from-mobile) → 1 core foundation → 2 server-side capture pipeline → 3 frontend capture flows → 4 transactions surface + R1 → 5 workflows desktop-heavy (W1/W3/W4) → 6 import (D1/D3, after user pre-cleanup) → 7 R2 + polish + stretch backlog. ~55 tasks. Sequential. Output: `tasks.md` reset for Cycle 3. |
| Session 6: Phase A + test-gap audit | 2026-04-24 | Complete | 4-perspective reviews generated (Security + Logical Consistency on Opus 4.7, Performance + Bloat/Complexity on Sonnet 4.6) at `reviews/2026-04-24-*.md`. **3 Critical findings**: (1) `core/src/routines.rs:99` `Frequency::Monthly` silently skips months without the anchor day (day-31 anchor never runs in Feb/Apr/Jun/Sep/Nov); (2) `tauri-app/frontend/src/journal_template.rs:22-37` ↔ `core/src/events/notes_projection.rs:280-305` parity break — template emits `tags:` followed by `- daily_note` YAML list item, parser terminates on first non-kv line, the three reflection keys never get scanned, `is_complete` stays `false`, auto-close never fires; (3) `core/src/sync/buffer.rs:159-163` `do_flush` drains queue before `append_batch`, silently drops events on DB error (`flush_loop` swallows Result via `let _`). Regression: Cycle 1 Session 6 marked `on_item_modified` DB-query consolidation as fixed, but only `on_group_modified` was updated — `on_item_modified` still runs 3 queries per edit. Test-gap proposal at `reviews/2026-04-24-test-gaps.md` (12 proposed tests: 8 regression guards, 2 tripwires, 2 edge-case fill-ins; 2 deliberately skipped with rationale). `reviews/` is git-ignored — no commits for Phase A; `project.md` is the only tracked record of the findings. **Next:** user triages findings per-review one-by-one in fresh context → Phase B test implementation → Phase C fix cycle. |
| Session 6: Performance review FULLY DISPOSITIONED + auto-save derail | 2026-04-26 | Complete | `reviews/2026-04-24-performance.md` annotated and closed. **Items 1, 2** absorbed into Logic W5 (one fix, two reviews — same code smell from different angles). **Item 3** (`commit_import` batched) fixed in `185548f` — refactored `commit_one` → `build_event_for_row` (pure-build), extracted `commit_import_inner` for testability via `&dyn EventStore` + `&ProjectionRunner` + `&str device_id`, Phase 1 collects `NewEvent`s + Phase 2 calls `append_batch` + `apply_events` once. Actual ratio ~3N → N+2 round-trips (review's "2N→2" was optimistic — projection-apply-per-event still iterates). New `commit_import_empty_rows_returns_zero_counts` test locks the empty-input contract. **Item 4** (`body_preview` single-pass) fixed in `c9af143` via `chars.by_ref().take(N).collect()` + `chars.next().is_some()` — single pass, zero alloc, cleaner than reviewer's `Vec<char>` suggestion. **Item 5** (`is_complete` byte-scan) — DISPOSITION INVERTED. User questioned the reviewer's "one save per day" framing, which surfaced a missed Cycle 2 product feature: auto-save was explicitly scoped (project.md:225 "debounced auto-save + auto-sync") but cross-track integration was never tasked — past-self left a comment in `tasks.md:72` about "future auto-save wiring" without filing it. **Auto-save built mid-review** (commits `9bb9e3c` journal + `a93d2a4` notes): 1s keystroke debounce via new shared `frontend/src/timer.rs` (`sleep_ms` extracted from `sync_status.rs` + `AUTOSAVE_DEBOUNCE_MS = 1000`), generation-counter for cancel-on-newer-keystroke, peek() over read() for `last_saved_content` to avoid self-trigger feedback loop, skip-if-stale `markClean` guard (compare snapshot vs current after resolve). Journal: option (i) — auto-save handles both create + update (1-per-date phantom risk acceptable). Notes: option (ii) — auto-save only updates; manual Save still required for first creation. Notes flow also fixed a pre-existing bug: manual Save in new-note mode now captures the created id into a `local_note_id` signal so a second Save click no longer creates a duplicate. With auto-save shipping, the reviewer's "no action needed if not keystroke-triggered" hedge became load-bearing → `is_complete` rewrite landed in `ab7e50c`: single-pass scan over `&str` slices, `[bool; 3]` tracker, short-circuits when all three found, zero `String` alloc. All 5 existing `is_complete_*` tests pass. **Item 6** (WASM `panic = "abort"` + `codegen-units = 1`) fixed in `d4e81ac`. **Tally:** 4 inline fixes + 2 absorbed + 1 disposition-flip + auto-save feature gap closed. Frontend wasm32 + core both pass `cargo check` / `cargo clippy` clean; 163 core tests + 22 import tests + 5 is_complete tests all green. **Notable finding outside perf scope:** `tauri-app/frontend/src/components/editor.rs:179` (production `cfg(not(debug_assertions))` branch) calls `editor_options(journal_mode)` with one argument but the function signature requires two — release builds fail to compile. The Cycle 1 read_only re-check fix only landed on the debug branch. Flagged for follow-up; not in perf scope. **Untested in browser:** auto-save logic verified via `cargo check` + clippy + walkthrough but no `dx serve` run this session — user accepted the risk and will exercise during Cycle 3 use. **Next:** Bloat/complexity review (`reviews/2026-04-24-bloat-complexity.md`) — last Phase-A doc remaining for Session 6. |
| Session 6: Logic Cycle 1 re-check (read_only prop) | 2026-04-26 | Complete | The Cycle 1 review's "PARTIAL FIX" note about `tauri-app/frontend/src/components/editor.rs::read_only` was understated — verification revealed it was still a **two-layer no-op**: Rust's `editor_options` only embedded `journalMode`, and `editor.js::createEditor` only read `options.journalMode`. CodeMirror itself was accepting all input. User initially recalled testing had blocked typing, then re-tested live and discovered the `opacity-60` wrapper was making the cursor invisible — typing actually worked, just looked like it didn't. **Both layers wired:** `editor_options(journal_mode, read_only)` now embeds `readOnly` in the JS options object; `editor.js` reads `options.readOnly` and applies `EditorView.editable.of(false)` (chosen over `readOnly.of(true)` because `editable` removes the input cursor entirely — clearer signal). Bundle rebuilt via `npm run build:editor`. Frontend clippy clean. Three independent layers now prevent edits to closed journals: CodeMirror non-interactive, visual opacity-60, disabled Save button. **Logic review now fully dispositioned** — 3 Critical / 4 Warning / 3 Info / 4 Cycle 1 re-checks all annotated. **Next:** Performance review (`reviews/2026-04-24-performance.md`) in fresh context. |
| Session 6: Logic Info #9 | 2026-04-26 | Complete | `core/src/events/notes_projection.rs::is_complete` doesn't guard against duplicate frontmatter keys — `.any()` picks any non-empty occurrence, which differs from Python yaml / Obsidian (last-wins). **DOCUMENTED — no behavior change.** Docstring expanded with a "Duplicate-key semantics" paragraph: explains the any-non-empty-wins choice, notes divergence from Obsidian's likely last-wins behavior, and rationale (duplicates essentially never occur via normal edits or property-panel UIs; forgiving rule favors the realistic mistake mode "user typed it once, accidentally added a blank line later"). Future contributor can reconsider when an Obsidian-round-trip case exposes the difference. **Next:** Logic Info #10 (`auto_close` should `tracing::info!` when `closed > 1`). |
| Session 6: Logic Info #8 | 2026-04-26 | Complete | `tauri-app/src-tauri/src/commands/import.rs::commit_one` re-reads files fresh from disk at commit time; doesn't reuse preview's parsed data. **DOCUMENTED — no behavior change.** Re-read is a deliberate security choice from Cycle 2 Phase C H1 fix (`40faf00`): backend-authoritative content prevents a malicious renderer from injecting text into a journal entry. Staleness trade-off (file edited between Preview and Commit lands with current disk content, not what the user saw) is acceptable because preview→commit window is seconds, concurrent vault editing is near-zero frequency, and recovery is "re-import". Code-comment added explaining the rationale so a future contributor doesn't "fix" the re-read without understanding why it exists. No mtime tracking — proportional to the failure mode's severity. **Next:** Logic Info #9 (`is_complete` duplicate frontmatter keys). |
| Session 6: Logic Warning #7 | 2026-04-26 | Complete | `tauri-app/src-tauri/src/auto_close_scheduler.rs` DST fallback was wrong: `from_local_datetime().single()` returned `None` for both `Ambiguous` (DST fall-back) and `None` (DST spring-forward gap), and the fallback `tz.from_utc_datetime(&midnight)` misinterpreted local midnight as UTC, producing a target several offset-hours off. **Option C — skip-day on gap, no panic, no tracing.** Switched `.single()` → `.earliest()` (handles `Ambiguous` correctly by picking the earlier of two valid datetimes for fall-back overlap). For `None` (true gap, essentially impossible in modern zones since transitions happen at 02:00 not 00:00), function now returns `Duration::from_secs(24 * 60 * 60)` — auto-close skips today and re-evaluates tomorrow. User reasoning: closing journals is a fully reversible single-button action, so even a tracing log felt heavier than warranted. Misleading inline comment ("Take the latest candidate (usually +1h)" — `from_utc_datetime` does NOT do that) removed and replaced with one explaining the `.earliest()` choice + the gap-case fallback. 3/3 existing scheduler tests pass; clippy clean. No new tests — the Ambiguous case requires finding a zone with DST fall-back exactly at midnight (nearly extinct in chrono-tz). **Next:** Logic Info #8-10 (the 3 Info items), then full review doc completion. |
| Session 6: Logic Warning #6 | 2026-04-26 | Complete | `core/src/auto_close.rs:37-60` scan→emit loop is non-atomic — user manually closing a candidate journal between `list_completable_unclosed_journals` snapshot and `append` lands two `JournalEntryClosed` events with conflicting `trigger` values. **ACCEPTED — documented in code comment.** User rationale: journal close→reopen→close is a designed lifecycle, so the projection cannot reject duplicate close events outright; race likelihood (midnight scheduler + active user click in same millisecond) is near zero; observable state (`closed = true`) is idempotent either way. Code-comment added at `auto_close.rs` documents the TOCTOU and the rationale so a future contributor doesn't "fix" it without understanding the design constraint. No tests changed. **Next:** Logic Warning #7 (DST fallback misuses `from_utc_datetime`). |
| Session 6: Logic Warning #5 + on_item_modified regression | 2026-04-26 | Complete | Triage surfaced architectural question first — surveyed all 26 `db.query` sites, found only 1 (`store.rs::append_batch`) used BEGIN/COMMIT; only 2 handlers needed it (`on_group_reordered`, `on_item_modified`). Adopted **SurrealDB transaction policy** (`project_db_transaction_policy.md`): single-statement default; BEGIN/COMMIT for multi-statement coupled writes; collapse multiple SETs on same row into one UPDATE. **W5 fix (`on_group_reordered`):** dedup on `group_id` via HashMap (last-wins) + N updates wrapped in single BEGIN/COMMIT query with indexed parameter names (mirrors `append_batch`'s pattern). **Cycle 1 regression fix (`on_item_modified`):** collapsed from 3 separate conditional UPDATEs into 1 statement with conditional SETs — single-statement is atomic by definition. Added `use std::collections::HashMap` to module. 3 new tests: dedup last-wins, all-three-fields-in-one-update, no-recognized-changes-noop (defensive against payload schema drift). 163/163 core tests pass; clippy clean. Existing tests (`group_reordered_updates_multiple_groups`, `item_modified_partial_changes`) continue to pass — refactor preserved observable behavior. **Next:** Logic Warning #6 (`auto_close` TOCTOU). |
| Session 6: Logic Warning #4 | 2026-04-26 | Complete | `core/src/routines.rs` `Frequency::FromStr` previously accepted any positive `u32` (mismatch with UI's `[2, 365]` clamp). User reframed the triage: Routines section is for **habit formation**, not calendar tasks — anything firing less often than monthly belongs in a future scheduled-tasks feature. **Bound TIGHTENED, not just defensively bounded.** New `CUSTOM_FREQUENCY_MIN = 2` / `CUSTOM_FREQUENCY_MAX = 31` constants; parser rejects outside `[2, 31]`. UI clamp at `pages/routines.rs:582-588` updated to match (`max="31"`, `clamp(2, 31)`). Doc on `Frequency::Custom` variant references the bound constants. 3 new tests (`parse_custom_below_min_is_invalid`, `parse_custom_above_max_is_invalid` covering 32/60/365/4B, `parse_custom_at_bounds_is_valid`); existing `display_roundtrip_through_parse` updated to use bound endpoints. 17/17 routine tests pass; clippy clean on core + frontend. Durable product rationale: `project_routine_definition.md` in user memory — captures *why* 31 (habits = high-frequency reinforcement; > monthly = task) so future contributors don't liberalize the bound without re-arguing the case. Cascading insight: this reframes the Cycle 3 consistency-visualizer scoping item (filed during Critical #1) — short windows are *correct* for habits, visualizer only needs Monthly/Custom-31 breathing room. **Next:** Logic Warning #5 (`on_group_reordered` doesn't dedup `group_id`). |
| Session 6: Logic Critical #3 | 2026-04-26 | Complete | `core/src/sync/buffer.rs` — behavior already fixed in M2 (`c32c990`) but failure path had no test. Added `Arc<dyn EventStore + Send + Sync>` seam to `Inner.store` (one-line type change; `EventStore` trait was already `#[async_trait]`-decorated and used dyn-style in `llm/pipeline.rs:65`). Production caller at `lib.rs:162` updated to `Arc::new(event_store.clone())`. New `ScriptedStore` mock in test module supports pre-canned response sequences + optional gate for concurrent-interleaving tests; `NewEvent` gained `PartialEq` derive for sequence equality. **5 new tests** all passing: `flush_failure_keeps_events_in_queue_in_order`, `flush_failure_then_success_persists_in_order`, `flush_failure_broadcasts_flush_failed_with_correct_count`, `concurrent_append_during_failed_flush_preserves_order`, `repeated_failures_recover_on_eventual_success`. 157/157 core tests, clippy clean, Tauri backend builds. **Subagent experiment:** plan written by Opus 4.7, implementation handed off to Sonnet 4.6. Sonnet produced correct seam + mock + 2 of 5 tests. 3 tests had runtime-semantics bugs (gate released only once for two `append_batch` calls causing 8-min hang; broadcast `recv()` returns oldest-first not latest, so "subscribe-then-drive-failure-then-success" pattern surfaced FlushFailed when assertion expected Flushed). Patched here on Opus — ~10 LOC of edits to fix three tests. Lessons in `feedback_subagent_test_writing.md`. **Out of scope for this PR (kept narrow):** `auto_close_scheduler.rs` and `AppState.event_store` remain concrete (separate decisions); `BufferEvent::FlushFailed` consumer wiring deferred to Cycle 3 StatusReporter; re-queue cap overshoot edge case (Tier 3 finding) deferred. **Next:** Logic Warnings #4-7. |
| Session 6: Logic Critical #2 | 2026-04-25 | Complete | `tauri-app/frontend/src/journal_template.rs:22-37` ↔ `core/src/events/notes_projection.rs:280-305` parity break. **Fixed via Option A (template change)** — `tags:\n    - daily_note` block list collapsed to inline `tags: [daily_note]`. User picked Option A deliberately as a quick fix because the editor's properties UI is on the rework backlog (avoid investing in parser permissiveness now if the whole properties surface may be reworked anyway). Inline-comment in `render()` documents the constraint so a future template addition with a YAML block list doesn't silently re-break `is_complete`. Cross-crate regression test in `notes_projection::tests::is_complete_recognizes_journal_template_when_filled` — hardcoded fixture mirroring a filled-in template render asserts `is_complete == true`. 10/10 notes_projection tests pass; 2/2 frontend journal_template host tests pass; clippy clean. Test-gaps proposal #10 (tripwire) superseded. **Next:** Logic Critical #3 — `SyncBuffer::do_flush` drains-before-append (Cycle 2 Session 6 Phase C M2 may already cover this — need to check). |
| Session 6: Logic Critical #1 | 2026-04-25 | Complete | `core/src/routines.rs:99` `Frequency::Monthly` clamp fix landed. New `last_day_of_month(date) -> u32` helper uses chrono's "biggest valid day-of-month wins" pattern (4-element table `[31, 30, 29, 28]`, `find` short-circuits on first valid) — written by user via Learn-by-Doing; no leap-year arithmetic in our code, fully delegated to chrono. Line 99 changed to `today.day() == anchor.day().min(last_day_of_month(today))` — end-of-month semantics: Jan-31 anchor fires on Feb-28/29, Apr-30, Jun-30, Sep-30, Nov-30. 3 new regression tests: short-month clamp across all 5 short months, leap-year Feb-29 handling (2024 leap, 2026 non-leap, 2100 century non-leap, 2000 century leap), and exhaustive `last_day_of_month` per month. 14/14 routine tests pass; clippy clean. Test-gap proposal #9 (tripwire for buggy behavior) was superseded — written directly as the correct-behavior assertion. **New Cycle 3 backlog item filed:** Daily Flow consistency visualizer hard-codes a 7-day window, but routines can now be Monthly / Biweekly / Custom — for any routine whose period exceeds 7 days the visualizer cannot render meaningful consistency. Frontend↔backend assumption drift introduced when Cycle 2 expanded `Frequency`. Disposition annotated in `reviews/2026-04-24-logical-inconsistencies.md`. **Next:** Logic Critical #2 (journal template ↔ frontmatter parser parity break). |
| Session 6: Phase C — Security fix cycle | 2026-04-25 | Complete | All 5 security review items addressed in 5 discrete commits (one per item, walk-through-and-discuss-each style per user preference). **H1** `40faf00` — `commit_import` now validates row paths against an `AppState`-stored canonical scanned root (Option B: backend-authoritative); also added `FORCE_GENERIC_DIRS` carve-out (`Work/`) in `core::import::classify_path` via Learn-by-Doing (user picked `Component::Normal` + `OsStr` exact-segment match), with vault-root strip in `build_preview_row` so vaults stored under `Work/` aren't false-positive-classified. **H2** `bfef0eb` — migrated `serde_yml` (RUSTSEC-2025-0068, AI-generated code) to `serde-saphyr 0.0.24`; deleted `yaml_to_json` (37 lines) since saphyr deserializes straight into `serde_json::Value`; added `MAX_FRONTMATTER_BYTES = 65_536` pre-parse cap and saphyr `Budget` (max_anchors:200, max_depth:100, max_total_scalar_bytes:65536, max_events:50000) using the `options!` / `budget!` macros for 1.0-stability; 3 new defense tests (oversized, billion-laughs, deep-nest). **M1** `afd3940` — `sanitize_filename` adds Windows reserved-name guard (CON/PRN/AUX/NUL/COM1-9/LPT1-9, case-insensitive, exact-segment); pure `assign_unique_filenames` helper detects collisions across the export set and appends 8-char FNV-1a hex suffix (rolled inline so it doesn't depend on `DefaultHasher`'s implementation-defined contract); `export_obsidian` pre-resolves filenames before write loop. **M2** `c32c990` — `SyncBuffer` overhaul (security review's Medium overlapping logic review's Critical): `DEFAULT_MAX_QUEUE_LEN = 10_000` cap returns `BufferError::Overflow` on full queue; `do_flush` clones events before `append_batch` then re-queues at front (rev-iter + push_front) on failure to preserve order; replaced `FlushResult` struct with `BufferEvent` sum-type enum (Flushed / FlushFailed / Overflow) on the broadcast channel (Option 6 — extensible vs separate channels); `pusher::forward_flush` now matches on `Flushed` only so it doesn't trigger pushes on local-failure or overflow events. **M3** `d898f90` — `wipe_all_data` Tauri command now requires a `confirmation: String` parameter validated against `WIPE_CONFIRM_PHRASE` on the backend; closes the DevTools `invoke()` bypass (the frontend phrase check was never the only barrier anymore); pure `check_wipe_confirmation` helper for unit tests; phrase constant duplicated in `frontend/src/pages/settings.rs` and `commands/routines.rs` (must-match comment at both ends). **Side decisions:** Cycle 3 backlog gained "consider configurable `FORCE_GENERIC_DIRS`" + "wire `BufferEvent` into `StatusReporter` for user-visible 'stuck buffer' indicator" (events broadcast but no consumer yet). Cycle 1-deferred items (CSP=null, CORS permissive, Gemini key-in-URL) remain deferred — risk profile unchanged. Test totals: 43/43 core import, 21/21 commands::import, 17/17 sync, 4/4 wipe; full workspace passes; clippy clean. **Next:** Logical consistency review fix cycle (Critical findings 1 + 2 are highest priority). |

---

## Session 1: Project Initiation

**Date:** 2026-03-02

### Questions & Answers

**1. What is the goal of the project?**
Two layers:

- **Deliverable:** A personal all-in-one app for budgeting, journaling, goals, tasks, decisions, and life management.
- **Underlying motivation:** Extreme curiosity leads to information overload and anxiety about losing track. LLMs now make it possible to not just collect personal data, but meaningfully process it. The app serves two goals: (1) accelerate personal growth by having an intelligent feedback loop on your own life, and (2) create an objective record of that growth to replace vague feelings with verifiable history. A third key driver is **data sovereignty** — every need this app addresses is served by existing products that monetize user data; building it yourself keeps that data under personal control and positions well for the coming age of AI.

**2. Who will be the primary user/consumer (who benefits)?**
Solely the user themselves. No multi-user or sharing features needed.

**3. What does success look like?**
Daily use as a first choice, not a fallback. The test is: would you reach for it naturally? Concrete example — tax season: if finances had been tracked for the past year, you could file with confidence knowing exact spend, reliable data, archived invoices/receipts/images. Two dimensions of success:
- **Usage:** It's built, used, and *wanted* — the preferred tool for everything it's intended to cover
- **Adaptability:** It can grow (and shrink) with evolving needs over time — not a static product
Core requirement underlying both: the data being collected is actually being processed in a way that serves the original purpose (growth acceleration + verifiable record).

**4. What are the time/urgency expectations?**
- **MVP:** End of March 2026 (~4 weeks)
- **Full-featured prototype:** Before September 2026 (~7 months)
- **Final product:** Doesn't exist — this is an evergreen personal tool that will always be iterated on; no planned end state.

**5. Why does this project matter to you right now? What's driving this need?**
The idea has existed for a while but now the pain is acute and the excuses are gone:
- **Active pain:** Tax season is forcing a painful document hunt that better organization would have prevented
- **Ongoing loss:** Consuming interesting books and ideas daily but losing them to distraction before implementing anything
- **Enablers removed:** A website is already set up to track development, and LLM coding agents make building this solo realistic in a way it wasn't before
- The honest answer: should have started a year ago. Starting now because there's no good reason not to anymore.

---

## Session 2: Research Session

**Date:** 2026-03-05

**Summary:** Dedicated research session before architecture decisions. Reviewed all 13 features, all service options, and finalized core technical choices. Output captured in `research.md`.

**Key Decisions Made:**
- **Deployment:** Tauri v2 (Android APK, sideloaded) + PWA fallback. Dioxus for UI, CodeMirror 6 for editor.
- **VPS:** DigitalOcean 2GB Droplet (~$12 USD/month, $200 credit). Planned Hetzner migration before credit expires.
- **CI/CD:** GitHub Actions → DigitalOcean (high priority, same workflow as personal website)
- **LLM:** Gemini Flash free API. Trait-based abstraction to add Claude API later.
- **Database:** SurrealDB (multi-model, Rust-native, schema-flexible, graph support)
- **Sync:** Event Sourcing (append-only immutable events, no conflict resolution needed, schema-flexible)
- **Editor:** CodeMirror 6 (same editor as Obsidian, excellent Android touch support, MIT)
- **Data model:** Single note type, LLM derives all structure. Tool calling + structured output for consistency.
- **LLM pipeline architecture:** Structured output mode + tool/function calling + deterministic pre-processing + prompt versioning + confidence review gates

**Feature decisions:** All 13 features reviewed. See `research.md` Section 5 for full service map.

**MVP Scope (end of March 2026):**
- Week 1: Infrastructure (Tauri + SurrealDB + Event Store + Sync + CI/CD + LLM pipeline + CodeMirror)
- Weeks 2-3: Journal/Notes feature + Routine Manager
- Validates: APK sideloading, multi-device sync, LLM pipeline

**Reference:** See `research.md` for complete research documentation

---

## Session 3: Architecture

**Date:** 2026-03-07

**Summary:** Reviewed all decisions from research session, confirmed every section unchanged. Formalized into `architecture.md` — concise, model-parseable reference document with tables and clean headers. Added security section covering VPS hardening, data-in-transit encryption, LLM data exposure (accepted risk for MVP), and backup strategy. Conducted risk review identifying 5 risks with mitigations (Tauri sideloading and SurrealDB maturity as highest priority POCs).

**Key Output:** `architecture.md` — the authoritative technical reference for implementation.

**Process Note:** Research session (between Session 1 and Session 2) proved very valuable. Consider updating `PROJECT_PROCESS.md` to include dedicated research step.

---

## Cycle 1: MVP — Infrastructure + Journal + Routines

### Session 3: Planning

**Date:** 2026-03-08

**Objective:** Build the foundational infrastructure (event sourcing, sync, LLM pipeline, Tauri/Dioxus/CodeMirror stack) and two features (Journal/Notes, Routine Manager) to validate the full vertical from mobile input to LLM derivation to multi-device sync.

**Scope:**
- Included: Risk POCs, Rust workspace, VPS provisioning, CI/CD, event store, sync protocol, LLM pipeline (Gemini), Dioxus UI shell, CodeMirror editor, Journal/Notes feature, Routine Manager feature, Android APK
- Excluded: PWA fallback (deferred to Cycle 2), all Cycle 2+ features (tasks, goals, calendar, budget, locations, meals, people, knowledge, archive)

**Key Decisions:**
- PWA fallback deferred to Cycle 2 (tight timeline, single user controls all devices)
- All Claude Code implementation (no aipack) — also validates subscription capacity
- Maximum parallelization via worktree subagents during Session 4
- VPS not yet provisioned — included as Phase 1 task

**High-Level Phases:**
0. Risk Validation POCs (Tauri Android, SurrealDB embedded, Dioxus-in-Tauri, CodeMirror-in-Tauri)
1. Project Skeleton + Infrastructure (workspace, VPS, CI/CD, DB layer, server)
2. Event Store + Sync (event model, projections, sync protocol, integration test)
3. LLM Pipeline (Gemini client, tool calling, prompt versioning, note processing)
4. UI Shell + CodeMirror (Dioxus app shell, editor IPC bridge, wrapper component)
5. Journal/Notes (create, edit, list, LLM trigger, search)
6. Routine Manager (group CRUD, items, daily checklist, editing, history)
7. Integration + Polish (sync-on-open, APK final, settings, error handling)

**Task Count:** 38 tasks across 7 phases. 3 parallel tracks during core infrastructure.

**Reference:** See `tasks.md` for detailed atomic task breakdown with dependencies and parallel execution map

---

### Session 4: Implementation

**Date Started:** 2026-03-08
**Date Completed:** [Date]

**Phase 0: Risk Validation POCs — COMPLETE (all passed)**

| POC | Desktop | Android | Key Findings |
|-----|---------|---------|--------------|
| P2: SurrealDB Embedded | PASSED | n/a | SurrealDB v3 uses `SurrealValue` derive (not serde), `select()` errors on non-existent tables |
| P1: Tauri v2 | PASSED | PASSED | Needs `withGlobalTauri: true` for IPC, `mobile_entry_point` macro for Android |
| P3: Dioxus-in-Tauri | PASSED | PASSED | Dioxus WASM renders in WebView, IPC round-trip works via `window.__TAURI__` |
| P4: CodeMirror-in-Tauri | PASSED | PASSED | esbuild bundle (590KB), bidirectional JS↔WASM interop works |

**Tooling Installed:**
- Tauri CLI v2.10.1, SurrealDB CLI, Android SDK (platform 35+36, NDK r28), Java 17
- Rust targets: wasm32-unknown-unknown, aarch64-linux-android, armv7-linux-androideabi, x86_64-linux-android, i686-linux-android
- Environment vars in `~/.bashrc`: JAVA_HOME, ANDROID_HOME, NDK_HOME

**Notes:**
- No fallbacks needed — all technology bets validated
- `wasm-opt` crashes (DWARF version mismatch) but is non-fatal
- Android APK frontend assets require manual copy to `gen/android/app/src/main/assets/` — needs build script automation
- dx 0.7.2 warns about dioxus 0.7.3 version mismatch but builds fine

---

### Session 6: Code Review

**Date:** 2026-04-11 onwards
**Status:** Complete (see `reviews/` for per-perspective findings)

**Phase A — Multi-Perspective Review:**
Four parallel review documents produced in `reviews/2026-04-11-*.md`:
- Security, logical consistency, performance, bloat/complexity
- Findings bucketed Critical / High / Medium with file:line references

**Phase B — Test Coverage Audit:**
- Identified untested branches (sync client orchestration, idempotency, event payload schemas, UserDate format)
- Added 9 locked-in tests across core + server integration + frontend
- Extracted shared test fixtures into `server/tests/common/mod.rs`

**Phase C — Fix Cycle:**
- Review fixes landed in commit `5c8af12` (bloat + logical inconsistency fixes, timezone system)
- Build pipeline + UI dev workflow fixes in commit `37172b6`

**Feedback for Next Cycle:**
- SurrealKV commit queue overflow after ~24h — watch bug, not blocking
- wasm-opt DWARF crash — only affects `--release` builds
- Sync endpoints unauthenticated — acceptable behind Tailscale for now
- UI_WORKFLOW.md process is tentative; validate during Cycle 2 first UI work

**Milestone Commit:** `80581ca` (session end 2026-04-18)

---

## Cycle 2: Daily-Usable (Obsidian-Replacement MVP)

### Session 4: Planning

**Date:** 2026-04-19

**Feedback Incorporated from Cycle 1:**
- Editor fixes (line wrapping, no line numbers) landed in Cycle 1 polish — good base for Cycle 2 auto-wrap work
- `UI_WORKFLOW.md` tentative process gets first real test in Phase 1 (editor revamp) and Phase 3 (nav shell)
- Cycle 1 backlog rolled into Cycle 2 scope: undo complete/skip, duration unit, edit routine items, remove routine items/groups → Tier 2
- Cycle 1 backlog rolled to stretch: search clear button → Phase 7
- Sync endpoints still unauthed, acceptable while behind Tailscale — no auth work this cycle

**Embedded Research:**
- Obsidian Sync settings researched via WebSearch — Obsidian defaults: ~1s debounce, passive retry, ~60s backoff cap. Cycle 2 adopts similar defaults (1s local / 2s push / 60s cap).
- Obsidian screenshots walkthrough (14 images across laptop + mobile) — enumerated actual daily-use features: auto-wrap, checkboxes, YAML frontmatter, calendar, daily notes template. Wikilinks / graph view / dataview / canvas explicitly excluded from scope.

**Objective:** Get omni-me to the point where the user would reach for it before Obsidian for daily journaling. Specifically: editor ergonomics match, auto-save/auto-sync is invisible, journal-per-day + free generic notes both work, existing Obsidian vault imports cleanly.

**Scope:**
- **Tier 1 (MVP to switch):** Editor revamp (auto-wrap, checkbox, timestamps), debounced auto-save + auto-sync with exponential backoff, note naming domain (journal vs. generic), Obsidian import/export with `legacy_properties` capture
- **Tier 2 (full):** Routine UX — undo, edit, remove, frequency expansion, duration unit, data wipe; drop time-of-day labels, replace with user-ordered flat list
- **Excluded:** Budget feature (Tier 3) deferred to Cycle 3. Sync auth deferred (still behind Tailscale). PWA fallback still deferred.

**Key Decisions:**
- **Event schema — breaking changes allowed:** pre-daily-use means old local events are test data, not production history. No `Option<>` fallback shims or kind-inference tricks. Clean event vocabulary wins over preserving ~50 throwaway records.
- **Note domain split:** Separate event types — `JournalEntryCreated/Updated/Closed/Reopened` (date-keyed, one-per-day, templated) vs. `GenericNoteCreated/Updated/Renamed` (id-keyed, user-titled, free-form). Journal entries get a `journal_id` ULID for LLM/sync aggregate references in addition to the date key.
- **Day-close triggers:** Soft-lock via `JournalEntryClosed` / `JournalEntryReopened`. Auto-trigger fires only when BOTH end-of-day has passed AND all 3 manual properties (`homework_for_life`, `grateful_for`, `learnt_today`) are filled — handles the "fill next morning" case. Manual close button always available.
- **Generic note nav:** Flat recency list + search. Tags rejected as primary nav — LLM-derived tags are too unreliable.
- **Nav shell:** Bottom tab bar (mobile) + sidebar (desktop), feature-level. Second-level tabs within feature for content (Today/Calendar for Journal, Recent/Search for Notes).
- **Import strategy — Option D "Pragmatic capture":** Known frontmatter → typed fields; unknown → `legacy_properties` JSON blob on created-note events. Handles 2022-2023 schema drift without blocking import.
- **Sync defaults (user deferred to recommendation):** 1s local debounce, 2s push debounce, 1s→60s exponential backoff with jitter, OS network events as hints (not authority). 4-state status indicator: synced/pending/retrying/offline. Editing never blocks on sync.
- **Frequency canonical format:** `"daily"` | `"weekly"` | `"biweekly"` | `"monthly"` | `"custom:N"` with shared parser.
- **Time-of-day labels removed:** morning/afternoon/evening dropped entirely. `time_of_day` field removed from `RoutineGroupCreated`; `order` field added; new `RoutineGroupReordered` event. Daily Flow shows a user-ordered flat list with drag-to-reorder.
- **Cycle 1 event data:** wiped by default at Session 5 start. Migration script available as fallback if retention wanted.

**High-Level Phases:**
0. Core foundation (Track A serial) — events + projections + commands
1. Editor revamp (Track B)
2. Sync debounce + retry (Track D)
3. Nav shell revamp (Track C)
4. Calendar + day-close UI (Track C)
5. Templates + Obsidian import/export (Tracks A+C)
6. Tier 2 routines (Tracks A+C)
7. Stretch (optional)

**Parallelization:**
Phase 0 serial (Track A foundation) → Phases 1/2/3 parallel (3 worktree agents: Track B editor, Track C nav shell, Track D sync) → Phases 4/5/6 parallel (3 agents across Tracks A+C) → Phase 7 stretch.

**Task Count:** 48 tasks across 7 phases + 3 stretch items.

**Reference:** See `tasks.md` for detailed atomic task breakdown with dependencies and parallel execution map.

---

### Session 5: Implementation

**Date Started:** [Date]
**Date Completed:** [Date]

**Planned Work:**
[Brief summary]

**Actual Work:**
[Brief summary with deviations]

**Key Commits:**
- `[commit hash]`: [description]

---

### Session 6: Code Review

**Date Started:** 2026-04-24
**Status:** Phase A documents all produced. Document-then-triage in progress: Logical Consistency (2026-04-26), Performance (2026-04-26), and Bloat/Complexity (2026-04-26) fully dispositioned with fixes landed inline. Test-gap audit document still requires user review before any test code lands.

**Phase A — Multi-Perspective Review:**
Review documents in `reviews/2026-04-24-*.md` (directory is git-ignored; files live locally only). Scope: Cycle 2 git range `1031e23..22395f8`, ~35 commits.

Model split: Opus 4.7 for Security + Logical Consistency (cross-file invariant reasoning); Sonnet 4.6 for Performance + Bloat/Complexity (pattern-match passes).

- **Security:** 2 High (path traversal via frontend-controlled `AcceptedRow.path` in `commit_import`; unbounded `serde_yml` parse exposed to billion-laughs / deep-nesting attacks), 3 Medium (Windows reserved-name sanitizer gap, unbounded `SyncBuffer::append` queue with silent error drop, hardcoded Danger Zone phrase), 5 Positive findings, 3 Cycle-1 deferrals still deferred (CSP, CORS, Gemini query-string logging).
- **Logical consistency:** **3 Critical** — see Session Log row for details. Plus 4 Warnings (parser-ceiling desync, reorder no-dedup non-transactional, auto-close scan→emit race, DST spring-forward fallback wrong), 3 Info, 4 Cycle-1 re-checks (one partial-fix flag on `read_only` prop wiring).
- **Performance:** 5 Runtime findings (biggest: `on_item_modified` regression — Cycle 1 fix only covered `on_group_modified`; `commit_import` sequential 2N DB queries; `on_group_reordered` N queries per reorder), 1 WASM (missing `panic=abort`/`codegen-units=1` in frontend profile), 4 Verified-no-issues (retry jitter/cap, retry u32 saturating counter, `build_month_cells` O(35), calendar cache). Top-5 bang-for-buck table included.
- **Bloat/complexity:** 3 High (triplicated `append_and_apply` helper across `notes.rs`/`routines.rs`/`import.rs`; `preview_import` backend counts duplicated client-side; `body_preview` double char-iteration), 4 Medium (shadow `last_sync_timestamp` pair, `ImportPhase`/`ExportPhase` minor duplication, `split_frontmatter_and_body` asymmetric return, `COMPLETE_PROPERTIES` ↔ `KNOWN_KEYS` sync hazard), 2 Low, 5 Cycle-1 re-checks (bridge macro approaching 19 functions; `append_and_apply` escalated from deferred to High).

**Phase B — Test Coverage Audit:**
- Output: `reviews/2026-04-24-test-gaps.md` (12 proposed tests, not yet implemented).
  - Priority 1 — 8 regression guards (pass today, lock current correct behavior): Frequency parser rejects negative/overflow, parser is case-sensitive, auto-close date-underflow returns Err, auto-close skips already-closed entries, retry backoff never exceeds cap, `split_frontmatter_and_body` edge cases, `split_minutes_for_display` exact-divisor policy.
  - Priority 2 — 2 tripwires for Critical bugs: `Frequency::Monthly` day-31 current-behavior test, `extract_frontmatter_properties` YAML-list-terminates current-behavior test. Both assert today's buggy behavior and will fail automatically once fixed in Phase C.
  - Priority 3 — 2 edge-case fill-ins: `reorder_groups_after_drop` empty/single-element, `sanitize_filename` Windows forbidden chars.
  - Skipped with rationale: cross-crate parity test (needs plumbing first), sync-buffer mid-flush interleaving (belongs with Phase C fix).
- Implementation deferred: per feedback `test-gap audit proposals first`, user triages proposals before any test code lands.

**Phase C — Fix Cycle:**
- Pending. User will go through each review doc one-by-one with an LLM in a fresh context to triage each finding and decide disposition (fix / defer / keep).

**Feedback for Next Cycle:**
- `reviews/` being git-ignored means `project.md` is the durable record — keep finding summaries in the session log row, not just the review file.
- Sonnet-on-Perf+Bloat vs Opus-on-Security+Logic split worked well: Opus caught the cross-file parity bug (Critical #2), Sonnet caught the missed `on_item_modified` regression from Cycle 1. Use same split pattern next cycle.
- "Deferred" status markers from Cycle 1 are not reliable once code grows — `append_and_apply` went from 2 to 3 copies. Deferrals should carry a trip-wire condition for revisit.
- **Daily Flow consistency visualizer (deferred to Cycle 3)** — visualizer was built around the Cycle 1 assumption of `Daily`-only routines and shows a 7-day window. Cycle 2 added `Weekly` / `Biweekly` / `Monthly` / `Custom(N)` frequencies, so for any routine whose period > 7 days the visualizer cannot render meaningful "consistency". This is a **frontend↔backend contract drift**: backend frequencies grew but the UI's display assumption didn't. Trip-wire surfaced during Logic Critical #1 triage 2026-04-25. Cycle 3 work needs to either (a) widen the window per-routine to ≥ 2× period (Monthly = 60-day window, Custom(N) = 2N days), (b) drop "consistency" for low-frequency routines and show a different metric (e.g. "last 6 occurrences" boolean strip), or (c) tier the UI: keep the 7-day strip for Daily/Weekly, swap to a calendar-grid completion view for Monthly+. Likely needs a Planning-session decision before implementation.

**Milestone Commit:** [TBD at end of Session 6 Phase C]

---

## Cycle 3: Budget Feature

### Session 4: Planning

**Date:** 2026-05-02

**Feedback Incorporated from Cycle 2:**
- Document-then-triage Session 6 process (Cycle 2 mid-cycle pivot) is now canonical — see `project_process_evolution.md`. Cycle 3 will repeat: 4 review docs + test-coverage doc generated upfront, walked one at a time, test-coverage last with most expected to be already-fix-cycle-absorbed.
- Subagent default model = `opus` (per `feedback_subagent_default_model.md`); cheaper models only on user opt-in for token-budget reasons.
- No parallel worktrees (per `feedback_parallel_agents_cost.md`). Sequential execution.
- New feedback `feedback_prefer_integration_over_rewrite.md` saved 2026-05-01 — drove the hledger-as-live-engine decision (option (iii) "events as primary, hledger export-only" was rejected by the user as a near-rewrite of bookkeeping logic).
- MEMORY.md compacted 26.0 KB → 8.9 KB; project-local `PROJECT_PROCESS.md` got a sync-banner pointing at `setup_files/PROJECT_PROCESS.md` to prevent the staleness Cycle 2 hit.

**Embedded Research:**
- Original Cycle 1 architecture (`architecture.md`) had "Financial: hledger CLI + Paisa visualization via embedded WebView + Tailscale". Session 4 surfaced two issues: (a) Paisa's UI is overwhelming and was likely part of why the user's prior Paisa attempt failed, (b) Paisa-on-mobile compatibility was unverified. User overwrote the decision: drop Paisa, build custom Dioxus UI.
- hledger handles multi-currency natively via commodities + `P` directives + inline `@` rates. Frankfurter (free, ECB-sourced, no API key) chosen for daily-rate fallback.
- Mindee free tier: 250 credits/month across all APIs combined. User volume probably fits; Gemini fallback covers any cap-exceeded cases.

**Objective:** Add the budget feature, leaving omni-me with three of the core features built (notes + routines + budget). Prepares for Cycle 4 polish + stable-v1 release.

**Scope:**
- **Tier 1 — must-have (15 items):**
  - **A1** multi-currency, **A2** business/personal tags, **A3** chart-of-accounts (audited externally pre-import), **A4** investments-distinct (handled via hledger account types)
  - **D1** import existing hledger journal, **D3** account audit at import-time
  - **C1** email body, **C2** PDF (incl. paystubs), **C4** in-person photo + description, **C5** file-attachment storage
  - **W1** reconciliation, **W2** multi-account, **W3** recurring detection, **W4** budget/forecast, **W5** investment value updates (folded into W2/W3 capture flow — same shape as bank statement transactions)
  - **R1** financial-health glance, **R2** ad-hoc queries
- **Deferred to Cycle 4 / post-v1 (2 items):** R3 self-employment tracking (depends on stored data shape), R4 tax form validation reports
- **Out of scope for omni-me entirely:** Pre-Cycle-3 user-initiated cleanup of existing hledger journal happens in a separate Claude session; omni-me's import is parser+projection against clean input, not a data-cleaning surface.

**Key Decisions:**
- **Drop Paisa.** Custom Dioxus UI on both mobile and desktop. Cycle 1 architecture decision explicitly overwritten with rationale captured above.
- **hledger as live engine.** Events → projections write to hledger journal file → hledger CLI for queries → custom Dioxus UI. Mirrors Notes' (events → projections → SurrealDB → search → UI) pattern. Journal file is itself a projection, regenerable from events; events stay source of truth (sync, audit, replay).
- **A2 tag-based** business/personal separation (`type:business` / `type:personal` posting tags) chosen over virtual accounts because user has no separate business bank account; tag-based handles cross-account business purchases naturally; if a separate business account is opened later, tags still work (new account, all postings tagged `type:business` by default).
- **A1 multi-currency** — CAD base, native commodity per posting (always — receipt's currency is what we store), inline `@` FX rates extracted from receipts when present, Frankfurter daily `P` directives as fallback for gaps. ~couple USD transactions per month → polish-tier path.
- **A3 chart of accounts** — audited externally by user (separate Claude session) BEFORE Phase 6 import. omni-me's in-app audit at import time is "preview, accept/skip per account, basic edits", not full normalization.
- **A4 investments** — naturally handled via hledger account types (investment accounts as `Assets:Investments:...`); investment statement value-updates and bank-account transactions take the same capture flow (similar form factor).
- **LLM extraction routing (server-side only per `feedback_llm_server_side.md`):** Mindee Receipts (photos), Mindee Invoices (PDFs incl. paystubs), Gemini Flash structured-output (text + fallback for any Mindee miss). Verification: line-item-sum == total, gross - deductions == net, confidence threshold gates Gemini outputs.
- **Attachments (C5):** content-addressable server-side blob store at `/blobs/<sha256>` over HTTP, on-device LRU cache (~200MB cap) with **manual clear button in Settings** (escape hatch for misbehaving cache). PDF + PNG + JPEG MVP; HEIC / spreadsheets / plain-text deferred. Single attachment per transaction MVP; multi-attachment is Cycle 4.
- **Mobile UI is a subset of desktop**, split along time-sensitive (capture, R1 glance, transaction list/edit) vs session-work (W1 reconciliation, W4 budget setup, R2 query builder, D1 import) lines. Same nav shell as Cycle 2.

**Cross-Cycle Commitments (durable, set in Session 4):**
- **Cycle 4 = polish + logo + stable v1 + branch-gate.** Almost entirely UI polish, edges, testing. Logo replaces default Tauri assets across desktop + Android. End of Cycle 4 stamps stable v1; future feature work happens on branches with merge gates to protect stable.
- **Daily-use baseline shifted to end-of-Cycle-4** (originally end-of-Cycle-2 per Session 1). Editor-vs-Obsidian friction was the gap; explicitly deferred to Cycle 4 polish.

**High-Level Phases:**
0. Risk Validation POCs (hledger-on-Android, blob-roundtrip, Mindee-from-mobile)
1. Core Foundation (events + projections + Tauri commands)
2. Server-Side Capture Pipeline (Mindee + Gemini + verification + FX + `/blobs`)
3. Frontend Capture Flows (4 input modalities + attachment cache + Settings)
4. Transactions Surface + R1 (list, detail, accounts, health-glance)
5. Workflows desktop-heavy (W1 reconciliation, W3 recurring, W4 budget)
6. Import (D1 + D3) — runs after user pre-cleanup completes
7. R2 + Settings polish + backlog stretch (Daily Flow visualizer, FlushFailed wiring, editor.rs:179, FORCE_GENERIC_DIRS, scheduler `Arc<dyn EventStore>`, seconds duration unit)

**Parallelization:** Sequential. No worktree subagents (per `feedback_parallel_agents_cost.md`).

**Task Count:** ~55 tasks across 8 phases (10 in Phase 1, 10 in Phase 2, 8 in Phase 3, 6 in Phase 4, 7 in Phase 5, 5 in Phase 6, 10 in Phase 7 incl. 6 backlog items, 4 in Phase 0).

**Reference:** See `tasks.md` for atomic task breakdown with sequential execution map.

---

## Cycle N: [Continue pattern as needed]

---

## Phase 2 Known Limitations (post-implementation 2026-05-17)

Items found via the end-of-Phase-2 audit that are *known* and either intentionally deferred or scoped to a later phase. Documented here so code review + Phase 3 work has them on the radar.

- **JournalFile projection covers only 3 of 16 budget event types** (`TransactionRecorded` + `AccountAdded` + `ExchangeRateRecorded`). The remaining 13 (Categorized, Tagged, Updated, Deleted, Cleared, Merged, Budget*, Recurring*, ...) don't update the on-disk hledger file. As soon as you delete/merge/tag a transaction, the file diverges from SurrealDB state. No regenerate-on-mutation path yet. **Fix scope:** add per-event handlers + a `regenerate_from_events` fallback path for events that can't be appended (mutations). Phase 4/5 polish.
- **WS driver `description` field is a fragile fallback chain** (`description` → `spendMerchant` → `counterPartyName` → `type`). Different WS transaction types yield inconsistently-shaped descriptions: card payments show merchant name, transfers show recipient, position adjustments show just "BUY". Users will see this inconsistency during review.
- **No frontend bindings for the 12 budget Tauri commands.** `record_transaction`, `list_transactions`, `add_account`, etc. are wired into `invoke_handler` but no Dioxus page calls them. Currently orphan code only callable from DevTools. **Fix:** Phase 3 capture-flow UI work.
- **`tracing-subscriber` env filter not unified across Tauri + server.** Two binaries, two `.init()` calls, two default filters. Cycle 4 polish should consolidate.
- **`imap-proto` future-Rust warning** — transitive dep of `imap` crate uses syntax some future Rust will reject. Waiting for upstream maintainer release.
- **WS first-run still needs interactive OTP entry.** After password rotation today, the saved session at `~/.local/share/omni-me/ws-session.json` was invalidated; until interactive re-auth provides a fresh OTP, the WS source fails every tick (driver correctly logs `OTP required`, scheduler backs off exponentially capped at 1h). **Fix:** Phase 3/4 "WS session expired, click to re-auth" UI affordance.
- **No surface for "WS session expired" alerts** — failure is only visible via `RUST_LOG=omni_me_core=info` server logs. Pairs with the previous item.
- **SurrealDB tempfile race in tests** — known + documented in `~/.claude/projects/.../memory/project_known_bugs.md` (reproduced 3× during this session). Tests pass on rerun.
- **`cargo tauri dev` runtime not smoke-tested this session.** Only `cargo run -p omni-me-server` got the boot smoke test. Tauri startup path changed (auto-import wiring removed) but app wasn't actually booted. **Fix:** verify on first Phase 3 UI work.
- **Wise `fxRate` and `totalFees` surface as posting tags, not typed fields** — see Session 5 log entry. Phase 4 polish could promote to typed `FxRate` + separate fee posting if a reliable Wise direction convention is confirmed.
- **Auto-import source health bugs (surfaced 2026-05-18 device test)** — three independent issues observed in `/auto_import/status` on a running server:
  - **`gmail_work` + `yahoo`: intermittent `save cursor: Connection uninitialised`.** Same error in both; only fires on ticks that actually have an email to save cursor state for (most ticks have 0 events and succeed). Likely a SurrealDB connection-state issue specific to the cursor-store write path. The handlers still consume the events; it's the cursor save that fails.
  - **`gmail_personal`: manual tick returns HTTP 502 after successfully producing a batch.** The `AutoImportBatchProposed` event is appended (verified — phone synced and rendered the walmart receipt batch correctly during device test) but the HTTP handler returns Bad Gateway. Some downstream error after the append.
  - **`wealthsimple-python`: OTP required (genuine credential expiry).** Saved session at `~/.local/share/omni-me/ws-session.json` invalidated; needs interactive re-auth. Already known per the earlier WS session-expired entry above — re-tripped after the next rotation.

---

## Phase 3 Known Gaps (post-implementation 2026-05-17)

Capture-flow bundle (3.1 + 3.2 + 3.4 + 3.5 + 3.6) ships at the UI surface today; 3.3 is partial (manifest entry only, gitignored). End-to-end **mock** flow visually verified via Playwright; **real-server + Android paths NOT smoke-tested**. This section is the honest gap list — what's intentionally deferred vs. what would burn the user.

### Likely to bite soon

- ~~**Attachment bytes are dropped after extraction.**~~ **RESOLVED 2026-05-17 via Phase 3.7.** Server `/documents/extract` now accepts `?attach=true`, stores bytes content-addressably, returns `AttachmentRef`; Tauri client mirrors to `<app_data>/attachments/<sha256>` LRU cache and threads the ref through to the `TransactionRecorded` event. `TransactionForm` shows an "Attachment saved" line in lieu of the deferred thumbnail (thumbnail render lives in Phase 4 transaction detail view, which `fetch_attachment` already supports).
- **No balance validation in `TransactionForm`.** The form doesn't check that postings sum to zero per commodity. User can submit `Expenses:Foo 10 CAD` + `Assets:Bar 10 CAD` (both positive); backend will store an unbalanced transaction. [[project-unmatched-account-pattern]] depends on `Unmatched` clearing to zero — an unbalanced submission breaks that invariant. **Fix:** validate per-commodity sum before `invoke_record_transaction`; auto-balance by injecting an `Unmatched` posting if the user wants one but didn't enter it.
- **Amount parser laxness.** Frontend uses `f64::parse` for amount validation — accepts `1e2`, `0.1.2`, scientific notation. Backend's `rust_decimal::Decimal::from_str` is stricter and will fail. Frontend says OK, server says no. **Fix:** add a stricter amount-pattern regex on the form.
- **Tab-switch during capture race.** User clicks Photo, picks file, then taps a different top-level tab before the ~1.2s extract round trip completes. `FinancesPage` unmounts; the `spawn`ed task may try to `state.set(...)` on a Signal whose scope no longer exists. Dioxus 0.7.2 likely cancels spawned futures on scope drop but I haven't verified the exact semantics. **Fix scope:** verify and add a stop-token if needed; minimum, ensure no panic on dropped-scope writes. **Broader rework deferred to Cycle 4** (see `tasks.md` → "Cross-submodule state management"): lift in-flight captures to a top-level shared store keyed by capture-id so cross-tab navigation doesn't destroy in-flight work.
- **`cargo tauri dev` runtime path NOT tested.** Carries forward from the Phase 2 known-limitations list. Frontend mock branch is the only thing exercised; the real Tauri-IPC branches for `extract_document` and `record_transaction` are unobserved.
- **Real Gemini extraction unverified.** The mock returns a canned `Loblaws — Groceries` receipt regardless of input. Actual Gemini response shape, confidence calibration, and error modes against a real Walmart receipt photo are unknown. The `extract_document` server endpoint compiles + boots, but no live round trip has happened.

### UX gaps worth knowing

- **No "saved" confirmation.** After `Save transaction`, the form unmounts and the user lands on Finances Home with no toast / banner. Transaction "vanishes" until Phase 4 transactions list lands. **Fix:** small ephemeral toast in `HomeView` ("Saved at HH:MM") that fades after 3s.
- **No account / commodity autocomplete.** Users type `Expenses:Groceries` by hand for every transaction. `list_accounts` Tauri command exists from Phase 1 but is not wired into the form. **Fix:** `use_future` to load accounts on mount + datalist-based autocomplete.
- **PDF retry uses stale hint.** If the user picks `bank_statement`, upload fails, then changes the radio to `paystub`, clicking Retry uses the original `bank_statement` (value was captured into the error variant at failure time). Either is defensible; document the choice or read the hint signal fresh inside the retry closure.
- **No network timeout on the `reqwest` client call.** If the server hangs (slow Gemini, network blip), the Tauri command + frontend spinner block indefinitely. **Fix:** `reqwest::ClientBuilder::default().timeout(Duration::from_secs(45))` on `AppState.http` init.
- **Mobile touch-target concerns.** `Remove` button on posting rows is ~24px (below iOS 44px / Android 48px guideline). `capture="environment"` on the photo input is unverified on actual Android.
- **Form date validation laxness.** HTML date input rejects format errors in modern browsers but doesn't catch invalid dates like 2026-02-31 in some configurations. Frontend says OK; backend `NaiveDate` parse fails.

### Architecture / deferred (Cycle 4 polish territory)

- **3.3 share-target — manifest-only.** Edit sits in `gen/android/.../AndroidManifest.xml` which is gitignored. Durable home requires Tauri 2 override convention (likely `tauri.conf.json` `android.manifestExtras` or a side-by-side template). The Rust intent-handler glue is a separate item; needs `tauri-plugin-deep-link` or custom JNI. **Defer to a dedicated Android session** with the APK build pipeline active.
- **Frontend types duplicate backend types.** `ExtractedDraft` / `TransactionFormDraft` / `PostingInput` mirror `core::extraction::ExtractionResult` / `commands::budget::TransactionDraft` / `core::events::Posting`. No shared types crate — drift will eventually bite (renames silently break deserialization). Per [[feedback-breaking-changes-ok]] this is acceptable pre-daily-use.
- **No tests for the Phase 3 code.** Bridge mock functions, `finances.rs` components, `extract_handler` route, `extract_document` Tauri command — all untested. Per [[feedback-test-gap-audit-yield]] the user prefers fixes-with-tests-inline; nothing's broken yet, so deferred.
- **Server `/documents/extract` has no auth.** Same situation as sync routes — acceptable behind Tailscale per [[project-auth-deferred]]. Worth calling out separately because LLM calls cost money: an exposed unauthed server burns Gemini credits, not just CPU.
- **`Vec<u8>` Tauri IPC inefficiency.** Bytes serialize as JSON arrays — ~3× wire overhead for multi-MB photos. Tempfile-based handoff or Tauri's binary IPC would be faster but adds coupling. Acceptable for Phase 3 MVP.
- **`HintRadio` `'static` constraint.** Component takes `&'static str` for value/label, locking the PDF hint set at compile time. Fine for now; would need refactor if hints ever come from user config.
- **15 MB document body limit.** Modern iPhones produce 5-10 MB receipt photos easily. Heuristic limit; bump if 413s show up.

---

## Lifecycle Events

### Status Change: [Event Type - e.g., "Paused"]
**Date:** [Date]
**Reason:** [Why the state changed - e.g., "Motivation changed - need shifted to different priority"]
**Potential Resume Conditions:** [If paused, under what conditions might you resume]
**Notes:** [Additional context]

---

### Status Change: [Event Type]
**Date:** [Date]
**Reason:** [Why]
**Notes:** [Context]
