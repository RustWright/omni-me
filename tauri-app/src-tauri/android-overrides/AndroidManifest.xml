<?xml version="1.0" encoding="utf-8"?>
<!--
  Omni-Me's customized Android manifest. `tauri android init` regenerates a
  stock manifest under `gen/android/`, which we then overwrite from here via
  `build.rs` (see `apply_android_overrides`) on every build. Edit this file
  to add intent filters, permissions, etc. — the generated file is treated
  as throwaway.

  Custom additions vs. Tauri's default:
    - SEND intent-filter for image/* and application/pdf so Omni-Me appears
      in the system share sheet. The Kotlin handler in MainActivity.kt
      catches the URI, writes it to filesDir, and the WASM frontend pulls
      the pending share via `take_pending_share_intent` on mount.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- AndroidTV support -->
    <uses-feature android:name="android.software.leanback" android:required="false" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.omni_me_app"
        android:usesCleartextTraffic="${usesCleartextTraffic}">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:launchMode="singleTask"
            android:label="@string/main_activity_title"
            android:name=".MainActivity"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <!-- AndroidTV support -->
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
            <!-- Phase 3.3: share-target for receipts (image/*) and statements (PDF). -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
                <data android:mimeType="application/pdf" />
            </intent-filter>
        </activity>

        <provider
          android:name="androidx.core.content.FileProvider"
          android:authorities="${applicationId}.fileprovider"
          android:exported="false"
          android:grantUriPermissions="true">
          <meta-data
            android:name="android.support.FILE_PROVIDER_PATHS"
            android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
